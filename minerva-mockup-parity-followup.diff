diff --git a/minerva/app/pages/base.py b/minerva/app/pages/base.py
index d8ce9ea..56f2760 100644
--- a/minerva/app/pages/base.py
+++ b/minerva/app/pages/base.py
@@ -55,6 +55,10 @@ class BasePage(QtWidgets.QWidget):
     def activate(self) -> None:
         """Called when this page becomes the visible page."""
 
+    def can_deactivate(self) -> bool:
+        """Return False to cancel navigation away from this page."""
+        return True
+
     def deactivate(self) -> None:
         """Called when this page is no longer visible."""
 
diff --git a/minerva/app/pages/collections.py b/minerva/app/pages/collections.py
index bd93ed5..fca8ab5 100644
--- a/minerva/app/pages/collections.py
+++ b/minerva/app/pages/collections.py
@@ -1,402 +1,434 @@
-"""
-Collection/system overview page with metric cards, summary tables, and diagnostics.
-"""
+"""Collections dashboard: source coverage, rebuilds, imports, and diagnostics."""
 
 from __future__ import annotations
 
 import enum
 import logging
-from datetime import datetime
+import os
+import sqlite3
+import tempfile
+import uuid
+from datetime import datetime, timezone
 from pathlib import Path
 
-from PyQt6 import QtCore, QtWidgets
+from PyQt6 import QtCore, QtGui, QtWidgets
+from PyQt6.QtCore import Qt
 
 from minerva.app.app_state import AppState
 from minerva.app.pages.base import BasePage, _state_label
 from minerva.app.task_runner import TaskRunner
-from minerva.domain.collections import (
-    CollectionStatus,
-    CollectionSummary,
-    IndexOverview,
-    SystemSummary,
-)
+from minerva.domain.collections import CollectionSummary, IndexOverview, ReferenceDat, SystemSummary
 from minerva.ui.icons import Icons
-from minerva.ui.models.delegates import DisplayDelegate
+from minerva.ui.models.delegates import DisplayDelegate, ProgressDelegate
 from minerva.ui.models.record_model import ColumnSpec, RecordListModel
-from minerva.ui.models.proxy import SortFilterProxy
+from minerva.ui.widgets.activity_list import ActivityList
 from minerva.ui.widgets.metric_card import MetricCard, MetricKind
+from minerva.ui.widgets.notification_banner import NotificationBanner
 from minerva.ui.widgets.page_header import PageHeader
 from minerva.ui.widgets.section_card import SectionCard
-
-from minerva_db import MinervaDB
+from minerva_db import DEFAULT_INDEX_PATH, DEFAULT_TORRENT_DIR, MinervaDB, build_index, parse_dat_file
+from minerva_state import MinervaState
 
 log = logging.getLogger(__name__)
 
 
 class CollectionsPageState(enum.Enum):
-    """Visual states for the CollectionsPage stack."""
-
-    LOADING = "loading"  # Initial load in progress
-    EMPTY = "empty"      # No collections found
-    RESULTS = "results"  # Collections displayed
-    ERROR = "error"      # Load failed
-
-
-# ── Column specs for CollectionSummary ──────────────────────────────────────
-
-_COLLECTION_COLUMNS: list[ColumnSpec[CollectionSummary]] = [
-    ColumnSpec(
-        header="Collection",
-        accessor=lambda s: s.name,
-    ),
-    ColumnSpec(
-        header="Systems",
-        accessor=lambda s: s.systems,
-    ),
-    ColumnSpec(
-        header="Files",
-        accessor=lambda s: f"{s.files:,}",
-    ),
-    ColumnSpec(
-        header="Status",
-        accessor=lambda s: s.status.value.title(),
-    ),
-]
+    LOADING = "loading"
+    EMPTY = "empty"
+    RESULTS = "results"
+    ERROR = "error"
+
 
-CollectionTableModel = RecordListModel[CollectionSummary]
-
-# ── Column specs for SystemSummary ──────────────────────────────────────────
-
-_SYSTEM_COLUMNS: list[ColumnSpec[SystemSummary]] = [
-    ColumnSpec(
-        header="System",
-        accessor=lambda s: s.system,
-    ),
-    ColumnSpec(
-        header="Files",
-        accessor=lambda s: f"{s.files:,}",
-    ),
-    ColumnSpec(
-        header="Source",
-        accessor=lambda s: "\u2713" if s.source_present else "\u2717",
-    ),
-    ColumnSpec(
-        header="Expected",
-        accessor=lambda s: f"{s.expected_files:,}" if s.expected_files else "\u2014",
-    ),
+_COLLECTION_COLUMNS = [
+    ColumnSpec("Collection", lambda row: row.name),
+    ColumnSpec("Systems", lambda row: row.systems),
+    ColumnSpec("Files", lambda row: row.files, format_fn=lambda value: f"{value:,}"),
+    ColumnSpec("Status", lambda row: row.status.value.title()),
+]
+_SYSTEM_COLUMNS = [
+    ColumnSpec("System", lambda row: row.system),
+    ColumnSpec("Files", lambda row: row.files, format_fn=lambda value: f"{value:,}"),
+    ColumnSpec("Source", lambda row: "Available" if row.source_present else "Missing"),
+    ColumnSpec("Expected", lambda row: row.expected_files if row.expected_files else -1, format_fn=lambda value: f"{value:,}" if value >= 0 else "—"),
+    ColumnSpec("Coverage", lambda row: row.coverage if row.coverage is not None else -1.0, format_fn=lambda value: f"{value:.1%}" if value >= 0 else "Unknown"),
 ]
 
-SystemTableModel = RecordListModel[SystemSummary]
 
+def _atomic_rebuild_task(torrent_dir: str, index_path: str) -> str:
+    target = Path(index_path)
+    target.parent.mkdir(parents=True, exist_ok=True)
+    fd, temp_name = tempfile.mkstemp(prefix=f".{target.name}.", suffix=".tmp", dir=target.parent)
+    os.close(fd)
+    temp = Path(temp_name)
+    temp.unlink(missing_ok=True)
+    try:
+        build_index(Path(torrent_dir), temp)
+        with sqlite3.connect(temp) as connection:
+            result = connection.execute("PRAGMA quick_check").fetchone()
+            if not result or result[0] != "ok":
+                raise RuntimeError(f"Temporary index failed quick_check: {result}")
+        os.replace(temp, target)
+        return str(target)
+    finally:
+        temp.unlink(missing_ok=True)
 
-class CollectionsPage(BasePage):
-    """Collection/system overview page with metric cards, summary tables,
-    and diagnostics.
 
-    Composes metric cards for index overview, a ``QTableView`` for
-    collection summaries, and a detail table for systems within a selected
-    collection.  Action buttons provide rebuild, integrity check, and
-    source-check capabilities.
-
-    States (``QStackedWidget``):
-        LOADING — initial load in progress
-        EMPTY   — zero collections returned
-        RESULTS — collections displayed
-        ERROR   — load failed
-    """
+class CollectionsPage(BasePage):
+    """Mockup-grade collection and index maintenance page."""
 
     def __init__(self, app_state: AppState) -> None:
         super().__init__(app_state)
-
         self._pool = QtCore.QThreadPool.globalInstance()
         self._db: MinervaDB | None = None
-        try:
-            self._db = MinervaDB()
-        except Exception:
-            log.warning("CollectionsPage: MinervaDB not available", exc_info=True)
+        self._selected_collection: str | None = None
 
-        # ── Page header ──────────────────────────────────────────────────
         self._header = PageHeader(
-            title="Index Overview",
-            subtitle="Loading\u2026",
-            parent=self,
-        )
-
-        # ── Metric cards ─────────────────────────────────────────────────
-        self._card_collections = MetricCard("Collections", "\u2014", parent=self)
-        self._card_systems = MetricCard("Systems", "\u2014", parent=self)
-        self._card_files = MetricCard("Files", "\u2014", parent=self)
-        self._card_db_size = MetricCard("DB Size", "\u2014", parent=self)
-        self._card_last_build = MetricCard("Last Build", "\u2014", parent=self)
-
-        metrics_row = QtWidgets.QHBoxLayout()
-        metrics_row.setSpacing(8)
-        metrics_row.addWidget(self._card_collections)
-        metrics_row.addWidget(self._card_systems)
-        metrics_row.addWidget(self._card_files)
-        metrics_row.addWidget(self._card_db_size)
-        metrics_row.addWidget(self._card_last_build)
-
-        # ── Collection table ─────────────────────────────────────────────
-        self._collection_model = CollectionTableModel(
-            [], _COLLECTION_COLUMNS, parent=self,
-        )
-        self._collection_proxy = SortFilterProxy(self)
-        self._collection_proxy.setSourceModel(self._collection_model)
-
-        self._collection_view = QtWidgets.QTableView(self)
-        self._collection_view.setModel(self._collection_proxy)
-        self._collection_view.setSelectionBehavior(
-            QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows,
-        )
-        self._collection_view.setSelectionMode(
-            QtWidgets.QAbstractItemView.SelectionMode.SingleSelection,
+            "Collections",
+            "Manage indexed sources, rebuild the database, and inspect collection coverage.",
         )
+        self._rebuild_btn = self._header.add_action("Rebuild index", Icons.refresh(), primary=True)
+        self._import_btn = self._header.add_action("Import DAT", Icons.upload())
+        self._verify_btn = self._header.add_action("Verify paths", Icons.check())
+        self._rebuild_btn.clicked.connect(self._on_rebuild)
+        self._import_btn.clicked.connect(self._on_import_dat)
+        self._verify_btn.clicked.connect(self._on_check_sources)
+
+        self._card_collections = MetricCard("Collections", "—", icon=Icons.collections())
+        self._card_systems = MetricCard("Systems", "—", MetricKind.SUCCESS, icon=Icons.app())
+        self._card_files = MetricCard("Indexed files", "—", MetricKind.WARNING, icon=Icons.file())
+        self._card_db_size = MetricCard("Database size", "—", MetricKind.PURPLE, icon=Icons.database())
+        metrics = QtWidgets.QHBoxLayout()
+        for card in (self._card_collections, self._card_systems, self._card_files, self._card_db_size):
+            metrics.addWidget(card)
+
+        self._collection_model = RecordListModel([], _COLLECTION_COLUMNS, self)
+        self._collection_view = QtWidgets.QTableView()
+        self._collection_view.setModel(self._collection_model)
+        self._collection_view.setSelectionBehavior(QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows)
+        self._collection_view.setSelectionMode(QtWidgets.QAbstractItemView.SelectionMode.SingleSelection)
         self._collection_view.verticalHeader().hide()
-        self._collection_view.setSortingEnabled(True)
-        self._collection_view.selectionModel().selectionChanged.connect(
-            self._on_collection_selected,
-        )
-
-        header_c = self._collection_view.horizontalHeader()
-        header_c.setStretchLastSection(False)
-        header_c.setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
-        header_c.resizeSection(1, 100)
-        header_c.resizeSection(2, 100)
-        header_c.resizeSection(3, 100)
-
-        collection_card = SectionCard(
-            title="Collections",
-            icon=Icons.collections(),
-            parent=self,
-        )
+        self._collection_view.setItemDelegate(DisplayDelegate(self._collection_view))
+        self._collection_view.horizontalHeader().setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
+        self._collection_view.selectionModel().selectionChanged.connect(self._on_collection_selected)
+        collection_card = SectionCard("Collections", Icons.collections())
         collection_card.content_layout().addWidget(self._collection_view)
+        collection_card.setMinimumWidth(245)
+        collection_card.setMaximumWidth(340)
 
-        # ── System table ─────────────────────────────────────────────────
-        self._system_model = SystemTableModel([], _SYSTEM_COLUMNS, parent=self)
-        self._system_proxy = SortFilterProxy(self)
-        self._system_proxy.setSourceModel(self._system_model)
-
-        self._system_view = QtWidgets.QTableView(self)
-        self._system_view.setModel(self._system_proxy)
-        self._system_view.setSelectionBehavior(
-            QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows,
-        )
+        self._system_model = RecordListModel([], _SYSTEM_COLUMNS, self)
+        self._system_view = QtWidgets.QTableView()
+        self._system_view.setModel(self._system_model)
+        self._system_view.setSelectionBehavior(QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows)
         self._system_view.verticalHeader().hide()
-        self._system_view.setSortingEnabled(True)
-
-        header_s = self._system_view.horizontalHeader()
-        header_s.setStretchLastSection(False)
-        header_s.setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
-        header_s.resizeSection(1, 100)
-        header_s.resizeSection(2, 80)
-        header_s.resizeSection(3, 100)
-
-        system_card = SectionCard(
-            title="Systems",
-            icon=Icons.search(),
-            parent=self,
-        )
-        system_card.content_layout().addWidget(self._system_view)
-
-        # ── Action buttons ───────────────────────────────────────────────
-        self._rebuild_btn = QtWidgets.QPushButton("Rebuild Index", self)
-        self._rebuild_btn.setObjectName("primaryButton")
-        self._rebuild_btn.clicked.connect(self._on_rebuild)
-
-        self._verify_btn = QtWidgets.QPushButton("Verify Integrity", self)
-        self._verify_btn.setObjectName("subtleButton")
-        self._verify_btn.clicked.connect(self._on_verify_integrity)
-
-        self._check_sources_btn = QtWidgets.QPushButton("Check Sources", self)
-        self._check_sources_btn.setObjectName("subtleButton")
-        self._check_sources_btn.clicked.connect(self._on_check_sources)
-
-        button_row = QtWidgets.QHBoxLayout()
-        button_row.setSpacing(8)
-        button_row.addWidget(self._rebuild_btn)
-        button_row.addWidget(self._verify_btn)
-        button_row.addWidget(self._check_sources_btn)
-        button_row.addStretch(1)
-
-        # ── Diagnostics output ───────────────────────────────────────────
-        self._diagnostics = QtWidgets.QLabel("")
-        self._diagnostics.setObjectName("mutedLabel")
-        self._diagnostics.setWordWrap(True)
-
-        # ── Results widget layout ────────────────────────────────────────
-        results_widget = QtWidgets.QWidget()
-        results_layout = QtWidgets.QVBoxLayout(results_widget)
+        self._system_view.setAlternatingRowColors(True)
+        self._system_view.setItemDelegate(DisplayDelegate(self._system_view))
+        self._system_view.setItemDelegateForColumn(4, ProgressDelegate(self._system_view))
+        self._system_view.horizontalHeader().setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
+        for column, width in {1: 90, 2: 90, 3: 95, 4: 100}.items():
+            self._system_view.horizontalHeader().resizeSection(column, width)
+        self._system_search = QtWidgets.QLineEdit()
+        self._system_search.setPlaceholderText("Search systems…")
+        self._system_search.addAction(Icons.search(), QtWidgets.QLineEdit.ActionPosition.LeadingPosition)
+        self._system_search.textChanged.connect(self._filter_systems)
+        systems_panel = QtWidgets.QWidget()
+        systems_layout = QtWidgets.QVBoxLayout(systems_panel)
+        systems_layout.setContentsMargins(0, 0, 0, 0)
+        systems_layout.addWidget(self._system_search)
+        systems_layout.addWidget(self._system_view, 1)
+
+        self._detail_title = QtWidgets.QLabel("Select a collection")
+        self._detail_title.setObjectName("sectionTitle")
+        self._detail_meta = QtWidgets.QLabel("")
+        self._detail_meta.setObjectName("mutedLabel")
+        self._detail_meta.setWordWrap(True)
+        self._open_source_btn = QtWidgets.QPushButton(Icons.folder_open(), "Open source folder")
+        self._open_source_btn.clicked.connect(self._open_source_folder)
+        self._remove_collection_btn = QtWidgets.QPushButton(Icons.trash(), "Remove collection")
+        self._remove_collection_btn.setObjectName("dangerButton")
+        self._remove_collection_btn.clicked.connect(self._remove_collection)
+        detail_card = SectionCard("Collection details", Icons.database())
+        detail_card.content_layout().addWidget(self._detail_title)
+        detail_card.content_layout().addWidget(self._detail_meta)
+        detail_card.content_layout().addStretch(1)
+        detail_card.content_layout().addWidget(self._open_source_btn)
+        detail_card.content_layout().addWidget(self._remove_collection_btn)
+        detail_card.setMinimumWidth(245)
+        detail_card.setMaximumWidth(310)
+
+        splitter = QtWidgets.QSplitter(Qt.Orientation.Horizontal)
+        splitter.addWidget(collection_card)
+        splitter.addWidget(systems_panel)
+        splitter.addWidget(detail_card)
+        splitter.setSizes([270, 720, 270])
+        splitter.setStretchFactor(1, 1)
+        self._splitter = splitter
+
+        self._health_title = QtWidgets.QLabel("Not checked")
+        self._health_title.setObjectName("metricLabel")
+        self._health_text = QtWidgets.QLabel("Run an integrity check to validate the index.")
+        self._health_text.setObjectName("mutedLabel")
+        self._health_text.setWordWrap(True)
+        health_btn = QtWidgets.QPushButton("Run integrity check")
+        health_btn.clicked.connect(self._on_verify_integrity)
+        health_card = SectionCard("Database health", Icons.status_success())
+        health_card.content_layout().addWidget(self._health_title)
+        health_card.content_layout().addWidget(self._health_text)
+        health_card.content_layout().addWidget(health_btn)
+
+        self._missing_title = QtWidgets.QLabel("—")
+        self._missing_title.setObjectName("warningLabel")
+        self._missing_text = QtWidgets.QLabel("Source paths have not been checked.")
+        self._missing_text.setObjectName("mutedLabel")
+        self._missing_text.setWordWrap(True)
+        missing_btn = QtWidgets.QPushButton("View missing paths")
+        missing_btn.clicked.connect(self._show_missing_paths)
+        missing_card = SectionCard("Missing paths", Icons.status_warning())
+        missing_card.content_layout().addWidget(self._missing_title)
+        missing_card.content_layout().addWidget(self._missing_text)
+        missing_card.content_layout().addWidget(missing_btn)
+
+        self._imports = ActivityList(max_items=4)
+        imports_card = SectionCard("Recent imports", Icons.upload())
+        imports_card.content_layout().addWidget(self._imports)
+
+        self._performance = QtWidgets.QLabel("")
+        self._performance.setObjectName("mutedLabel")
+        self._performance.setWordWrap(True)
+        performance_card = SectionCard("Performance", Icons.chart())
+        performance_card.content_layout().addWidget(self._performance)
+
+        diagnostics = QtWidgets.QHBoxLayout()
+        for card in (health_card, missing_card, imports_card, performance_card):
+            diagnostics.addWidget(card)
+
+        results = QtWidgets.QWidget()
+        results_layout = QtWidgets.QVBoxLayout(results)
         results_layout.setContentsMargins(0, 0, 0, 0)
-        results_layout.setSpacing(8)
+        results_layout.setSpacing(10)
         results_layout.addWidget(self._header)
-        results_layout.addLayout(metrics_row)
-        results_layout.addLayout(button_row)
-        results_layout.addWidget(self._diagnostics)
-        results_layout.addWidget(collection_card)
-        results_layout.addWidget(system_card)
-
-        # ── State stack ──────────────────────────────────────────────────
-        self._stack = QtWidgets.QStackedWidget(self)
-
-        self._label_loading = _state_label("Loading index overview\u2026")
-        self._stack.addWidget(self._label_loading)  # 0
-
-        self._label_empty = _state_label("No collections found")
-        self._stack.addWidget(self._label_empty)  # 1
-
-        self._label_error = _state_label("Failed to load collections")
-        self._stack.addWidget(self._label_error)  # 2
-
-        self._stack.addWidget(results_widget)  # 3
-
-        # ── Layout ───────────────────────────────────────────────────────
-        layout = QtWidgets.QVBoxLayout(self)
-        layout.setContentsMargins(0, 0, 0, 0)
-        layout.addWidget(self._stack)
-
-        # Defer initial data load
-        QtCore.QTimer.singleShot(0, self._load_data)
-
-    # ── State management ─────────────────────────────────────────────────
+        results_layout.addLayout(metrics)
+        results_layout.addWidget(splitter, 1)
+        results_layout.addLayout(diagnostics)
+
+        self._stack = QtWidgets.QStackedWidget()
+        self._stack.addWidget(_state_label("Loading index overview…"))
+        self._stack.addWidget(_state_label("No indexed collections found"))
+        self._error = _state_label("Failed to load collections")
+        self._stack.addWidget(self._error)
+        self._stack.addWidget(results)
+        root = QtWidgets.QVBoxLayout(self)
+        root.setContentsMargins(0, 0, 0, 0)
+        root.addWidget(self._stack)
+        self._missing_sources: list[dict] = []
+        QtCore.QTimer.singleShot(0, self.refresh)
 
     def _set_state(self, state: CollectionsPageState) -> None:
-        """Switch the visible stack widget for *state*."""
-        index_map = {
+        self._stack.setCurrentIndex({
             CollectionsPageState.LOADING: 0,
             CollectionsPageState.EMPTY: 1,
             CollectionsPageState.ERROR: 2,
             CollectionsPageState.RESULTS: 3,
-        }
-        self._stack.setCurrentIndex(index_map[state])
+        }[state])
 
-    # ── Data loading ─────────────────────────────────────────────────────
-
-    def _load_data(self) -> None:
-        """Load index overview and collection summaries (runs on init)."""
-        if self._db is None:
-            self._set_state(CollectionsPageState.ERROR)
-            return
+    def activate(self) -> None:
+        self.refresh()
 
+    def refresh(self) -> None:
+        self._set_state(CollectionsPageState.LOADING)
         try:
+            self._db = MinervaDB()
             overview = self._db.get_index_overview()
             summaries = self._db.get_collection_summaries()
-        except Exception:
-            log.warning("Failed to load collection data", exc_info=True)
+            runs = self._db.get_index_runs(5)
+            refs = MinervaState().list_reference_dats()
+        except Exception as exc:
+            self._error.setText(str(exc))
             self._set_state(CollectionsPageState.ERROR)
             return
-
         self._apply_overview(overview)
         self._collection_model.set_records(summaries)
-
-        if summaries:
-            self._set_state(CollectionsPageState.RESULTS)
+        self._imports.clear()
+        for reference in refs[:4]:
+            self._imports.add_event("check", f"{reference.name} · {reference.entry_count:,} expected entries", reference.imported_at)
+        if not refs:
+            self._imports.add_event("info", "No reference DATs · Use Import DAT to add expected counts", "")
+        latest = runs[0] if runs else None
+        if latest:
+            elapsed = (latest.get("elapsed_ms") or 0) / 1000
+            self._performance.setText(
+                f"Last build time\n{elapsed:.1f} s\n\nIndexed files\n{latest.get('file_count', 0):,}\n\n"
+                f"Torrent sources\n{latest.get('torrent_count', 0):,}\n\nErrors\n{latest.get('error_count', 0):,}"
+            )
         else:
-            self._set_state(CollectionsPageState.EMPTY)
+            self._performance.setText("No build history is available.")
+        self._set_state(CollectionsPageState.RESULTS if summaries else CollectionsPageState.EMPTY)
+        if summaries:
+            target = next((index for index, item in enumerate(summaries) if item.name == self._selected_collection), 0)
+            self._collection_view.selectRow(target)
 
     def _apply_overview(self, overview: IndexOverview) -> None:
-        """Update metric cards and header with index overview data."""
-        self._header.set_subtitle(
-            f"{overview.collections} collections, "
-            f"{overview.systems} systems, "
-            f"{overview.files:,} files",
-        )
-
         self._card_collections.set_value(str(overview.collections))
         self._card_systems.set_value(str(overview.systems))
         self._card_files.set_value(f"{overview.files:,}")
+        self._card_db_size.set_value(self._format_size(overview.database_size))
+        if overview.last_build:
+            self._card_db_size.set_subtitle(f"Last rebuild {overview.last_build:%Y-%m-%d %H:%M}")
 
-        db_size_mb = overview.database_size / (1024 * 1024)
-        self._card_db_size.set_value(f"{db_size_mb:.1f} MB")
-
-        if overview.last_build is not None:
-            if isinstance(overview.last_build, datetime):
-                self._card_last_build.set_value(
-                    overview.last_build.strftime("%Y-%m-%d %H:%M"),
-                )
-            else:
-                self._card_last_build.set_value(str(overview.last_build))
-        else:
-            self._card_last_build.set_value("Unknown")
-
-    # ── Collection selection ─────────────────────────────────────────────
-
-    def _on_collection_selected(
-        self,
-        selected: QtCore.QItemSelection,
-        _deselected: QtCore.QItemSelection,
-    ) -> None:
-        """Load system summaries for the selected collection."""
-        indexes = selected.indexes()
-        if not indexes:
+    def _on_collection_selected(self) -> None:
+        rows = self._collection_view.selectionModel().selectedRows()
+        if not rows or self._db is None:
             self._system_model.set_records([])
             return
-
-        source_index = self._collection_proxy.mapToSource(indexes[0])
-        row = source_index.row()
-        if 0 <= row < len(self._collection_model._records):
-            collection = self._collection_model._records[row].name
-            self._load_systems(collection)
-
-    def _load_systems(self, collection: str) -> None:
-        """Load system summaries for *collection* in the background."""
-        if self._db is None:
-            return
-
-        try:
-            systems = self._db.get_system_summaries(collection)
-        except Exception:
-            log.warning("Failed to load systems for %s", collection, exc_info=True)
-            self._system_model.set_records([])
-            return
-
+        summary = self._collection_model._records[rows[0].row()]
+        self._selected_collection = summary.name
+        expected = MinervaState().expected_counts()
+        systems = []
+        for item in self._db.get_system_summaries(summary.name):
+            expected_files = expected.get((item.collection, item.system))
+            systems.append(SystemSummary(item.collection, item.system, item.files, item.last_update, item.source_present, expected_files))
         self._system_model.set_records(systems)
+        self._detail_title.setText(summary.name)
+        refs = [reference for reference in MinervaState().list_reference_dats() if reference.collection == summary.name]
+        self._detail_meta.setText(
+            f"Systems\n{summary.systems:,}\n\nIndexed files\n{summary.files:,}\n\nStatus\n{summary.status.value.title()}\n\n"
+            f"Reference DATs\n{len(refs):,}\n\nSource folder\n{self._torrent_dir()}"
+        )
 
-    # ── Actions ──────────────────────────────────────────────────────────
+    def _filter_systems(self, text: str) -> None:
+        needle = text.casefold().strip()
+        for row in range(self._system_model.rowCount()):
+            item = self._system_model._records[row]
+            self._system_view.setRowHidden(row, bool(needle and needle not in item.system.casefold()))
 
     def _on_rebuild(self) -> None:
-        """Trigger a full index rebuild."""
-        self._diagnostics.setText("Rebuild not yet implemented from GUI.")
+        settings = QtCore.QSettings("MinervaFixDAT", "MinervaGUI")
+        torrent_dir = settings.value("torrent_dir", str(DEFAULT_TORRENT_DIR), str)
+        index_path = settings.value("index_path", str(DEFAULT_INDEX_PATH), str)
+        self._rebuild_btn.setEnabled(False)
+        self._rebuild_btn.setText("Rebuilding…")
+        task = TaskRunner(_atomic_rebuild_task, torrent_dir, index_path)
+        task.signals.result.connect(self._on_rebuild_complete)
+        task.signals.error.connect(self._on_rebuild_error)
+        self._pool.start(task)
+
+    def _on_rebuild_complete(self, path: object) -> None:
+        self._rebuild_btn.setEnabled(True)
+        self._rebuild_btn.setText("Rebuild index")
+        self._app_state.index_state = {"ready": True, "path": str(path)}
+        NotificationBanner.show_success(self, "Index rebuilt", str(path))
+        self.refresh()
+
+    def _on_rebuild_error(self, details: str) -> None:
+        self._rebuild_btn.setEnabled(True)
+        self._rebuild_btn.setText("Rebuild index")
+        NotificationBanner.show_error(self, "Index rebuild failed", details, duration=8000)
+
+    def _on_import_dat(self) -> None:
+        paths, _ = QtWidgets.QFileDialog.getOpenFileNames(self, "Import reference DAT", "", "DAT files (*.dat)")
+        state = MinervaState()
+        imported = 0
+        for raw_path in paths:
+            try:
+                path = Path(raw_path)
+                info = parse_dat_file(path)
+                state.save_reference_dat(ReferenceDat(
+                    id=uuid.uuid4().hex,
+                    path=str(path),
+                    name=info.name or path.stem,
+                    collection=info.collection,
+                    system=info.system,
+                    entry_count=len(info.entries),
+                    imported_at=datetime.now(timezone.utc).isoformat(),
+                ))
+                state.add_event("index", f"Imported reference DAT {path.name}")
+                imported += 1
+            except Exception as exc:
+                NotificationBanner.show_error(self, "DAT import failed", f"{raw_path}: {exc}")
+        if imported:
+            NotificationBanner.show_success(self, "Reference DATs imported", f"Imported {imported} file(s)")
+            self.refresh()
 
     def _on_verify_integrity(self) -> None:
-        """Run PRAGMA quick_check and display results."""
         if self._db is None:
-            self._diagnostics.setText("Database not available.")
             return
-
         try:
             result = self._db.run_integrity_check()
         except Exception as exc:
-            log.warning("Integrity check failed", exc_info=True)
-            self._diagnostics.setText(f"Integrity check error: {exc}")
+            self._health_title.setText("Check failed")
+            self._health_text.setText(str(exc))
             return
-
-        lines = [
-            f"Quick check: {result['quick_check']}",
-            "Table counts:",
-        ]
-        for table, count in result.get("table_counts", {}).items():
-            lines.append(f"  {table}: {count:,}")
-        self._diagnostics.setText("\n".join(lines))
+        if result["ok"]:
+            self._health_title.setText("Healthy")
+            self._health_title.setObjectName("successLabel")
+            self._health_text.setText("No schema or consistency issues detected.")
+        else:
+            self._health_title.setText("Problems found")
+            self._health_title.setObjectName("errorLabel")
+            self._health_text.setText(str(result["quick_check"]))
 
     def _on_check_sources(self) -> None:
-        """Check for missing torrent source files."""
         if self._db is None:
-            self._diagnostics.setText("Database not available.")
             return
-
         try:
-            missing = self._db.find_missing_torrent_sources()
+            self._missing_sources = self._db.find_missing_torrent_sources()
         except Exception as exc:
-            log.warning("Source check failed", exc_info=True)
-            self._diagnostics.setText(f"Source check error: {exc}")
+            NotificationBanner.show_error(self, "Path verification failed", str(exc))
             return
-
-        if not missing:
-            self._diagnostics.setText("All source torrents present.")
-        else:
-            self._diagnostics.setText(
-                f"Missing {len(missing)} source torrent(s):\n"
-                + "\n".join(m["torrent"] for m in missing[:20])
-                + ("\n\u2026 (truncated)" if len(missing) > 20 else ""),
-            )
+        count = len(self._missing_sources)
+        self._missing_title.setText(str(count))
+        self._missing_text.setText("All source torrents are present." if count == 0 else f"{count} source torrent(s) cannot be found on disk.")
+        NotificationBanner.show_success(self, "Path verification complete", f"{count} missing source(s)")
+
+    def _show_missing_paths(self) -> None:
+        if not self._missing_sources:
+            self._on_check_sources()
+        dialog = QtWidgets.QDialog(self)
+        dialog.setWindowTitle("Missing source torrents")
+        dialog.resize(760, 420)
+        layout = QtWidgets.QVBoxLayout(dialog)
+        table = QtWidgets.QTableWidget(0, 2)
+        table.setHorizontalHeaderLabels(["Torrent", "Expected path"])
+        table.horizontalHeader().setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.ResizeToContents)
+        table.horizontalHeader().setSectionResizeMode(1, QtWidgets.QHeaderView.ResizeMode.Stretch)
+        for entry in self._missing_sources:
+            row = table.rowCount()
+            table.insertRow(row)
+            table.setItem(row, 0, QtWidgets.QTableWidgetItem(entry["torrent"]))
+            table.setItem(row, 1, QtWidgets.QTableWidgetItem(entry["path"]))
+        layout.addWidget(table)
+        buttons = QtWidgets.QDialogButtonBox(QtWidgets.QDialogButtonBox.StandardButton.Close)
+        buttons.rejected.connect(dialog.reject)
+        layout.addWidget(buttons)
+        dialog.exec()
+
+    def _open_source_folder(self) -> None:
+        QtGui.QDesktopServices.openUrl(QtCore.QUrl.fromLocalFile(str(self._torrent_dir())))
+
+    def _remove_collection(self) -> None:
+        if not self._selected_collection or self._db is None:
+            return
+        if QtWidgets.QMessageBox.warning(
+            self,
+            "Remove collection",
+            f'Remove all indexed rows for "{self._selected_collection}"?\nThe source torrent files will not be deleted.',
+            QtWidgets.QMessageBox.StandardButton.Yes | QtWidgets.QMessageBox.StandardButton.Cancel,
+            QtWidgets.QMessageBox.StandardButton.Cancel,
+        ) != QtWidgets.QMessageBox.StandardButton.Yes:
+            return
+        removed = self._db.remove_collection(self._selected_collection)
+        NotificationBanner.show_success(self, "Collection removed", f"Removed {removed:,} indexed rows")
+        self._selected_collection = None
+        self.refresh()
+
+    @staticmethod
+    def _format_size(size: int) -> str:
+        value = float(size)
+        for unit in ("B", "KB", "MB", "GB", "TB"):
+            if value < 1024 or unit == "TB":
+                return f"{value:.1f} {unit}" if unit != "B" else f"{int(value)} B"
+            value /= 1024
+        return f"{value:.1f} TB"
+
+    @staticmethod
+    def _torrent_dir() -> Path:
+        return Path(QtCore.QSettings("MinervaFixDAT", "MinervaGUI").value("torrent_dir", str(DEFAULT_TORRENT_DIR), str))
diff --git a/minerva/app/pages/library.py b/minerva/app/pages/library.py
index 5774105..0b8375a 100644
--- a/minerva/app/pages/library.py
+++ b/minerva/app/pages/library.py
@@ -1,363 +1,522 @@
-"""
-Browse/search the indexed Myrient library.
-"""
+"""Faceted browser for the indexed torrent library."""
 
 from __future__ import annotations
 
 import enum
 import logging
+import time
+from pathlib import Path
 
-from PyQt6 import QtCore, QtWidgets
+from PyQt6 import QtCore, QtGui, QtWidgets
 from PyQt6.QtCore import QModelIndex, QPoint, Qt
+from superqt import QToggleSwitch
 
 from minerva.app.app_state import AppState
 from minerva.app.pages.base import BasePage, _state_label
 from minerva.app.task_runner import TaskRunner
-from minerva.domain.library import LibraryItem, LibraryQuery
-from minerva.ui.models.delegates import CheckboxDelegate, SizeDelegate
-from minerva.ui.models.proxy import SortFilterProxy
+from minerva.domain.library import LibraryFacets, LibraryItem, LibraryQuery
+from minerva.ui.icons import Icons
+from minerva.ui.models.delegates import CheckboxDelegate, DisplayDelegate, SizeDelegate
 from minerva.ui.models.record_model import ColumnSpec, RecordListModel
+from minerva.ui.widgets.cover_art import CoverLabel, CoverProvider
+from minerva.ui.widgets.facet_list import FacetList
+from minerva.ui.widgets.notification_banner import NotificationBanner
+from minerva.ui.widgets.page_header import PageHeader
 from minerva.ui.widgets.pagination_bar import PaginationBar
 from minerva.ui.widgets.search_toolbar import SearchToolbar
+from minerva.ui.widgets.section_card import SectionCard
 from minerva.ui.widgets.tag_flow import TagFlow
-
 from minerva_db import MinervaDB
+from minerva_state import MinervaState
 
 log = logging.getLogger(__name__)
 
 
 class PageState(enum.Enum):
-    """Visual states for the LibraryPage stack."""
-
-    PRE_INDEX = "pre_index"  # Index not loaded yet
-    EMPTY = "empty"          # No results — ready for search
-    LOADING = "loading"      # Search in progress
-    RESULTS = "results"      # Results displayed
-    ERROR = "error"          # Search error
-
+    PRE_INDEX = "pre_index"
+    EMPTY = "empty"
+    LOADING = "loading"
+    RESULTS = "results"
+    ERROR = "error"
 
-# ── Column specs for LibraryItem ────────────────────────────────────────────
 
 _LIBRARY_COLUMNS: list[ColumnSpec[LibraryItem]] = [
-    ColumnSpec(
-        header="\u2713",
-        accessor=lambda item: False,
-        is_checkbox=True,
-    ),
-    ColumnSpec(
-        header="Name",
-        accessor=lambda item: item.stem,
-    ),
-    ColumnSpec(
-        header="Size",
-        accessor=lambda item: item.size,
-        sort_role=Qt.ItemDataRole.UserRole,
-    ),
-    ColumnSpec(
-        header="Collection",
-        accessor=lambda item: item.collection,
-    ),
-    ColumnSpec(
-        header="System",
-        accessor=lambda item: item.system or "Unknown",
-    ),
+    ColumnSpec(header="Sel", accessor=lambda item: False, is_checkbox=True),
+    ColumnSpec(header="Game", accessor=lambda item: item.stem),
+    ColumnSpec(header="Collection", accessor=lambda item: item.collection),
+    ColumnSpec(header="System", accessor=lambda item: item.system or "Unknown"),
+    ColumnSpec(header="Size", accessor=lambda item: item.size),
+    ColumnSpec(header="Source", accessor=lambda item: "Torrent"),
+    ColumnSpec(header="Tags", accessor=lambda item: ", ".join((*item.regions, *item.tags))),
 ]
-
 LibraryTableModel = RecordListModel[LibraryItem]
 
+_SORT_FIELDS = {1: "stem", 2: "collection", 3: "system", 4: "size"}
+
 
 class LibraryPage(BasePage):
-    """Browse/search the indexed Myrient library.
-
-    Composes a ``QTableView`` with a ``LibraryTableModel``
-    (``RecordListModel[LibraryItem]``) filtered through a
-    ``SortFilterProxy``.  A ``SearchToolbar`` with debounced 350ms
-    input drives FTS5 trigram searches via ``MinervaDB.search_library()``
-    with a generation counter to discard stale background results.
-    A ``TagFlow`` provides tag-based filtering and a ``PaginationBar``
-    enables page-based navigation.
-
-    States (``QStackedWidget``):
-        PRE_INDEX — index not loaded yet (awaiting ``AppState.index_state_changed``)
-        EMPTY     — ready for search
-        LOADING   — search in progress
-        RESULTS   — results displayed
-        ERROR     — search failed
-    """
+    """SQL-paginated, faceted library browser with a persistent selection."""
 
     def __init__(self, app_state: AppState) -> None:
         super().__init__(app_state)
-
-        # ── Generation counter for stale-result rejection ────────────────
-        self._gen_counter = 0
         self._pool = QtCore.QThreadPool.globalInstance()
-        self._current_page = 0
-
-        # ── Database ─────────────────────────────────────────────────────
+        self._generation = 0
+        self._page = 0
+        self._sort_field = "stem"
+        self._descending = False
+        self._current_item: LibraryItem | None = None
         self._db: MinervaDB | None = None
         try:
             self._db = MinervaDB()
         except Exception:
-            log.warning("LibraryPage: MinervaDB not available", exc_info=True)
+            log.warning("Library index unavailable", exc_info=True)
 
-        # ── Model layer ──────────────────────────────────────────────────
-        self._model = LibraryTableModel([], _LIBRARY_COLUMNS, parent=self)
-        self._proxy = SortFilterProxy(self)
-        self._proxy.setSourceModel(self._model)
+        settings = QtCore.QSettings("MinervaFixDAT", "MinervaGUI")
+        self._cover_provider = CoverProvider(settings.value("cover_dir", "covers", str))
 
-        # ── View layer ───────────────────────────────────────────────────
+        self._model = LibraryTableModel([], _LIBRARY_COLUMNS, self)
         self._view = QtWidgets.QTableView(self)
-        self._view.setModel(self._proxy)
-        self._view.setSelectionBehavior(
-            QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows,
-        )
+        self._view.setModel(self._model)
+        self._view.setSelectionBehavior(QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows)
+        self._view.setSelectionMode(QtWidgets.QAbstractItemView.SelectionMode.SingleSelection)
+        self._view.setAlternatingRowColors(True)
         self._view.verticalHeader().hide()
-        self._view.setSortingEnabled(True)
         self._view.setContextMenuPolicy(Qt.ContextMenuPolicy.CustomContextMenu)
         self._view.customContextMenuRequested.connect(self._on_context_menu)
-
-        # Per-column delegates
+        self._view.selectionModel().selectionChanged.connect(self._on_row_selected)
+        self._view.setItemDelegate(DisplayDelegate(self._view))
         self._view.setItemDelegateForColumn(0, CheckboxDelegate(self._view))
-        self._view.setItemDelegateForColumn(2, SizeDelegate(self._view))
-
-        # Column widths
+        self._view.setItemDelegateForColumn(4, SizeDelegate(self._view))
+        self._view.horizontalHeader().sortIndicatorChanged.connect(self._on_sort_changed)
+        self._view.setSortingEnabled(True)
         header = self._view.horizontalHeader()
-        header.setStretchLastSection(False)
         header.setSectionResizeMode(1, QtWidgets.QHeaderView.ResizeMode.Stretch)
-        header.resizeSection(0, 40)   # ✓
-        header.resizeSection(2, 120)  # Size
-        header.resizeSection(3, 80)   # Collection
-        # Column 4 (System) = auto
-
-        # ── Cross-page selection sync ────────────────────────────────────
-        self._model.dataChanged.connect(self._on_model_checkstate_changed)
+        for column, width in {0: 46, 2: 105, 3: 170, 4: 95, 5: 80, 6: 180}.items():
+            header.resizeSection(column, width)
+        self._model.dataChanged.connect(self._on_checks_changed)
 
-        # ── Search toolbar ───────────────────────────────────────────────
-        self._search_toolbar = SearchToolbar(
+        self._header = PageHeader(
+            "Library",
+            "Search the indexed collection, filter by platform, and stage items for download.",
+        )
+        self._export_btn = self._header.add_action("Export DAT", Icons.file())
+        self._queue_btn = self._header.add_action("Add to queue", Icons.add(), primary=True)
+        self._open_btn = self._header.add_action("Open file", Icons.folder_open())
+        self._export_btn.clicked.connect(self._export_selected_dat)
+        self._queue_btn.clicked.connect(self._queue_selected)
+        self._open_btn.clicked.connect(self._open_selected_file)
+
+        self._search = SearchToolbar(
             show_collection_filter=True,
             show_system_filter=True,
-            placeholder="Search DAT index\u2026",
+            placeholder="Search games…",
             parent=self,
         )
+        self._search._search_input.setPlaceholderText("Search games…")
         self._populate_combos()
-
-        # Debounced search trigger (350ms after last keystroke)
         self._search_timer = QtCore.QTimer(self)
         self._search_timer.setSingleShot(True)
         self._search_timer.setInterval(350)
-        self._search_timer.timeout.connect(self._do_search)
-        self._search_toolbar._search_input.textChanged.connect(
-            lambda: self._search_timer.start()
-        )
-        self._search_toolbar.filter_changed.connect(
-            lambda _name, _value: self._on_filter_changed()
-        )
-        # Immediate search on Enter
-        self._search_toolbar.search_triggered.connect(
-            lambda _text: self._on_search_immediate()
-        )
-
-        # ── Tag flow ─────────────────────────────────────────────────────
-        self._tag_flow = TagFlow(parent=self)
-        self._tag_flow.tag_toggled.connect(
-            lambda _tag, _checked: self._do_search()
-        )
-
-        # ── Pagination bar ───────────────────────────────────────────────
-        self._pagination = PaginationBar(page_size=50, parent=self)
+        self._search_timer.timeout.connect(self._search_now)
+        self._search._search_input.textChanged.connect(lambda: self._search_timer.start())
+        self._search.search_triggered.connect(lambda _text: self._search_now(reset_page=True))
+        self._search.filter_changed.connect(lambda _name, _value: self._filters_changed())
+
+        self._selected_only = QToggleSwitch("Selected only")
+        self._selected_only.toggled.connect(lambda _checked: self._search_now(reset_page=True))
+        self._clear_filters_btn = QtWidgets.QPushButton(Icons.close(), "Clear filters")
+        self._clear_filters_btn.setObjectName("subtleButton")
+        self._clear_filters_btn.clicked.connect(self._clear_facets)
+
+        search_row = QtWidgets.QHBoxLayout()
+        search_row.setContentsMargins(0, 0, 0, 0)
+        search_row.setSpacing(8)
+        search_row.addWidget(self._search, 1)
+        search_row.addWidget(self._selected_only)
+        search_row.addWidget(self._clear_filters_btn)
+
+        self._active_filters = TagFlow(self)
+        self._active_filters.tag_toggled.connect(self._on_active_filter_toggled)
+
+        self._summary = QtWidgets.QLabel("Ready")
+        self._summary.setObjectName("summaryStrip")
+        self._selected_summary = QtWidgets.QLabel("0 selected")
+        self._selected_summary.setObjectName("mutedLabel")
+        summary_row = QtWidgets.QHBoxLayout()
+        summary_row.addWidget(self._summary)
+        summary_row.addStretch(1)
+        summary_row.addWidget(self._selected_summary)
+
+        self._region_facets = FacetList("Region")
+        self._category_facets = FacetList("Category")
+        self._tag_facets = FacetList("Tags")
+        for facets in (self._region_facets, self._category_facets, self._tag_facets):
+            facets.selection_changed.connect(self._facets_changed)
+
+        facet_content = QtWidgets.QWidget()
+        facet_layout = QtWidgets.QVBoxLayout(facet_content)
+        facet_layout.setContentsMargins(0, 0, 0, 0)
+        facet_layout.setSpacing(12)
+        facet_layout.addWidget(self._region_facets)
+        facet_layout.addWidget(self._category_facets)
+        facet_layout.addWidget(self._tag_facets)
+        facet_layout.addStretch(1)
+        facet_scroll = QtWidgets.QScrollArea()
+        facet_scroll.setWidgetResizable(True)
+        facet_scroll.setFrameShape(QtWidgets.QFrame.Shape.NoFrame)
+        facet_scroll.setWidget(facet_content)
+        facet_card = SectionCard("Filters", Icons.filter())
+        facet_card.content_layout().addWidget(facet_scroll)
+        facet_card.setMinimumWidth(210)
+        facet_card.setMaximumWidth(270)
+
+        self._pagination = PaginationBar(page_size=50)
         self._pagination.page_changed.connect(self._on_page_changed)
-        self._pagination.page_size_changed.connect(self._on_page_size_changed)
-
-        # ── Results widget layout ────────────────────────────────────────
-        results_widget = QtWidgets.QWidget()
-        results_layout = QtWidgets.QVBoxLayout(results_widget)
+        self._pagination.page_size_changed.connect(lambda _size: self._search_now(reset_page=True))
+
+        table_panel = QtWidgets.QWidget()
+        table_layout = QtWidgets.QVBoxLayout(table_panel)
+        table_layout.setContentsMargins(0, 0, 0, 0)
+        table_layout.setSpacing(6)
+        table_layout.addWidget(self._view, 1)
+        table_layout.addWidget(self._pagination)
+
+        self._cover = CoverLabel(170, 225)
+        self._detail_title = QtWidgets.QLabel("Select a game")
+        self._detail_title.setObjectName("sectionTitle")
+        self._detail_title.setWordWrap(True)
+        self._detail_meta = QtWidgets.QLabel("")
+        self._detail_meta.setObjectName("mutedLabel")
+        self._detail_meta.setWordWrap(True)
+        self._detail_tags = TagFlow()
+        self._detail_queue = QtWidgets.QPushButton(Icons.add(), "Queue")
+        self._detail_queue.setObjectName("primaryButton")
+        self._detail_queue.clicked.connect(self._queue_current)
+        self._detail_dat = QtWidgets.QPushButton(Icons.file(), "Generate DAT")
+        self._detail_dat.clicked.connect(self._export_current_dat)
+        self._detail_copy = QtWidgets.QPushButton(Icons.copy(), "Copy path")
+        self._detail_copy.clicked.connect(self._copy_current_path)
+        details_card = SectionCard("Game details", Icons.file())
+        details = details_card.content_layout()
+        details.addWidget(self._cover, 0, Qt.AlignmentFlag.AlignHCenter)
+        details.addWidget(self._detail_title)
+        details.addWidget(self._detail_meta)
+        details.addWidget(self._detail_tags)
+        details.addWidget(self._detail_queue)
+        details.addWidget(self._detail_dat)
+        details.addWidget(self._detail_copy)
+        details.addStretch(1)
+        details_card.setMinimumWidth(230)
+        details_card.setMaximumWidth(310)
+
+        splitter = QtWidgets.QSplitter(Qt.Orientation.Horizontal)
+        splitter.addWidget(facet_card)
+        splitter.addWidget(table_panel)
+        splitter.addWidget(details_card)
+        splitter.setStretchFactor(0, 0)
+        splitter.setStretchFactor(1, 1)
+        splitter.setStretchFactor(2, 0)
+        splitter.setSizes([230, 780, 270])
+        self._splitter = splitter
+
+        results = QtWidgets.QWidget()
+        results_layout = QtWidgets.QVBoxLayout(results)
         results_layout.setContentsMargins(0, 0, 0, 0)
-        results_layout.setSpacing(4)
-        results_layout.addWidget(self._search_toolbar)
-        results_layout.addWidget(self._tag_flow)
-        results_layout.addWidget(self._view)
-        results_layout.addWidget(self._pagination)
-
-        # ── State stack ──────────────────────────────────────────────────
-        self._stack = QtWidgets.QStackedWidget(self)
-
-        self._label_pre_index = _state_label("Index not loaded yet")
-        self._stack.addWidget(self._label_pre_index)  # index 0
-
-        self._label_empty = _state_label("No results \u2014 try a search")
-        self._stack.addWidget(self._label_empty)  # index 1
-
-        self._label_loading = _state_label("Searching\u2026")
-        self._stack.addWidget(self._label_loading)  # index 2
-
-        self._label_error = _state_label("Search error")
-        self._stack.addWidget(self._label_error)  # index 3
-
-        self._stack.addWidget(results_widget)  # index 4
-
-        # ── Layout ───────────────────────────────────────────────────────
-        layout = QtWidgets.QVBoxLayout(self)
-        layout.setContentsMargins(0, 0, 0, 0)
-        layout.addWidget(self._stack)
-
-        # ── Signal wiring ────────────────────────────────────────────────
-        self._app_state.index_state_changed.connect(
-            self._on_index_state_changed,
-        )
-
-        # Initial state
-        self._set_state(PageState.EMPTY)
-
-    # ── State management ─────────────────────────────────────────────────
+        results_layout.setSpacing(10)
+        results_layout.addWidget(self._header)
+        results_layout.addLayout(search_row)
+        results_layout.addWidget(self._active_filters)
+        results_layout.addLayout(summary_row)
+        results_layout.addWidget(splitter, 1)
+
+        self._stack = QtWidgets.QStackedWidget()
+        self._stack.addWidget(_state_label("Index not loaded yet"))
+        self._stack.addWidget(_state_label("No games match the current filters"))
+        self._stack.addWidget(_state_label("Searching…"))
+        self._error_label = _state_label("Search error")
+        self._stack.addWidget(self._error_label)
+        self._stack.addWidget(results)
+        root = QtWidgets.QVBoxLayout(self)
+        root.setContentsMargins(0, 0, 0, 0)
+        root.addWidget(self._stack)
+
+        self._app_state.index_state_changed.connect(lambda _value: self.refresh())
+        self._app_state.selected_count_changed.connect(self._on_selected_count_changed)
+        self._set_state(PageState.RESULTS)
+        QtCore.QTimer.singleShot(0, lambda: self._search_now(reset_page=True))
 
     def _set_state(self, state: PageState) -> None:
-        """Switch the visible stack widget for *state*."""
-        index_map = {
+        self._stack.setCurrentIndex({
             PageState.PRE_INDEX: 0,
             PageState.EMPTY: 1,
             PageState.LOADING: 2,
             PageState.ERROR: 3,
             PageState.RESULTS: 4,
-        }
-        self._stack.setCurrentIndex(index_map[state])
+        }[state])
 
-    def _on_index_state_changed(self, value: object) -> None:
-        """Transition from PRE_INDEX to EMPTY when the index is ready."""
-        if self._stack.currentIndex() == 0:
-            self._set_state(PageState.EMPTY)
+    def activate(self) -> None:
+        self._on_selected_count_changed(len(self._app_state.selected_game_ids))
 
-    # ── Combo population ─────────────────────────────────────────────────
-
-    def _populate_combos(self) -> None:
-        """Populate collection and system combo boxes from the database."""
-        if self._db is None:
-            return
+    def refresh(self) -> None:
         try:
-            collections = self._db.get_collections()
-            self._search_toolbar.set_collections(collections)
-            systems = self._db.get_systems()
-            self._search_toolbar.set_systems(systems)
-        except Exception:
-            log.warning("Failed to populate combos", exc_info=True)
+            self._db = MinervaDB()
+            self._populate_combos()
+            self._search_now(reset_page=False)
+        except Exception as exc:
+            self._error_label.setText(str(exc))
+            self._set_state(PageState.ERROR)
 
-    def _on_filter_changed(self) -> None:
-        """Filter changed — update systems combo and trigger search."""
+    def _populate_combos(self) -> None:
         if self._db is None:
             return
-        collection = self._search_toolbar.collection()
         try:
-            systems = self._db.get_systems(collection or None)
+            self._search.set_collections(self._db.get_collections())
+            self._search.set_systems(self._db.get_systems(self._search.collection() or None))
         except Exception:
-            systems = []
-        self._search_toolbar.set_systems(systems)
-        self._current_page = 0
-        self._search_timer.start()
-
-    def _on_search_immediate(self) -> None:
-        """Immediate search (user pressed Enter)."""
-        self._search_timer.stop()
-        self._current_page = 0
-        self._do_search()
-
-    # ── Pagination handlers ─────────────────────────────────────────────
-
-    def _on_page_changed(self, page: int) -> None:
-        """Handle page navigation."""
-        self._current_page = page
-        self._do_search()
-
-    def _on_page_size_changed(self, _size: int) -> None:
-        """Handle page size change — resets to page 0."""
-        self._current_page = 0
-        self._do_search()
-
-    # ── Search ───────────────────────────────────────────────────────────
+            log.warning("Failed to populate library filters", exc_info=True)
+
+    def _filters_changed(self) -> None:
+        if self._db is not None:
+            try:
+                self._search.set_systems(self._db.get_systems(self._search.collection() or None))
+            except Exception:
+                pass
+        self._search_now(reset_page=True)
+
+    def _facets_changed(self) -> None:
+        self._update_active_filters()
+        self._search_now(reset_page=True)
+
+    def _clear_facets(self) -> None:
+        self._region_facets.clear_selection()
+        self._category_facets.clear_selection()
+        self._tag_facets.clear_selection()
+        self._active_filters.set_tags([])
+        self._search_now(reset_page=True)
+
+    def _selected_file_ids(self) -> tuple[int, ...]:
+        values: list[int] = []
+        for key in self._app_state.selected_game_ids:
+            if key.startswith("library:"):
+                try:
+                    values.append(int(key.split(":", 1)[1]))
+                except ValueError:
+                    continue
+        return tuple(sorted(set(values)))
+
+    def _build_query(self) -> LibraryQuery:
+        return LibraryQuery(
+            text=self._search.search_text().strip(),
+            collection=self._search.collection(),
+            system=self._search.system(),
+            tags=frozenset(self._tag_facets.selected_values()),
+            regions=frozenset(self._region_facets.selected_values()),
+            categories=frozenset(self._category_facets.selected_values()),
+            selected_only=self._selected_only.isChecked(),
+            file_ids=self._selected_file_ids() if self._selected_only.isChecked() else (),
+            offset=self._page * self._pagination.page_size(),
+            limit=self._pagination.page_size(),
+            sort_field=self._sort_field,
+            descending=self._descending,
+        )
 
-    def _do_search(self) -> None:
-        """Run a background DB search with a generation counter."""
+    def _search_now(self, reset_page: bool = False) -> None:
         if self._db is None:
             self._set_state(PageState.ERROR)
             return
-
-        self._gen_counter += 1
-        generation = self._gen_counter
-
-        query = LibraryQuery(
-            text=self._search_toolbar.search_text().strip(),
-            collection=self._search_toolbar.collection(),
-            system=self._search_toolbar.system(),
-            tags=frozenset(self._tag_flow.selected_tags()),
-            offset=self._current_page * self._pagination.page_size(),
-            limit=self._pagination.page_size(),
-        )
-
+        if reset_page:
+            self._page = 0
+        self._generation += 1
+        generation = self._generation
         self._set_state(PageState.LOADING)
-
-        task = TaskRunner(self._search_task, generation, query)
+        task = TaskRunner(self._search_task, generation, self._build_query())
         task.signals.result.connect(self._on_search_result)
-        task.signals.error.connect(
-            lambda details, token=generation: self._on_search_error(
-                token, details,
-            ),
-        )
+        task.signals.error.connect(lambda details, gen=generation: self._on_search_error(gen, details))
         self._pool.start(task)
 
     @staticmethod
-    def _search_task(
-        generation: int,
-        query: LibraryQuery,
-    ) -> tuple[int, list[LibraryItem], int]:
-        """Run the DB query (executed on a background thread)."""
+    def _search_task(generation: int, query: LibraryQuery) -> tuple[int, list[LibraryItem], int, LibraryFacets, float]:
+        started = time.perf_counter()
         db = MinervaDB()
         items, total = db.search_library(query)
-        return generation, items, total
+        facets = db.get_library_facets(query)
+        return generation, items, total, facets, (time.perf_counter() - started) * 1000
 
     def _on_search_result(self, payload: object) -> None:
-        """Handle search results — discard stale generations."""
-        generation, items, total = payload  # type: ignore[misc]
-        if generation != self._gen_counter:
+        generation, items, total, facets, elapsed_ms = payload  # type: ignore[misc]
+        if generation != self._generation:
             return
         self._model.set_records(items)
-        self._pagination.configure(total, self._current_page)
-        if items:
-            self._set_state(PageState.RESULTS)
-        else:
-            self._set_state(PageState.EMPTY)
-
-    def _on_search_error(
-        self,
-        generation: int,
-        details: str,
-    ) -> None:
-        """Handle search error — discard stale generations."""
-        if generation != self._gen_counter:
+        selected = self._app_state.selected_game_ids
+        for row, item in enumerate(items):
+            if f"library:{item.id}" in selected:
+                self._model.setData(self._model.index(row, 0), Qt.CheckState.Checked, Qt.ItemDataRole.CheckStateRole)
+        self._pagination.configure(total, self._page)
+        self._region_facets.set_facets(facets.regions, self._region_facets.selected_values())
+        self._category_facets.set_facets(facets.categories, self._category_facets.selected_values())
+        self._tag_facets.set_facets(facets.tags[:30], self._tag_facets.selected_values())
+        self._summary.setText(f"{facets.total:,} indexed results  ·  Query {elapsed_ms:.0f} ms")
+        self._update_active_filters()
+        self._set_state(PageState.RESULTS if items else PageState.EMPTY)
+
+    def _on_search_error(self, generation: int, details: str) -> None:
+        if generation != self._generation:
             return
-        log.warning("Search error: %s", details)
+        self._error_label.setText(details)
         self._set_state(PageState.ERROR)
 
-    # ── Selection sync → AppState ────────────────────────────────────────
-
-    def _on_model_checkstate_changed(
-        self, _top_left: QModelIndex, _bot_right: QModelIndex,
-    ) -> None:
-        """Scan the model for checked rows and sync to AppState."""
-        selected: list[str] = []
-        check_states = self._model._check_states
-        for row in range(len(self._model._records)):
-            if check_states.get(row, Qt.CheckState.Unchecked) == Qt.CheckState.Checked:
-                item = self._model._records[row]
-                selected.append(f"library:{item.stem}")
+    def _on_page_changed(self, page: int) -> None:
+        self._page = page
+        self._search_now()
+
+    def _on_sort_changed(self, column: int, order: Qt.SortOrder) -> None:
+        if column not in _SORT_FIELDS:
+            return
+        self._sort_field = _SORT_FIELDS[column]
+        self._descending = order == Qt.SortOrder.DescendingOrder
+        self._search_now(reset_page=True)
+
+    def _on_checks_changed(self, _top: QModelIndex, _bottom: QModelIndex) -> None:
+        selected = set(self._app_state.selected_game_ids)
+        for row, item in enumerate(self._model._records):
+            key = f"library:{item.id}"
+            selected.discard(key)
+            if self._model._check_states.get(row) == Qt.CheckState.Checked:
+                selected.add(key)
         self._app_state.set_selected(selected)
 
-    # ── Context menu ────────────────────────────────────────────────────
+    def _on_selected_count_changed(self, count: int) -> None:
+        self._selected_summary.setText(f"{count:,} selected")
+        self._queue_btn.setEnabled(count > 0)
+        self._export_btn.setEnabled(count > 0)
+
+    def _on_row_selected(self) -> None:
+        rows = self._view.selectionModel().selectedRows()
+        if not rows:
+            self._set_current_item(None)
+            return
+        row = rows[0].row()
+        self._set_current_item(self._model._records[row] if 0 <= row < len(self._model._records) else None)
+
+    def _set_current_item(self, item: LibraryItem | None) -> None:
+        self._current_item = item
+        if item is None:
+            self._detail_title.setText("Select a game")
+            self._detail_meta.clear()
+            self._detail_tags.set_tags([])
+            self._cover.set_placeholder()
+            return
+        self._detail_title.setText(item.stem)
+        self._detail_meta.setText(
+            f"Path\n{item.path_in_torrent}\n\nSize\n{self._format_size(item.size)}\n\n"
+            f"Collection\n{item.collection}\n\nSystem\n{item.system}\n\nSource torrent\n{item.source_torrent}"
+        )
+        self._detail_tags.set_tags(list((*item.regions, *item.tags)))
+        self._cover.set_cover(self._cover_provider.resolve(item))
+
+    def _update_active_filters(self) -> None:
+        tags = [f"Region: {v}" for v in sorted(self._region_facets.selected_values())]
+        tags += [f"Category: {v}" for v in sorted(self._category_facets.selected_values())]
+        tags += [f"Tag: {v}" for v in sorted(self._tag_facets.selected_values())]
+        self._active_filters.set_tags(tags, set(tags))
+        self._active_filters.setVisible(bool(tags))
+
+    def _on_active_filter_toggled(self, label: str, checked: bool) -> None:
+        if checked:
+            return
+        prefix, _, value = label.partition(": ")
+        target = {"Region": self._region_facets, "Category": self._category_facets, "Tag": self._tag_facets}.get(prefix)
+        if target is None:
+            return
+        target._list.blockSignals(True)
+        for row in range(target._list.count()):
+            item = target._list.item(row)
+            if item.data(Qt.ItemDataRole.UserRole) == value:
+                item.setCheckState(Qt.CheckState.Unchecked)
+        target._list.blockSignals(False)
+        self._facets_changed()
+
+    def _current_or_selected_items(self) -> list[LibraryItem]:
+        ids = self._selected_file_ids()
+        if ids and self._db is not None:
+            return self._db.get_files_by_ids(ids)
+        return [self._current_item] if self._current_item is not None else []
+
+    def _queue_selected(self) -> None:
+        self._queue_items(self._current_or_selected_items())
+
+    def _queue_current(self) -> None:
+        self._queue_items([self._current_item] if self._current_item else [])
+
+    def _queue_items(self, items: list[LibraryItem]) -> None:
+        shell = self.window()
+        controller = getattr(shell, "download_controller", None)
+        if controller is None:
+            NotificationBanner.show_error(self, "Downloads unavailable", "qBittorrent controller is not initialised")
+            return
+        settings = QtCore.QSettings("MinervaFixDAT", "MinervaGUI")
+        output_root = Path(settings.value("output_dir", "downloads", str))
+        queue_items = [
+            (item.id, str(output_root / item.collection / item.system / item.basename), None)
+            for item in items
+        ]
+        if not queue_items:
+            return
+        controller.add_many_to_queue(queue_items)
+        NotificationBanner.show_success(self, "Added to queue", f"Queued {len(queue_items)} item(s)")
+
+    def _export_selected_dat(self) -> None:
+        self._export_items(self._current_or_selected_items())
+
+    def _export_current_dat(self) -> None:
+        self._export_items([self._current_item] if self._current_item else [])
+
+    def _export_items(self, items: list[LibraryItem]) -> None:
+        if not items or self._db is None:
+            return
+        path, _ = QtWidgets.QFileDialog.getSaveFileName(self, "Export DAT", "minerva-selection.dat", "DAT files (*.dat)")
+        if not path:
+            return
+        rows = [
+            {"stem": item.stem, "basename": item.basename, "size": item.size}
+            for item in items
+        ]
+        Path(path).write_text(self._db.build_synthetic_dat(rows, "Minerva selection"), encoding="utf-8")
+        NotificationBanner.show_success(self, "DAT exported", path)
+
+    def _copy_current_path(self) -> None:
+        if self._current_item:
+            QtWidgets.QApplication.clipboard().setText(self._current_item.path_in_torrent)
+
+    def _open_selected_file(self) -> None:
+        item = self._current_item
+        if item is None:
+            return
+        records = MinervaState().find_queue_for_file(item.id)
+        target = next((Path(record.destination) for record in records if Path(record.destination).exists()), None)
+        if target is None:
+            NotificationBanner.show_info(self, "Not downloaded", "Queue this item before opening it")
+            return
+        QtGui.QDesktopServices.openUrl(QtCore.QUrl.fromLocalFile(str(target)))
 
     def _on_context_menu(self, pos: QPoint) -> None:
-        """Show a stub context menu."""
         index = self._view.indexAt(pos)
         menu = QtWidgets.QMenu(self)
         if index.isValid():
-            menu.addAction("Download\u2026")
-            menu.addAction("Copy name")
+            self._view.selectRow(index.row())
+            menu.addAction(Icons.add(), "Add to queue", self._queue_current)
+            menu.addAction(Icons.file(), "Generate DAT", self._export_current_dat)
+            menu.addAction(Icons.copy(), "Copy source path", self._copy_current_path)
+            menu.addAction(Icons.folder_open(), "Open downloaded file", self._open_selected_file)
         else:
-            menu.addAction("Refresh")
+            menu.addAction(Icons.refresh(), "Refresh", self.refresh)
         menu.exec(self._view.viewport().mapToGlobal(pos))
+
+    @staticmethod
+    def _format_size(size: int) -> str:
+        value = float(size)
+        for unit in ("B", "KB", "MB", "GB", "TB"):
+            if value < 1024 or unit == "TB":
+                return f"{value:.1f} {unit}" if unit != "B" else f"{int(value)} B"
+            value /= 1024
+        return f"{value:.1f} TB"
diff --git a/minerva/app/pages/match_review.py b/minerva/app/pages/match_review.py
index 00be4c3..6f469c7 100644
--- a/minerva/app/pages/match_review.py
+++ b/minerva/app/pages/match_review.py
@@ -1,961 +1,681 @@
-"""
-Match Review — per-entry review with candidate comparison and decisions.
-
-States (QStackedWidget):
-    EMPTY   — no entries for the current report
-    LOADING — loading entries or matching in progress
-    RESULTS — entry list and candidate panel displayed
-    ERROR   — database or match error
-"""
+"""Mockup-grade match review workspace with persistent decisions."""
 
 from __future__ import annotations
 
-import enum
 import logging
-import uuid
+from dataclasses import dataclass
 from pathlib import Path
-from typing import TYPE_CHECKING, Any
+from typing import Any
 
 from PyQt6 import QtCore, QtGui, QtWidgets
 from PyQt6.QtCore import Qt
 
 from minerva.app.app_state import AppState
-from minerva.app.pages.base import BasePage, _state_label
+from minerva.app.pages.base import BasePage
 from minerva.app.task_runner import TaskRunner
+from minerva.domain.library import LibraryItem
+from minerva.domain.reports import ReportSummary, ReviewEntry
 from minerva.ui.icons import Icons
 from minerva.ui.models.delegates import DisplayDelegate, SizeDelegate
 from minerva.ui.models.record_model import ColumnSpec, RecordListModel
-from minerva.ui.widgets.empty_state import EmptyState
+from minerva.ui.widgets.metric_card import MetricCard, MetricKind
+from minerva.ui.widgets.notification_banner import NotificationBanner
 from minerva.ui.widgets.page_header import PageHeader
-
-if TYPE_CHECKING:
-    from minerva.domain.reports import ReviewEntry
+from minerva.ui.widgets.section_card import SectionCard
+from minerva.ui.widgets.tag_flow import TagFlow
+from minerva_db import DatEntry, MinervaDB, stem_from_romname
+from minerva_state import MinervaState
 
 log = logging.getLogger(__name__)
 
 
-# ── State enum ─────────────────────────────────────────────────────────────────
-
+@dataclass(frozen=True)
+class ReviewRow:
+    entry: ReviewEntry
+    best: LibraryItem | None = None
 
-class MatchReviewPageState(enum.Enum):
-    EMPTY = "empty"
-    LOADING = "loading"
-    RESULTS = "results"
-    ERROR = "error"
+    @property
+    def category(self) -> str:
+        method = self.entry.automatic_method
+        if method == "exact":
+            return "exact"
+        if method in {"fts5", "trigram", "keyword", "fuzzy"}:
+            return "fuzzy"
+        return "unmatched"
 
+    @property
+    def best_title(self) -> str:
+        return self.best.stem if self.best is not None else "No match found"
 
-# ── Match category filter proxy ────────────────────────────────────────────────
+    @property
+    def confidence(self) -> float | None:
+        return self.entry.automatic_confidence
 
 
-class MatchCategoryFilterProxy(QtCore.QSortFilterProxyModel):
-    """Filters entries by match category: exact, fuzzy, unmatched, or all.
-
-    The match category is derived from the ``automatic_method`` field
-    (column index 2, ``UserRole`` returns the raw method string).
-    """
+_REVIEW_COLUMNS: list[ColumnSpec[ReviewRow]] = [
+    ColumnSpec("Requested title", lambda row: row.entry.filename),
+    ColumnSpec("Best match", lambda row: row.best_title),
+    ColumnSpec(
+        "Confidence",
+        lambda row: row.confidence,
+        format_fn=lambda value: f"{value:.0%}" if value is not None else "—",
+    ),
+    ColumnSpec("Method", lambda row: row.entry.automatic_method or "Unmatched"),
+    ColumnSpec("Size", lambda row: row.entry.size),
+    ColumnSpec("Decision", lambda row: row.entry.decision.title()),
+]
 
-    MATCH_ALL = ""
-    MATCH_EXACT = "exact"
-    MATCH_FUZZY = "fuzzy"
-    MATCH_UNMATCHED = "unmatched"
 
+class _ReviewFilter(QtCore.QSortFilterProxyModel):
     def __init__(self, parent: QtCore.QObject | None = None) -> None:
         super().__init__(parent)
-        self._match_filter: str = self.MATCH_ALL
-
-    def setMatchFilter(self, filter_text: str) -> None:
-        """Set the active match filter category.
-
-        Pass ``MATCH_ALL`` (empty string) to show all entries.
-        """
-        self._match_filter = filter_text
-        self.invalidateFilter()
-
-    def matchFilter(self) -> str:
-        """Return the active match filter category."""
-        return self._match_filter
-
-    def filterAcceptsRow(
-        self,
-        source_row: int,
-        source_parent: QtCore.QModelIndex,
-    ) -> bool:
-        if not self._match_filter:
-            return True
+        self.category = "all"
+        self.text = ""
 
+    def filterAcceptsRow(self, source_row, source_parent):
         model = self.sourceModel()
         if model is None:
             return True
-
-        method_idx = model.index(source_row, 2, source_parent)
-        method = model.data(method_idx, Qt.ItemDataRole.UserRole)
-
-        if self._match_filter == self.MATCH_EXACT:
-            return method == "exact"
-        if self._match_filter == self.MATCH_FUZZY:
-            return method in ("fts5", "trigram", "keyword")
-        if self._match_filter == self.MATCH_UNMATCHED:
-            return method is None
-
+        row: ReviewRow = model._records[source_row]  # noqa: SLF001
+        if self.category != "all" and row.category != self.category:
+            return False
+        if self.text:
+            needle = self.text.casefold()
+            return needle in row.entry.filename.casefold() or needle in row.best_title.casefold()
         return True
 
 
-# ── Column specs ───────────────────────────────────────────────────────────────
-
-_ENTRY_COLUMNS: list[ColumnSpec["ReviewEntry"]] = [
-    ColumnSpec(header="Filename", accessor=lambda e: e.filename),
-    ColumnSpec(header="Size", accessor=lambda e: e.size),
-    ColumnSpec(
-        header="Method",
-        accessor=lambda e: e.automatic_method or "—",
-    ),
-    ColumnSpec(
-        header="Confidence",
-        accessor=lambda e: e.automatic_confidence if e.automatic_confidence is not None else -1,
-        format_fn=lambda v: f"{v:.0%}" if isinstance(v, float) and v >= 0 else "—",
-    ),
-    ColumnSpec(header="Decision", accessor=lambda e: e.decision),
-]
-
-EntryListModel = RecordListModel["ReviewEntry"]
-
-
-# ── Background tasks ───────────────────────────────────────────────────────────
-
-
-def _load_candidates_task(
-    generation: int,
-    entry_filename: str,
-    entry_size: int,
-    collection: str,
-    system: str,
-) -> tuple[int, dict]:
-    """Run ``match_dat_detailed`` for a single entry on a background thread."""
-    from minerva_db import MinervaDB, DatEntry
-
-    db = MinervaDB()
-    entry = DatEntry(filename=entry_filename, size=entry_size)
-    result = db.match_dat_detailed([entry], collection=collection, system=system)
-    return generation, result
-
-
-def _load_entries_task(
-    report_id: str,
-) -> tuple[str, list[dict]]:
-    """Load report entries from ``MinervaState`` on a background thread.
-
-    Returns (report_id, serialised_entries).
-    """
-    from minerva_state import MinervaState
-
+def _load_review_task(report_id: str) -> tuple[ReportSummary | None, list[ReviewEntry], dict[int, LibraryItem]]:
     state = MinervaState()
+    report = state.get_report(report_id)
     entries = state.get_entries(report_id)
-    # Serialise for safe cross-thread transfer
-    serialised = [
-        {
-            "id": e.id,
-            "report_id": e.report_id,
-            "ordinal": e.ordinal,
-            "filename": e.filename,
-            "size": e.size,
-            "automatic_file_id": e.automatic_file_id,
-            "automatic_method": e.automatic_method,
-            "automatic_confidence": e.automatic_confidence,
-            "decision": e.decision,
-            "selected_file_id": e.selected_file_id,
-        }
-        for e in entries
-    ]
-    return report_id, serialised
-
-
-# ── Candidate card widget ─────────────────────────────────────────────────────
-
-
-class _CandidateCard(QtWidgets.QFrame):
-    """A single candidate card with confidence bar and approve button."""
-
-    approved = QtCore.pyqtSignal(int, str)  # (file_id, method)
-
-    def __init__(
-        self,
-        candidate: dict[str, Any],
-        parent: QtWidgets.QWidget | None = None,
-    ) -> None:
-        super().__init__(parent)
-        self._candidate = candidate
-        self.setObjectName("surfaceCard")
-        self.setFrameShape(QtWidgets.QFrame.Shape.StyledPanel)
-
-        layout = QtWidgets.QVBoxLayout(self)
-        layout.setContentsMargins(8, 8, 8, 8)
-        layout.setSpacing(4)
-
-        # Title
-        title = QtWidgets.QLabel(candidate.get("title", "Unknown"))
-        title.setObjectName("metricLabel")
-        title.setWordWrap(True)
-        layout.addWidget(title)
-
-        # Info line
-        info = QtWidgets.QLabel(
-            f'{candidate.get("collection", "")} / {candidate.get("system", "")}  '
-            f'  \u2022  {_fmt_bytes(candidate.get("size", 0))}',
-        )
-        info.setObjectName("mutedLabel")
-        layout.addWidget(info)
-
-        # Method + confidence bar
-        method_label = QtWidgets.QLabel(
-            f'Method: {candidate.get("method", "?")}  '
-            f'  Confidence: {candidate.get("confidence", 0):.1%}',
-        )
-        method_label.setObjectName("mutedLabel")
-        layout.addWidget(method_label)
-
-        confidence = max(0.0, min(1.0, candidate.get("confidence", 0.0)))
-        bar = QtWidgets.QProgressBar()
-        bar.setRange(0, 100)
-        bar.setValue(int(confidence * 100))
-        bar.setTextVisible(False)
-        bar.setFixedHeight(8)
-        layout.addWidget(bar)
-
-        # Reasons
-        reasons = candidate.get("reasons", [])
-        if reasons:
-            reasons_label = QtWidgets.QLabel("\n".join(f"\u2022 {r}" for r in reasons))
-            reasons_label.setObjectName("mutedLabel")
-            reasons_label.setWordWrap(True)
-            layout.addWidget(reasons_label)
-
-        # Approve button
-        self._approve_btn = QtWidgets.QPushButton("Approve This Match")
-        self._approve_btn.setObjectName("accentButton")
-        self._approve_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._approve_btn.clicked.connect(self._on_approve)
-        layout.addWidget(self._approve_btn)
-
-    def _on_approve(self) -> None:
-        self.approved.emit(
-            self._candidate.get("file_id", 0),
-            self._candidate.get("method", "fuzzy"),
-        )
-
-
-def _fmt_bytes(size: int) -> str:
-    """Format byte count as a human-readable binary size string."""
-    units = ["B", "KB", "MB", "GB", "TB"]
-    v = float(size)
-    idx = 0
-    while v >= 1024 and idx < len(units) - 1:
-        v /= 1024.0
-        idx += 1
-    if idx == 0:
-        return f"{int(v)} {units[idx]}"
-    return f"{v:.1f} {units[idx]}"
-
-
-# ── Page implementation ────────────────────────────────────────────────────────
+    ids = {
+        file_id
+        for entry in entries
+        for file_id in (entry.selected_file_id, entry.automatic_file_id)
+        if file_id is not None
+    }
+    items = MinervaDB().get_files_by_ids(ids)
+    return report, entries, {item.id: item for item in items}
+
+
+def _candidate_task(entry: ReviewEntry, report: ReportSummary) -> dict[str, Any]:
+    result = MinervaDB().match_dat_detailed(
+        [DatEntry(filename=entry.filename, size=entry.size)],
+        collection=report.collection or "",
+        system=report.system or "",
+        candidate_limit=8,
+    )
+    return result["results"][0]
 
 
 class MatchReviewPage(BasePage):
-    """Review individual fix report entries and make match decisions.
-
-    Displays the entry list for the current report (identified by
-    ``AppState.current_report_id``) with match-category filtering and
-    per-entry candidate comparison.
-
-    The right panel shows candidates (re-matched on demand via
-    ``match_dat_detailed``) with confidence bars, approve/reject/skip
-    buttons, a bulk "Approve All Exact" action, and a "Generate Final
-    DAT" export.
-    """
+    """Three-column review workspace matching the approved mockup."""
 
     def __init__(self, app_state: AppState) -> None:
         super().__init__(app_state)
-
-        # ── Generation counter for stale-result rejection ─────────────────
-        self._gen_counter = 0
         self._pool = QtCore.QThreadPool.globalInstance()
-
-        # Current report data (loaded on activate)
-        self._report_name: str = ""
-        self._report_collection: str = ""
-        self._report_system: str = ""
-
-        # ── Model layer ───────────────────────────────────────────────────
-        self._entry_model: EntryListModel = EntryListModel(
-            [], _ENTRY_COLUMNS, parent=self,
-        )
-        self._entry_proxy = MatchCategoryFilterProxy(self)
-        self._entry_proxy.setSourceModel(self._entry_model)
-        self._entry_proxy.setDynamicSortFilter(False)
-
-        # ── View: entry list ─────────────────────────────────────────────
-        self._entry_view = QtWidgets.QTableView(self)
-        self._entry_view.setModel(self._entry_proxy)
-        self._entry_view.setSelectionBehavior(
-            QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows,
-        )
-        self._entry_view.setSelectionMode(
-            QtWidgets.QAbstractItemView.SelectionMode.SingleSelection,
+        self._report: ReportSummary | None = None
+        self._rows: list[ReviewRow] = []
+        self._candidate_generation = 0
+        self._selected_candidate: dict[str, Any] | None = None
+
+        self._model = RecordListModel([], _REVIEW_COLUMNS, self)
+        self._proxy = _ReviewFilter(self)
+        self._proxy.setSourceModel(self._model)
+        self._view = QtWidgets.QTableView()
+        self._view.setModel(self._proxy)
+        self._view.setSelectionBehavior(QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows)
+        self._view.setSelectionMode(QtWidgets.QAbstractItemView.SelectionMode.ExtendedSelection)
+        self._view.setAlternatingRowColors(True)
+        self._view.verticalHeader().hide()
+        self._view.setItemDelegate(DisplayDelegate(self._view))
+        self._view.setItemDelegateForColumn(4, SizeDelegate(self._view))
+        self._view.horizontalHeader().setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
+        self._view.horizontalHeader().setSectionResizeMode(1, QtWidgets.QHeaderView.ResizeMode.Stretch)
+        for column, width in {2: 90, 3: 90, 4: 90, 5: 90}.items():
+            self._view.horizontalHeader().resizeSection(column, width)
+        self._view.selectionModel().selectionChanged.connect(self._on_selection_changed)
+
+        self._header = PageHeader(
+            "Match review",
+            "Inspect unmatched and fuzzy entries before creating the final queue.",
         )
-        self._entry_view.verticalHeader().hide()
-        self._entry_view.setSortingEnabled(True)
-        self._entry_view.setItemDelegate(DisplayDelegate(self._entry_view))
-        self._entry_view.setItemDelegateForColumn(1, SizeDelegate(self._entry_view))
-
-        header = self._entry_view.horizontalHeader()
-        header.setStretchLastSection(False)
-        header.setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
-        header.resizeSection(1, 100)
-        header.resizeSection(2, 80)
-        header.resizeSection(3, 90)
-        header.resizeSection(4, 80)
-
-        self._entry_view.selectionModel().selectionChanged.connect(
-            self._on_entry_selected,
-        )
-
-        # ── Filter buttons ───────────────────────────────────────────────
-        self._filter_btns: dict[str, QtWidgets.QPushButton] = {}
-        filter_group = QtWidgets.QButtonGroup(self)
-        filter_group.setExclusive(True)
-
-        for key, label in [
-            (MatchCategoryFilterProxy.MATCH_ALL, "All"),
-            (MatchCategoryFilterProxy.MATCH_EXACT, "Exact"),
-            (MatchCategoryFilterProxy.MATCH_FUZZY, "Fuzzy"),
-            (MatchCategoryFilterProxy.MATCH_UNMATCHED, "Unmatched"),
-        ]:
-            btn = QtWidgets.QPushButton(label)
-            btn.setCheckable(True)
-            btn.setObjectName("subtleButton")
-            btn.setCursor(Qt.CursorShape.PointingHandCursor)
-            btn.clicked.connect(lambda _checked, k=key: self._set_match_filter(k))
-            filter_group.addButton(btn)
-            self._filter_btns[key] = btn
-
-        # Default to "All"
-        self._filter_btns[MatchCategoryFilterProxy.MATCH_ALL].setChecked(True)
-
-        # ── Approve all exact button ─────────────────────────────────────
-        self._approve_all_exact_btn = QtWidgets.QPushButton(
-            Icons.check(), "Approve All Exact",
-        )
-        self._approve_all_exact_btn.setObjectName("accentButton")
-        self._approve_all_exact_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._approve_all_exact_btn.clicked.connect(self._on_approve_all_exact)
-
-        # ── Filter toolbar ───────────────────────────────────────────────
-        filter_bar = QtWidgets.QHBoxLayout()
-        filter_bar.setContentsMargins(0, 0, 0, 0)
-        filter_bar.setSpacing(8)
-        for key in (
-            MatchCategoryFilterProxy.MATCH_ALL,
-            MatchCategoryFilterProxy.MATCH_EXACT,
-            MatchCategoryFilterProxy.MATCH_FUZZY,
-            MatchCategoryFilterProxy.MATCH_UNMATCHED,
-        ):
-            filter_bar.addWidget(self._filter_btns[key])
-        filter_bar.addStretch(1)
-        filter_bar.addWidget(self._approve_all_exact_btn)
-
-        # ── Candidate panel (right side) ─────────────────────────────────
-        self._candidate_scroll = QtWidgets.QScrollArea()
-        self._candidate_scroll.setWidgetResizable(True)
-        self._candidate_scroll.setFrameShape(QtWidgets.QFrame.Shape.NoFrame)
-
-        self._candidate_container = QtWidgets.QWidget()
-        self._candidate_layout = QtWidgets.QVBoxLayout(self._candidate_container)
-        self._candidate_layout.setContentsMargins(8, 8, 8, 8)
-        self._candidate_layout.setSpacing(8)
-
-        # Selected entry info header
-        self._candidate_entry_label = QtWidgets.QLabel("Select an entry")
-        self._candidate_entry_label.setObjectName("pageSubtitle")
-        self._candidate_entry_label.setWordWrap(True)
-        self._candidate_layout.addWidget(self._candidate_entry_label)
-
-        # Candidates placeholder
-        self._candidate_empty = QtWidgets.QLabel(
-            "Select an entry to view candidates",
-        )
-        self._candidate_empty.setAlignment(Qt.AlignmentFlag.AlignCenter)
-        self._candidate_empty.setObjectName("mutedLabel")
-        self._candidate_layout.addWidget(self._candidate_empty)
-        self._candidate_layout.addStretch(1)
-
-        self._candidate_scroll.setWidget(self._candidate_container)
-
-        # ── Decision buttons (below candidates) ──────────────────────────
-        self._accept_btn = QtWidgets.QPushButton(Icons.check(), "Accept Best")
-        self._accept_btn.setObjectName("accentButton")
-        self._accept_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._accept_btn.clicked.connect(self._on_accept_best)
-        self._accept_btn.setEnabled(False)
-
-        self._reject_btn = QtWidgets.QPushButton(Icons.error(), "Reject")
-        self._reject_btn.setObjectName("subtleButton")
-        self._reject_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._reject_btn.clicked.connect(self._on_reject)
-        self._reject_btn.setEnabled(False)
-
-        self._skip_btn = QtWidgets.QPushButton(Icons.skip(), "Skip")
-        self._skip_btn.setObjectName("subtleButton")
-        self._skip_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._skip_btn.clicked.connect(self._on_skip)
-        self._skip_btn.setEnabled(False)
-
-        decision_row = QtWidgets.QHBoxLayout()
-        decision_row.setContentsMargins(0, 0, 0, 0)
-        decision_row.setSpacing(8)
-        decision_row.addWidget(self._accept_btn)
-        decision_row.addWidget(self._reject_btn)
-        decision_row.addWidget(self._skip_btn)
-        decision_row.addStretch(1)
-
-        # ── Generate Final DAT ───────────────────────────────────────────
-        self._generate_dat_btn = QtWidgets.QPushButton(
-            Icons.download(), "Generate Final DAT",
-        )
-        self._generate_dat_btn.setObjectName("accentButton")
-        self._generate_dat_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._generate_dat_btn.clicked.connect(self._on_generate_dat)
-
-        # Right panel layout
-        right_panel = QtWidgets.QWidget()
-        right_layout = QtWidgets.QVBoxLayout(right_panel)
-        right_layout.setContentsMargins(0, 0, 0, 0)
-        right_layout.setSpacing(8)
-        right_layout.addWidget(self._candidate_scroll, stretch=1)
-        right_layout.addLayout(decision_row)
-        right_layout.addWidget(self._generate_dat_btn)
-
-        # ── Entry list + filter (left panel wrapper) ─────────────────────
-        left_widget = QtWidgets.QWidget()
-        left_layout = QtWidgets.QVBoxLayout(left_widget)
-        left_layout.setContentsMargins(0, 0, 0, 0)
-        left_layout.setSpacing(4)
-        left_layout.addLayout(filter_bar)
-        left_layout.addWidget(self._entry_view, stretch=1)
-
-        # ── Splitter ─────────────────────────────────────────────────────
-        self._splitter = QtWidgets.QSplitter(Qt.Orientation.Horizontal, self)
-        self._splitter.addWidget(left_widget)
-        self._splitter.addWidget(right_panel)
-        self._splitter.setStretchFactor(0, 1)
-        self._splitter.setStretchFactor(1, 2)
-
-        # ── State stack ──────────────────────────────────────────────────
-        self._stack = QtWidgets.QStackedWidget(self)
-
-        self._empty_state = EmptyState(
-            parent=self._stack,
-            icon=Icons.match_review(),
-            title="No entries to review",
-            description="Import a report first from the Reports page.",
-        )
-        self._stack.addWidget(self._empty_state)  # index 0 — EMPTY
-
-        self._label_loading = _state_label("Loading\u2026")
-        self._stack.addWidget(self._label_loading)  # index 1 — LOADING
-
-        self._label_error = _state_label("An error occurred")
-        self._stack.addWidget(self._label_error)  # index 2 — ERROR
-
-        # Results: page header + splitter
-        results_widget = QtWidgets.QWidget()
-        results_layout = QtWidgets.QVBoxLayout(results_widget)
-        results_layout.setContentsMargins(0, 0, 0, 0)
-        results_layout.setSpacing(8)
-
-        # Header with back button
-        header_row = QtWidgets.QHBoxLayout()
-        header_row.setContentsMargins(0, 0, 0, 0)
-        header_row.setSpacing(12)
-
-        self._back_btn = QtWidgets.QPushButton(Icons.back(), "Back to Reports")
-        self._back_btn.setObjectName("subtleButton")
-        self._back_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._back_btn.clicked.connect(self._go_back)
-
-        self._page_header = PageHeader("Match Review")
-        header_row.addWidget(self._page_header, stretch=1)
-        header_row.addWidget(self._back_btn)
-
-        results_layout.addLayout(header_row)
-        results_layout.addWidget(self._splitter, stretch=1)
+        back = self._header.add_action("Back", Icons.back())
+        self._approve_btn = self._header.add_action("Approve selected", Icons.check(), primary=True)
+        self._ignore_btn = self._header.add_action("Ignore", Icons.skip())
+        self._queue_btn = self._header.add_action("Add to queue", Icons.add(), primary=True)
+        back.clicked.connect(self._go_back)
+        self._approve_btn.clicked.connect(self._approve_selected)
+        self._ignore_btn.clicked.connect(self._ignore_selected)
+        self._queue_btn.clicked.connect(self._queue_approved)
+
+        self._exact_card = MetricCard("Exact", "0", MetricKind.INFO, icon=Icons.check())
+        self._fuzzy_card = MetricCard("Fuzzy", "0", MetricKind.WARNING, icon=Icons.search())
+        self._unmatched_card = MetricCard("Unmatched", "0", MetricKind.ERROR, icon=Icons.status_error())
+        self._ignored_card = MetricCard("Ignored", "0", MetricKind.NEUTRAL, icon=Icons.skip())
+        metrics = QtWidgets.QHBoxLayout()
+        for card in (self._exact_card, self._fuzzy_card, self._unmatched_card, self._ignored_card):
+            metrics.addWidget(card)
+
+        self._report_list = QtWidgets.QListWidget()
+        self._report_list.setSelectionMode(QtWidgets.QAbstractItemView.SelectionMode.SingleSelection)
+        self._report_list.currentItemChanged.connect(self._on_report_changed)
+        report_card = SectionCard("Reports", Icons.reports())
+        report_card.content_layout().addWidget(self._report_list)
+        report_card.setMinimumWidth(220)
+        report_card.setMaximumWidth(290)
+
+        search = QtWidgets.QLineEdit()
+        search.setPlaceholderText("Search entries…")
+        search.setClearButtonEnabled(True)
+        search.addAction(Icons.search(), QtWidgets.QLineEdit.ActionPosition.LeadingPosition)
+        search.textChanged.connect(self._set_text_filter)
+        filters = QtWidgets.QHBoxLayout()
+        filters.addWidget(search, 1)
+        self._filter_buttons: dict[str, QtWidgets.QPushButton] = {}
+        button_group = QtWidgets.QButtonGroup(self)
+        button_group.setExclusive(True)
+        for key, label in (("all", "All"), ("exact", "Exact"), ("fuzzy", "Fuzzy"), ("unmatched", "Unmatched")):
+            button = QtWidgets.QPushButton(label)
+            button.setObjectName("subtleButton")
+            button.setCheckable(True)
+            button.clicked.connect(lambda _checked, value=key: self._set_category_filter(value))
+            button_group.addButton(button)
+            filters.addWidget(button)
+            self._filter_buttons[key] = button
+        self._filter_buttons["all"].setChecked(True)
+
+        comparison = self._build_comparison_panel()
+        center = QtWidgets.QWidget()
+        center_layout = QtWidgets.QVBoxLayout(center)
+        center_layout.setContentsMargins(0, 0, 0, 0)
+        center_layout.setSpacing(10)
+        center_layout.addLayout(filters)
+        center_layout.addWidget(self._view, 1)
+        center_layout.addWidget(comparison)
+
+        bulk_card = self._build_bulk_panel()
+        bulk_card.setMinimumWidth(245)
+        bulk_card.setMaximumWidth(310)
+
+        splitter = QtWidgets.QSplitter(Qt.Orientation.Horizontal)
+        splitter.addWidget(report_card)
+        splitter.addWidget(center)
+        splitter.addWidget(bulk_card)
+        splitter.setStretchFactor(1, 1)
+        splitter.setSizes([245, 850, 275])
+        self._splitter = splitter
+
+        root = QtWidgets.QVBoxLayout(self)
+        root.setContentsMargins(18, 18, 18, 18)
+        root.setSpacing(12)
+        root.addWidget(self._header)
+        root.addLayout(metrics)
+        root.addWidget(splitter, 1)
+
+        self._refresh_reports()
+        self.refresh()
 
-        self._stack.addWidget(results_widget)  # index 3 — RESULTS
+    # ── UI construction ───────────────────────────────────────────────
 
-        # ── Root layout ──────────────────────────────────────────────────
-        layout = QtWidgets.QVBoxLayout(self)
+    def _build_comparison_panel(self) -> QtWidgets.QWidget:
+        wrapper = QtWidgets.QWidget()
+        layout = QtWidgets.QHBoxLayout(wrapper)
         layout.setContentsMargins(0, 0, 0, 0)
-        layout.addWidget(self._stack)
-
-        # Cache of raw entry dicts (for decision updates without re-fetch)
-        self._raw_entries: list[dict[str, Any]] = []
-
-        # Initial state
-        self._set_state(MatchReviewPageState.EMPTY)
-
-    # ── State management ─────────────────────────────────────────────────
-
-    def _set_state(self, state: MatchReviewPageState) -> None:
-        """Switch to the given visual state."""
-        index_map = {
-            MatchReviewPageState.EMPTY: 0,
-            MatchReviewPageState.LOADING: 1,
-            MatchReviewPageState.ERROR: 2,
-            MatchReviewPageState.RESULTS: 3,
-        }
-        self._stack.setCurrentIndex(index_map[state])
+        layout.setSpacing(10)
+
+        requested = SectionCard("Requested entry", Icons.file())
+        self._requested_title = QtWidgets.QLabel("Select an entry")
+        self._requested_title.setObjectName("sectionTitle")
+        self._requested_title.setWordWrap(True)
+        self._requested_meta = QtWidgets.QLabel("")
+        self._requested_meta.setObjectName("mutedLabel")
+        self._requested_meta.setWordWrap(True)
+        requested.content_layout().addWidget(self._requested_title)
+        requested.content_layout().addWidget(self._requested_meta)
+        requested.content_layout().addStretch(1)
+
+        proposed = SectionCard("Proposed match", Icons.search())
+        self._proposed_title = QtWidgets.QLabel("No candidate selected")
+        self._proposed_title.setObjectName("sectionTitle")
+        self._proposed_title.setWordWrap(True)
+        self._proposed_meta = QtWidgets.QLabel("")
+        self._proposed_meta.setObjectName("mutedLabel")
+        self._proposed_meta.setWordWrap(True)
+        self._candidate_reasons = TagFlow()
+        self._candidate_list = QtWidgets.QListWidget()
+        self._candidate_list.setMaximumHeight(95)
+        self._candidate_list.currentItemChanged.connect(self._on_candidate_changed)
+        use_candidate = QtWidgets.QPushButton(Icons.check(), "Use selected candidate")
+        use_candidate.setObjectName("primaryButton")
+        use_candidate.clicked.connect(self._approve_candidate)
+        proposed.content_layout().addWidget(self._proposed_title)
+        proposed.content_layout().addWidget(self._proposed_meta)
+        proposed.content_layout().addWidget(self._candidate_reasons)
+        proposed.content_layout().addWidget(self._candidate_list)
+        proposed.content_layout().addWidget(use_candidate)
+
+        layout.addWidget(requested, 1)
+        layout.addWidget(proposed, 1)
+        return wrapper
+
+    def _build_bulk_panel(self) -> SectionCard:
+        card = SectionCard("Bulk actions", Icons.queue())
+        self._threshold = QtWidgets.QSpinBox()
+        self._threshold.setRange(50, 100)
+        self._threshold.setValue(95)
+        self._threshold.setSuffix("% confidence")
+        approve_fuzzy = QtWidgets.QPushButton(Icons.check(), "Approve fuzzy above threshold")
+        approve_exact = QtWidgets.QPushButton(Icons.status_success(), "Approve all exact")
+        ignore = QtWidgets.QPushButton(Icons.skip(), "Ignore selected")
+        export = QtWidgets.QPushButton(Icons.download(), "Export unmatched")
+        generate = QtWidgets.QPushButton(Icons.file(), "Generate final DAT")
+        approve_fuzzy.clicked.connect(self._approve_fuzzy_threshold)
+        approve_exact.clicked.connect(self._approve_all_exact)
+        ignore.clicked.connect(self._ignore_selected)
+        export.clicked.connect(self._export_unmatched)
+        generate.clicked.connect(self._generate_final_dat)
+        card.content_layout().addWidget(self._threshold)
+        for button in (approve_fuzzy, approve_exact, ignore, export, generate):
+            card.content_layout().addWidget(button)
+
+        label = QtWidgets.QLabel("Review statistics")
+        label.setObjectName("sectionTitle")
+        card.content_layout().addSpacing(8)
+        card.content_layout().addWidget(label)
+        self._selected_stat = QtWidgets.QLabel("Selected\n0")
+        self._approved_stat = QtWidgets.QLabel("Approved\n0")
+        self._ignored_stat = QtWidgets.QLabel("Ignored\n0")
+        self._remaining_stat = QtWidgets.QLabel("Remaining\n0")
+        for widget in (self._selected_stat, self._approved_stat, self._ignored_stat, self._remaining_stat):
+            widget.setObjectName("mutedLabel")
+            card.content_layout().addWidget(widget)
+        card.content_layout().addStretch(1)
+        tip = QtWidgets.QLabel(
+            "Use filters to focus on fuzzy or unmatched entries. Manual decisions are persisted immediately."
+        )
+        tip.setWordWrap(True)
+        tip.setObjectName("mutedLabel")
+        card.content_layout().addWidget(tip)
+        return card
 
-    # ── Lifecycle ────────────────────────────────────────────────────────
+    # ── Lifecycle and loading ─────────────────────────────────────────
 
     def activate(self) -> None:
-        """Called when the page becomes visible — reload data."""
-        super().activate()
+        self._refresh_reports()
         self.refresh()
 
     def refresh(self) -> None:
-        """Reload entries for ``current_report_id`` from MinervaState."""
-        report_id = self._app_state.current_report_id
+        report_id = self.app_state.current_report_id
         if not report_id:
-            self._set_state(MatchReviewPageState.EMPTY)
+            self._clear_workspace("Choose a report from Reports")
             return
-
-        self._set_state(MatchReviewPageState.LOADING)
-
-        # Load report metadata and entries in background
-        self._gen_counter += 1
-        generation = self._gen_counter
-
-        task = TaskRunner(_load_entries_task, report_id)
-        task.signals.result.connect(
-            lambda payload, g=generation: self._on_entries_loaded(g, payload),
-        )
-        task.signals.error.connect(
-            lambda details, g=generation: self._on_load_error(g, details),
-        )
+        task = TaskRunner(_load_review_task, report_id)
+        task.signals.result.connect(self._on_loaded)
+        task.signals.error.connect(lambda error: NotificationBanner.show_error(self, "Review load failed", error))
         self._pool.start(task)
 
-    def _on_entries_loaded(self, generation: int, payload: object) -> None:
-        """Handle the result of the background entries load."""
-        if generation != self._gen_counter:
+    def _on_loaded(self, payload) -> None:
+        report, entries, item_map = payload
+        if report is None or report.id != self.app_state.current_report_id:
             return
-
-        try:
-            report_id, serialised = payload  # type: ignore[misc]
-        except (TypeError, ValueError) as exc:
-            log.warning("Invalid entries payload: %s", exc)
-            self._set_state(MatchReviewPageState.ERROR)
-            return
-
-        # Fetch report metadata
-        try:
-            from minerva_state import MinervaState
-
-            state = MinervaState()
-            report = state.get_report(report_id)
-            self._report_name = report.name if report else "Unknown Report"
-            self._report_collection = report.collection or ""
-            self._report_system = report.system or ""
-        except Exception as exc:
-            log.warning("Failed to load report: %s", exc)
-            self._report_name = "Unknown Report"
-
-        self._page_header.set_title(f"Match Review - {self._report_name}")
-
-        self._raw_entries = serialised
-
-        # Convert serialised dicts to ReviewEntry objects for the model
-        from minerva.domain.reports import ReviewEntry
-
-        entries = [
-            ReviewEntry(
-                id=e["id"],
-                report_id=e["report_id"],
-                ordinal=e["ordinal"],
-                filename=e["filename"],
-                size=e["size"],
-                automatic_file_id=e["automatic_file_id"],
-                automatic_method=e["automatic_method"],
-                automatic_confidence=e["automatic_confidence"],
-                decision=e["decision"],
-                selected_file_id=e["selected_file_id"],
-            )
-            for e in serialised
+        self._report = report
+        self._rows = [
+            ReviewRow(entry, item_map.get(entry.selected_file_id or entry.automatic_file_id or -1))
+            for entry in entries
         ]
-
-        self._entry_model.set_records(entries)
-
-        if entries:
-            self._set_state(MatchReviewPageState.RESULTS)
+        self._model.set_records(self._rows)
+        self._sync_report_selection(report.id)
+        self._update_counts()
+        if self._proxy.rowCount() > 0:
+            self._view.selectRow(0)
         else:
-            self._set_state(MatchReviewPageState.EMPTY)
-
-        # Clear candidate panel
-        self._clear_candidates()
+            self._clear_comparison()
+
+    def _refresh_reports(self) -> None:
+        current = self.app_state.current_report_id
+        self._report_list.blockSignals(True)
+        self._report_list.clear()
+        for report in MinervaState().list_reports():
+            item = QtWidgets.QListWidgetItem(Icons.reports(), f"{report.name}\n{report.status.title()} · {report.requested_count:,} entries")
+            item.setData(Qt.ItemDataRole.UserRole, report.id)
+            self._report_list.addItem(item)
+            if report.id == current:
+                self._report_list.setCurrentItem(item)
+        self._report_list.blockSignals(False)
+
+    def _sync_report_selection(self, report_id: str) -> None:
+        for index in range(self._report_list.count()):
+            item = self._report_list.item(index)
+            if item.data(Qt.ItemDataRole.UserRole) == report_id:
+                self._report_list.blockSignals(True)
+                self._report_list.setCurrentItem(item)
+                self._report_list.blockSignals(False)
+                break
 
-    def _on_load_error(self, generation: int, details: str) -> None:
-        """Handle entries load error."""
-        if generation != self._gen_counter:
+    def _on_report_changed(self, current, _previous) -> None:
+        if current is None:
             return
-        log.warning("Load entries error: %s", details)
-        self._set_state(MatchReviewPageState.ERROR)
-
-    # ── Entry filtering ──────────────────────────────────────────────────
+        report_id = current.data(Qt.ItemDataRole.UserRole)
+        if report_id and report_id != self.app_state.current_report_id:
+            self.app_state.current_report_id = report_id
+            self.refresh()
 
-    def _set_match_filter(self, filter_key: str) -> None:
-        """Update the match category filter on the proxy model."""
-        self._entry_proxy.setMatchFilter(filter_key)
+    # ── Filtering and selection ───────────────────────────────────────
 
-    # ── Entry selection → candidate loading ──────────────────────────────
+    def _set_text_filter(self, text: str) -> None:
+        self._proxy.text = text.strip()
+        self._proxy.invalidateFilter()
 
-    def _on_entry_selected(self) -> None:
-        """Load and display candidates for the selected entry."""
-        indexes = self._entry_view.selectionModel().selectedRows()
-        if not indexes:
-            self._clear_candidates()
-            return
+    def _set_category_filter(self, category: str) -> None:
+        self._proxy.category = category
+        self._proxy.invalidateFilter()
 
-        proxy_index = indexes[0]
-        source_index = self._entry_proxy.mapToSource(proxy_index)
-        row = source_index.row()
-        if row < 0 or row >= len(self._entry_model._records):  # noqa: SLF001
+    def _selected_source_rows(self) -> list[int]:
+        rows = {
+            self._proxy.mapToSource(index).row()
+            for index in self._view.selectionModel().selectedRows()
+            if index.isValid()
+        }
+        return sorted(rows)
+
+    def _current_row(self) -> ReviewRow | None:
+        current = self._view.currentIndex()
+        if not current.isValid():
+            return None
+        source = self._proxy.mapToSource(current)
+        if not source.isValid() or source.row() >= len(self._rows):
+            return None
+        return self._rows[source.row()]
+
+    def _on_selection_changed(self, _selected=None, _deselected=None) -> None:
+        self._selected_stat.setText(f"Selected\n{len(self._selected_source_rows())}")
+        row = self._current_row()
+        if row is None:
+            self._clear_comparison()
             return
+        self._show_requested(row)
+        self._load_candidates(row.entry)
 
-        entry = self._entry_model._records[row]  # noqa: SLF001
-        self._selected_entry = entry
+    # ── Candidate comparison ──────────────────────────────────────────
 
-        # Update header
-        self._candidate_entry_label.setText(
-            f"Entry: {entry.filename}  ({_fmt_bytes(entry.size)})",
+    def _show_requested(self, row: ReviewRow) -> None:
+        self._requested_title.setText(row.entry.filename)
+        self._requested_meta.setText(
+            f"Expected size\n{_fmt_bytes(row.entry.size)}\n\n"
+            f"Automatic method\n{row.entry.automatic_method or 'Unmatched'}\n\n"
+            f"Decision\n{row.entry.decision.title()}"
         )
-        self._update_decision_buttons(entry.decision)
-
-        # Load candidates in background
-        self._set_candidate_loading()
-
-        self._gen_counter += 1
-        generation = self._gen_counter
 
-        task = TaskRunner(
-            _load_candidates_task,
-            generation,
-            entry.filename,
-            entry.size,
-            self._report_collection,
-            self._report_system,
-        )
-        task.signals.result.connect(
-            lambda payload, g=generation: self._on_candidates_loaded(g, payload),
-        )
-        task.signals.error.connect(
-            lambda details, g=generation: self._on_candidate_error(g, details),
-        )
+    def _load_candidates(self, entry: ReviewEntry) -> None:
+        if self._report is None:
+            return
+        self._candidate_generation += 1
+        generation = self._candidate_generation
+        self._candidate_list.clear()
+        self._proposed_title.setText("Loading candidates…")
+        self._proposed_meta.clear()
+        self._candidate_reasons.set_tags([])
+        task = TaskRunner(_candidate_task, entry, self._report)
+        task.signals.result.connect(lambda result, value=generation: self._on_candidates_loaded(value, result))
+        task.signals.error.connect(lambda error: NotificationBanner.show_error(self, "Candidate search failed", error))
         self._pool.start(task)
 
-    def _set_candidate_loading(self) -> None:
-        """Show a loading indicator in the candidate panel."""
-        self._clear_candidate_cards()
-        self._candidate_empty.setText("Loading candidates\u2026")
-        self._candidate_empty.show()
-        self._accept_btn.setEnabled(False)
-        self._reject_btn.setEnabled(False)
-        self._skip_btn.setEnabled(False)
-
-    def _clear_candidates(self) -> None:
-        """Clear the candidate panel to its default empty state."""
-        self._selected_entry = None
-        self._candidate_entry_label.setText("Select an entry")
-        self._clear_candidate_cards()
-        self._candidate_empty.setText("Select an entry to view candidates")
-        self._candidate_empty.show()
-        self._accept_btn.setEnabled(False)
-        self._reject_btn.setEnabled(False)
-        self._skip_btn.setEnabled(False)
-
-    def _clear_candidate_cards(self) -> None:
-        """Remove all candidate cards from the layout (keep header + empty)."""
-        for i in reversed(range(self._candidate_layout.count())):
-            item = self._candidate_layout.itemAt(i)
-            if item is None:
-                continue
-            w = item.widget()
-            if w is not None and w not in (
-                self._candidate_entry_label,
-                self._candidate_empty,
-            ):
-                self._candidate_layout.removeWidget(w)
-                w.deleteLater()
-
-    def _on_candidates_loaded(self, generation: int, payload: object) -> None:
-        """Display loaded candidates in the right panel."""
-        if generation != self._gen_counter:
+    def _on_candidates_loaded(self, generation: int, result: dict[str, Any]) -> None:
+        if generation != self._candidate_generation:
             return
-
-        try:
-            _gen, result = payload  # type: ignore[misc]
-        except (TypeError, ValueError) as exc:
-            log.warning("Invalid candidates payload: %s", exc)
+        self._candidate_list.clear()
+        candidates = result.get("candidates", [])
+        for candidate in candidates:
+            item = QtWidgets.QListWidgetItem(
+                f"{candidate['confidence']:.0%}  ·  {candidate['title']}  ·  {candidate['system']}"
+            )
+            item.setData(Qt.ItemDataRole.UserRole, candidate)
+            self._candidate_list.addItem(item)
+        if self._candidate_list.count():
+            self._candidate_list.setCurrentRow(0)
+        else:
+            self._selected_candidate = None
+            self._proposed_title.setText("No candidate found")
+            self._proposed_meta.setText("Try searching the Library or leave this entry unmatched.")
+            self._candidate_reasons.set_tags([])
+
+    def _on_candidate_changed(self, current, _previous) -> None:
+        self._selected_candidate = current.data(Qt.ItemDataRole.UserRole) if current is not None else None
+        candidate = self._selected_candidate
+        if not candidate:
             return
+        self._proposed_title.setText(candidate["title"])
+        self._proposed_meta.setText(
+            f"{candidate['collection']} / {candidate['system']}\n"
+            f"{_fmt_bytes(candidate['size'])} · {candidate['method'].title()} · {candidate['confidence']:.1%}"
+        )
+        self._candidate_reasons.set_tags(list(candidate.get("reasons", [])))
 
-        results_list = result.get("results", [])
-        if not results_list:
-            self._candidate_empty.setText("No candidates found")
-            self._candidate_empty.show()
+    def _approve_candidate(self) -> None:
+        row = self._current_row()
+        candidate = self._selected_candidate
+        if row is None or candidate is None:
             return
+        decision = "accept" if candidate.get("method") == "exact" else "fuzzy"
+        self._apply_decisions([(row.entry.id, decision, int(candidate["file_id"]))])
 
-        entry_result = results_list[0]
-        candidates = entry_result.get("candidates", [])
-        self._display_candidates(candidates)
+    # ── Decisions and bulk operations ─────────────────────────────────
+
+    def _approve_selected(self) -> None:
+        updates: list[tuple[str, str, int | None]] = []
+        for source_row in self._selected_source_rows():
+            row = self._rows[source_row]
+            file_id = row.entry.selected_file_id or row.entry.automatic_file_id
+            if file_id is None:
+                continue
+            decision = "accept" if row.category == "exact" else "fuzzy"
+            updates.append((row.entry.id, decision, file_id))
+        self._apply_decisions(updates)
+
+    def _ignore_selected(self) -> None:
+        updates = [
+            (self._rows[source_row].entry.id, "reject", None)
+            for source_row in self._selected_source_rows()
+        ]
+        self._apply_decisions(updates)
+
+    def _approve_all_exact(self) -> None:
+        updates = [
+            (row.entry.id, "accept", row.entry.selected_file_id or row.entry.automatic_file_id)
+            for row in self._rows
+            if row.category == "exact"
+            and row.entry.decision == "pending"
+            and (row.entry.selected_file_id or row.entry.automatic_file_id) is not None
+        ]
+        self._confirm_and_apply(updates, "Approve exact matches")
+
+    def _approve_fuzzy_threshold(self) -> None:
+        threshold = self._threshold.value() / 100.0
+        updates = [
+            (row.entry.id, "fuzzy", row.entry.selected_file_id or row.entry.automatic_file_id)
+            for row in self._rows
+            if row.category == "fuzzy"
+            and row.entry.decision == "pending"
+            and (row.entry.automatic_confidence or 0.0) >= threshold
+            and (row.entry.selected_file_id or row.entry.automatic_file_id) is not None
+        ]
+        self._confirm_and_apply(updates, f"Approve fuzzy matches at or above {threshold:.0%}")
 
-    def _on_candidate_error(self, generation: int, details: str) -> None:
-        """Handle candidate load error."""
-        if generation != self._gen_counter:
+    def _confirm_and_apply(self, updates: list[tuple[str, str, int | None]], title: str) -> None:
+        if not updates:
+            NotificationBanner.show_info(self, "Nothing to change", "No pending entries match this action")
             return
-        log.warning("Candidate load error: %s", details)
-        self._candidate_empty.setText("Failed to load candidates")
-        self._candidate_empty.show()
-
-    def _display_candidates(self, candidates: list[dict[str, Any]]) -> None:
-        """Populate the candidate panel with candidate cards."""
-        self._clear_candidate_cards()
-        self._candidate_empty.hide()
-
-        if not candidates:
-            self._candidate_empty.setText("No candidates found")
-            self._candidate_empty.show()
+        if QtWidgets.QMessageBox.question(
+            self,
+            title,
+            f"Apply this decision to {len(updates):,} entries?",
+        ) != QtWidgets.QMessageBox.StandardButton.Yes:
             return
+        self._apply_decisions(updates)
 
-        for cand in candidates:
-            card = _CandidateCard(cand, self._candidate_container)
-            card.approved.connect(self._on_approve_candidate)
-            # Insert before the stretch
-            self._candidate_layout.insertWidget(
-                self._candidate_layout.count() - 1, card,
-            )
-
-        # Enable decision buttons
-        self._accept_btn.setEnabled(bool(candidates))
-        self._reject_btn.setEnabled(
-            self._selected_entry is not None
-            and self._selected_entry.decision != "reject",
-        )
-        self._skip_btn.setEnabled(True)
-
-    # ── Decision actions ─────────────────────────────────────────────────
-
-    def _update_decision_buttons(self, decision: str) -> None:
-        """Update button enabled state based on the current decision."""
-        self._accept_btn.setEnabled(decision != "accept")
-        self._reject_btn.setEnabled(decision != "reject")
-        self._skip_btn.setEnabled(True)
+    def _apply_decisions(self, updates: list[tuple[str, str, int | None]]) -> None:
+        if not updates:
+            return
+        MinervaState().update_entry_decisions(updates)
+        self._update_report_status()
+        NotificationBanner.show_success(self, "Review updated", f"Updated {len(updates):,} entries")
+        self.refresh()
 
-    def _on_approve_candidate(self, file_id: int, method: str) -> None:
-        """Approve a specific candidate (called from a candidate card)."""
-        if self._selected_entry is None:
+    def _update_report_status(self) -> None:
+        if self._report is None:
             return
-        self._apply_decision(
-            self._selected_entry.id,
-            "accept",
-            file_id,
-        )
+        entries = MinervaState().get_entries(self._report.id)
+        status = "reviewed" if entries and all(entry.decision != "pending" for entry in entries) else "draft"
+        MinervaState().update_report(self._report.id, status=status)
+
+    # ── Export and queue ───────────────────────────────────────────────
+
+    def _approved_entries(self, selected_only: bool = False) -> list[ReviewEntry]:
+        selected = set(self._selected_source_rows())
+        return [
+            row.entry
+            for index, row in enumerate(self._rows)
+            if row.entry.decision in {"accept", "fuzzy"}
+            and (not selected_only or index in selected)
+            and (row.entry.selected_file_id or row.entry.automatic_file_id) is not None
+        ]
 
-    def _on_accept_best(self) -> None:
-        """Accept the best (first) candidate."""
-        if self._selected_entry is None:
+    def _queue_approved(self) -> None:
+        selected_only = bool(self._selected_source_rows())
+        entries = self._approved_entries(selected_only=selected_only)
+        if not entries:
+            NotificationBanner.show_warning(self, "Nothing approved", "Approve matches before adding them to the queue")
             return
-        # Find the best candidate's file_id from the stored entry
-        file_id = self._selected_entry.automatic_file_id
-        self._apply_decision(
-            self._selected_entry.id,
-            "accept",
-            file_id,
-        )
+        shell = self.window()
+        controller = getattr(shell, "download_controller", None)
+        if controller is None:
+            NotificationBanner.show_error(self, "Downloads unavailable", "The qBittorrent controller is not initialised")
+            return
+        ids = [entry.selected_file_id or entry.automatic_file_id for entry in entries]
+        items = {item.id: item for item in MinervaDB().get_files_by_ids(ids)}
+        output_root = Path(QtCore.QSettings("MinervaFixDAT", "MinervaGUI").value("output_dir", "downloads", str))
+        queue = []
+        for entry in entries:
+            file_id = entry.selected_file_id or entry.automatic_file_id
+            item = items.get(file_id)
+            if item is None:
+                continue
+            destination = output_root / item.collection / item.system / item.basename
+            queue.append((item.id, str(destination), entry.id))
+        controller.add_many_to_queue(queue)
+        NotificationBanner.show_success(self, "Added to queue", f"Queued {len(queue):,} reviewed files")
 
-    def _on_reject(self) -> None:
-        """Reject the selected entry (mark as unmatched)."""
-        if self._selected_entry is None:
+    def _export_unmatched(self) -> None:
+        if self._report is None:
             return
-        self._apply_decision(
-            self._selected_entry.id,
-            "reject",
-            selected_file_id=None,
+        entries = [
+            row.entry
+            for row in self._rows
+            if row.category == "unmatched" or row.entry.decision == "reject"
+        ]
+        if not entries:
+            NotificationBanner.show_info(self, "No unmatched entries", "Every entry currently has an accepted candidate")
+            return
+        path, _ = QtWidgets.QFileDialog.getSaveFileName(
+            self,
+            "Export unmatched DAT",
+            f"{self._report.name}-unmatched.dat",
+            "DAT files (*.dat)",
         )
-
-    def _on_skip(self) -> None:
-        """Skip the selected entry (leave as pending)."""
-        if self._selected_entry is None:
+        if not path:
             return
-        self._apply_decision(
-            self._selected_entry.id,
-            "pending",
-            selected_file_id=None,
+        rows = [
+            {"stem": stem_from_romname(entry.filename), "basename": entry.filename, "size": entry.size}
+            for entry in entries
+        ]
+        xml = MinervaDB().build_synthetic_dat(
+            rows,
+            f"{self._report.name} unmatched",
+            self._report.system,
+            self._report.collection,
         )
+        Path(path).write_text(xml, encoding="utf-8")
+        NotificationBanner.show_success(self, "Unmatched DAT exported", path)
 
-    def _apply_decision(
-        self,
-        entry_id: str,
-        decision: str,
-        selected_file_id: int | None = None,
-    ) -> None:
-        """Persist a decision for an entry and update the UI."""
-        try:
-            from minerva_state import MinervaState
-
-            state = MinervaState()
-            state.update_entry_decision(
-                entry_id,
-                decision=decision,
-                selected_file_id=selected_file_id,
-            )
-        except Exception as exc:
-            log.warning("Failed to save decision: %s", exc)
+    def _generate_final_dat(self) -> None:
+        if self._report is None:
             return
-
-        # Update the local entry model
-        for row, entry in enumerate(self._entry_model._records):  # noqa: SLF001
-            if entry.id == entry_id:
-                entry.decision = decision
-                entry.selected_file_id = selected_file_id
-                self._entry_model.update_record(row, entry)
-                self._selected_entry = entry
-                break
-
-        self._update_decision_buttons(decision)
-
-    def _on_approve_all_exact(self) -> None:
-        """Approve all entries with exact automatic matches."""
-        try:
-            from minerva_state import MinervaState
-
-            state = MinervaState()
-            entries = state.get_entries(
-                self._app_state.current_report_id or "",
-            )
-        except Exception as exc:
-            log.warning("Failed to load entries for bulk approve: %s", exc)
+        entries = self._approved_entries()
+        ids = [entry.selected_file_id or entry.automatic_file_id for entry in entries]
+        items = MinervaDB().get_files_by_ids(ids)
+        if not items:
+            NotificationBanner.show_warning(self, "Nothing approved", "Approve at least one entry first")
             return
-
-        approved = 0
-        for entry in entries:
-            if entry.automatic_method == "exact" and entry.decision != "accept":
-                state.update_entry_decision(
-                    entry.id,
-                    decision="accept",
-                    selected_file_id=entry.automatic_file_id,
-                )
-                approved += 1
-
-        if approved:
-            log.debug("Bulk approved %d exact entries", approved)
-            self.refresh()
-        else:
-            log.debug("No exact entries to approve")
-
-    # ── Generate Final DAT ──────────────────────────────────────────────
-
-    def _on_generate_dat(self) -> None:
-        """Generate a synthetic DAT from accepted entries."""
-        report_id = self._app_state.current_report_id
-        if not report_id:
+        path, _ = QtWidgets.QFileDialog.getSaveFileName(
+            self,
+            "Generate final DAT",
+            f"{self._report.name}-reviewed.dat",
+            "DAT files (*.dat)",
+        )
+        if not path:
             return
-
-        try:
-            from minerva_state import MinervaState
-
-            state = MinervaState()
-            report = state.get_report(report_id)
-            entries = state.get_entries(report_id)
-
-            # Collect accepted entries with file IDs
-            accepted = [
-                e for e in entries
-                if e.decision == "accept"
-                and (e.selected_file_id or e.automatic_file_id) is not None
-            ]
-
-            if not accepted:
-                QtWidgets.QMessageBox.information(
-                    self,
-                    "No Accepted Entries",
-                    "No entries have been accepted yet.\n"
-                    "Approve matches before generating the final DAT.",
-                )
-                return
-
-            # Fetch matching file rows from MinervaDB
-            file_ids = [
-                (e.selected_file_id or e.automatic_file_id)
-                for e in accepted
-            ]
-
-            from minerva_db import MinervaDB
-
-            db = MinervaDB()
-            with db.conn() as conn:
-                placeholders = ",".join("?" * len(file_ids))
-                rows = conn.execute(
-                    f"SELECT * FROM files WHERE id IN ({placeholders})",
-                    file_ids,
-                ).fetchall()
-
-            if not rows:
-                QtWidgets.QMessageBox.warning(
-                    self,
-                    "No Files Found",
-                    "Could not find the matching file records in the index.",
-                )
-                return
-
-            dat_name = f"{report.name or 'Synthetic'} - Final"
-            dat_xml = db.build_synthetic_dat(
-                rows,
-                dat_name=dat_name,
-                system=report.system,
-                collection=report.collection,
-            )
-
-            # Save to file
-            path_str, _filter = QtWidgets.QFileDialog.getSaveFileName(
-                self,
-                "Save Final DAT",
-                f"{dat_name}.dat",
-                "DAT files (*.dat);;All files (*)",
-            )
-            if not path_str:
-                return
-
-            Path(path_str).write_text(dat_xml, encoding="utf-8")
-
-            QtWidgets.QMessageBox.information(
-                self,
-                "DAT Generated",
-                f"Final DAT saved to:\n{path_str}\n\n"
-                f"Contains {len(rows)} matched entries.",
-            )
-
-        except Exception as exc:
-            log.warning("Failed to generate DAT: %s", exc, exc_info=True)
-            QtWidgets.QMessageBox.critical(
-                self,
-                "Error",
-                f"Failed to generate final DAT:\n{exc}",
-            )
-
-    # ── Navigation ──────────────────────────────────────────────────────
+        rows = [{"stem": item.stem, "basename": item.basename, "size": item.size} for item in items]
+        xml = MinervaDB().build_synthetic_dat(
+            rows,
+            f"{self._report.name} reviewed",
+            self._report.system,
+            self._report.collection,
+        )
+        Path(path).write_text(xml, encoding="utf-8")
+        NotificationBanner.show_success(self, "Final DAT generated", path)
+
+    # ── Counters and helpers ──────────────────────────────────────────
+
+    def _update_counts(self) -> None:
+        exact = sum(row.category == "exact" for row in self._rows)
+        fuzzy = sum(row.category == "fuzzy" for row in self._rows)
+        unmatched = sum(row.category == "unmatched" for row in self._rows)
+        ignored = sum(row.entry.decision == "reject" for row in self._rows)
+        approved = sum(row.entry.decision in {"accept", "fuzzy"} for row in self._rows)
+        remaining = sum(row.entry.decision == "pending" for row in self._rows)
+        self._exact_card.set_value(f"{exact:,}")
+        self._fuzzy_card.set_value(f"{fuzzy:,}")
+        self._unmatched_card.set_value(f"{unmatched:,}")
+        self._ignored_card.set_value(f"{ignored:,}")
+        self._selected_stat.setText(f"Selected\n{len(self._selected_source_rows())}")
+        self._approved_stat.setText(f"Approved\n{approved:,}")
+        self._ignored_stat.setText(f"Ignored\n{ignored:,}")
+        self._remaining_stat.setText(f"Remaining\n{remaining:,}")
+        for key, count in (("all", len(self._rows)), ("exact", exact), ("fuzzy", fuzzy), ("unmatched", unmatched)):
+            label = key.title() if key != "all" else "All"
+            self._filter_buttons[key].setText(f"{label} {count:,}")
+
+    def _clear_workspace(self, message: str) -> None:
+        self._report = None
+        self._rows = []
+        self._model.set_records([])
+        self._clear_comparison()
+        self._requested_title.setText(message)
+        self._update_counts()
+
+    def _clear_comparison(self) -> None:
+        self._selected_candidate = None
+        self._requested_title.setText("Select an entry")
+        self._requested_meta.clear()
+        self._proposed_title.setText("No candidate selected")
+        self._proposed_meta.clear()
+        self._candidate_list.clear()
+        self._candidate_reasons.set_tags([])
 
     def _go_back(self) -> None:
-        """Navigate back to the Reports page."""
         from minerva.app.page_id import PageId
 
         shell = self.window()
-        if shell is not None and hasattr(shell, "show_page"):
+        if hasattr(shell, "show_page"):
             shell.show_page(PageId.REPORTS)
-        else:
-            log.warning("Cannot navigate back: shell has no show_page")
+
+
+def _fmt_bytes(value: int) -> str:
+    size = float(value)
+    for unit in ("B", "KB", "MB", "GB", "TB"):
+        if size < 1024 or unit == "TB":
+            return f"{int(size)} B" if unit == "B" else f"{size:.1f} {unit}"
+        size /= 1024
+    return f"{size:.1f} TB"
diff --git a/minerva/app/pages/reports.py b/minerva/app/pages/reports.py
index 5bc7bb4..d12d420 100644
--- a/minerva/app/pages/reports.py
+++ b/minerva/app/pages/reports.py
@@ -1,20 +1,14 @@
-"""
-Reports overview — imported DAT files with match results.
-
-States (QStackedWidget):
-    EMPTY   — no reports imported yet
-    LOADING — initial load or import/matching in progress
-    RESULTS — reports list + entry details
-    ERROR   — database or import error
-"""
+"""Reports dashboard: import, match, inspect, rematch, and export fix reports."""
 
 from __future__ import annotations
 
 import enum
 import logging
 import uuid
+from dataclasses import dataclass
+from datetime import datetime, timezone
 from pathlib import Path
-from typing import TYPE_CHECKING, Any
+from typing import Any
 
 from PyQt6 import QtCore, QtGui, QtWidgets
 from PyQt6.QtCore import Qt
@@ -22,23 +16,20 @@ from PyQt6.QtCore import Qt
 from minerva.app.app_state import AppState
 from minerva.app.pages.base import BasePage, _state_label
 from minerva.app.task_runner import TaskRunner
+from minerva.domain.reports import ReportSummary, ReviewEntry
 from minerva.ui.icons import Icons
 from minerva.ui.models.delegates import DisplayDelegate, SizeDelegate
-from minerva.ui.models.proxy import SortFilterProxy
 from minerva.ui.models.record_model import ColumnSpec, RecordListModel
-from minerva.ui.widgets.empty_state import EmptyState
 from minerva.ui.widgets.metric_card import MetricCard, MetricKind
+from minerva.ui.widgets.notification_banner import NotificationBanner
 from minerva.ui.widgets.page_header import PageHeader
-
-if TYPE_CHECKING:
-    from minerva.domain.reports import ReportSummary, ReviewEntry
+from minerva.ui.widgets.section_card import SectionCard
+from minerva_db import DatEntry, MinervaDB, parse_dat_file, parse_rv_fix_csv
+from minerva_state import MinervaState
 
 log = logging.getLogger(__name__)
 
 
-# ── State enum ─────────────────────────────────────────────────────────────────
-
-
 class ReportsPageState(enum.Enum):
     EMPTY = "empty"
     LOADING = "loading"
@@ -46,611 +37,429 @@ class ReportsPageState(enum.Enum):
     ERROR = "error"
 
 
-# ── Column specs ───────────────────────────────────────────────────────────────
-
-_REPORT_COLUMNS: list[ColumnSpec["ReportSummary"]] = [
-    ColumnSpec(header="Name", accessor=lambda r: r.name),
-    ColumnSpec(header="Collection", accessor=lambda r: r.collection or ""),
-    ColumnSpec(header="System", accessor=lambda r: r.system or ""),
-    ColumnSpec(
-        header="Entries",
-        accessor=lambda r: r.requested_count,
-        format_fn=lambda v: f"{v:,}",
-    ),
-    ColumnSpec(
-        header="Matched",
-        accessor=lambda r: r.matched_count,
-        format_fn=lambda v: f"{v:,}",
-    ),
-    ColumnSpec(
-        header="Unmatched",
-        accessor=lambda r: r.unmatched_count,
-        format_fn=lambda v: f"{v:,}",
-    ),
-    ColumnSpec(header="Status", accessor=lambda r: r.status),
-]
-
-_ENTRY_COLUMNS: list[ColumnSpec["ReviewEntry"]] = [
-    ColumnSpec(header="Filename", accessor=lambda e: e.filename),
-    ColumnSpec(header="Size", accessor=lambda e: e.size),
-    ColumnSpec(
-        header="Method",
-        accessor=lambda e: e.automatic_method or "—",
-    ),
-    ColumnSpec(
-        header="Confidence",
-        accessor=lambda e: e.automatic_confidence,
-        format_fn=lambda v: f"{v:.0%}" if isinstance(v, float) else "—",
-    ),
-    ColumnSpec(header="Decision", accessor=lambda e: e.decision),
-]
+@dataclass(frozen=True)
+class ReportEntryView:
+    entry: ReviewEntry
+    best_match: str = ""
+    collection: str = ""
+    system: str = ""
 
-ReportListModel = RecordListModel["ReportSummary"]
-EntryListModel = RecordListModel["ReviewEntry"]
+    @property
+    def category(self) -> str:
+        if self.entry.automatic_method == "exact":
+            return "exact"
+        if self.entry.automatic_method in {"fts5", "trigram", "keyword", "fuzzy"}:
+            return "fuzzy"
+        return "unmatched"
 
 
-# ── Background tasks ───────────────────────────────────────────────────────────
+_REPORT_COLUMNS: list[ColumnSpec[ReportSummary]] = [
+    ColumnSpec("Report", lambda report: report.name),
+    ColumnSpec("Entries", lambda report: report.requested_count, format_fn=lambda value: f"{value:,}"),
+    ColumnSpec("Status", lambda report: report.status.title()),
+]
+_ENTRY_COLUMNS: list[ColumnSpec[ReportEntryView]] = [
+    ColumnSpec("Requested title", lambda row: row.entry.filename),
+    ColumnSpec("Best match", lambda row: row.best_match or "No match found"),
+    ColumnSpec("Confidence", lambda row: row.entry.automatic_confidence, format_fn=lambda value: f"{value:.0%}" if value is not None else "—"),
+    ColumnSpec("Method", lambda row: row.entry.automatic_method or "Unmatched"),
+    ColumnSpec("Size", lambda row: row.entry.size),
+    ColumnSpec("Decision", lambda row: row.entry.decision.title()),
+]
 
 
-def _match_task(
-    generation: int,
-    entries: list[dict],
-    collection: str,
-    system: str,
-) -> tuple[int, dict]:
-    """Run ``match_dat_detailed`` on a background thread.
+class _EntryFilter(QtCore.QSortFilterProxyModel):
+    def __init__(self, parent=None) -> None:
+        super().__init__(parent)
+        self.category = "all"
+        self.text = ""
 
-    Returns (generation, result_dict).
+    def filterAcceptsRow(self, source_row, source_parent):
+        model = self.sourceModel()
+        if model is None:
+            return True
+        row = model._records[source_row]  # noqa: SLF001
+        if self.category != "all" and row.category != self.category:
+            return False
+        if self.text:
+            needle = self.text.casefold()
+            return needle in row.entry.filename.casefold() or needle in row.best_match.casefold()
+        return True
 
-    ``entries`` is a list of dicts with keys ``filename`` and ``size``
-    (JSON-safe serialisation of ``DatEntry`` values).
-    """
-    from minerva_db import MinervaDB, DatEntry
 
+def _match_task(generation: int, entries: list[dict], collection: str, system: str) -> tuple[int, dict]:
     db = MinervaDB()
-    dat_entries = [DatEntry(filename=e["filename"], size=e["size"]) for e in entries]
-    result = db.match_dat_detailed(dat_entries, collection=collection, system=system)
-    return generation, result
-
-
-# ── Page implementation ────────────────────────────────────────────────────────
+    values = [DatEntry(filename=item["filename"], size=item["size"]) for item in entries]
+    return generation, db.match_dat_detailed(values, collection=collection, system=system)
 
 
 class ReportsPage(BasePage):
-    """Reports overview screen.
-
-    Displays imported fix-reports with matching status.  Supports
-    importing DAT/CSV files via dialog or drag-drop, background
-    matching, and navigation to the MatchReviewPage.
-    """
+    """Three-column reports overview matching the Reports mockup."""
 
     def __init__(self, app_state: AppState) -> None:
         super().__init__(app_state)
-
-        # ── Generation counter for stale-result rejection ─────────────────
-        self._gen_counter = 0
         self._pool = QtCore.QThreadPool.globalInstance()
-
-        # ── Model layer ───────────────────────────────────────────────────
-        self._report_model: ReportListModel = ReportListModel(
-            [], _REPORT_COLUMNS, parent=self,
-        )
-        self._report_proxy = SortFilterProxy(self)
-        self._report_proxy.setSourceModel(self._report_model)
-
-        self._entry_model: EntryListModel = EntryListModel(
-            [], _ENTRY_COLUMNS, parent=self,
-        )
-        self._entry_proxy = SortFilterProxy(self)
-        self._entry_proxy.setSourceModel(self._entry_model)
-
-        # ── View: report list ────────────────────────────────────────────
-        self._report_view = QtWidgets.QTableView(self)
-        self._report_view.setModel(self._report_proxy)
-        self._report_view.setSelectionBehavior(
-            QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows,
-        )
-        self._report_view.setSelectionMode(
-            QtWidgets.QAbstractItemView.SelectionMode.SingleSelection,
-        )
+        self._generation = 0
+        self._selected_report_id: str | None = None
+
+        self._report_model = RecordListModel([], _REPORT_COLUMNS, self)
+        self._report_view = QtWidgets.QTableView()
+        self._report_view.setModel(self._report_model)
+        self._report_view.setSelectionBehavior(QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows)
+        self._report_view.setSelectionMode(QtWidgets.QAbstractItemView.SelectionMode.SingleSelection)
         self._report_view.verticalHeader().hide()
-        self._report_view.setSortingEnabled(True)
         self._report_view.setItemDelegate(DisplayDelegate(self._report_view))
-
-        header = self._report_view.horizontalHeader()
-        header.setStretchLastSection(False)
-        header.setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
-        for col in (1, 2):
-            header.resizeSection(col, 110)
-        for col in (3, 4, 5):
-            header.resizeSection(col, 80)
-        header.resizeSection(6, 70)
-
-        self._report_view.selectionModel().selectionChanged.connect(
-            self._on_report_selected,
-        )
-        self._report_view.doubleClicked.connect(self._on_report_double_clicked)
-
-        # ── View: entry details ──────────────────────────────────────────
-        self._entry_view = QtWidgets.QTableView(self)
+        self._report_view.horizontalHeader().setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
+        self._report_view.horizontalHeader().resizeSection(1, 70)
+        self._report_view.horizontalHeader().resizeSection(2, 80)
+        self._report_view.selectionModel().selectionChanged.connect(self._on_report_selected)
+        self._report_view.doubleClicked.connect(lambda _index: self._review_selected())
+
+        self._entry_model = RecordListModel([], _ENTRY_COLUMNS, self)
+        self._entry_proxy = _EntryFilter(self)
+        self._entry_proxy.setSourceModel(self._entry_model)
+        self._entry_view = QtWidgets.QTableView()
         self._entry_view.setModel(self._entry_proxy)
-        self._entry_view.setSelectionBehavior(
-            QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows,
-        )
+        self._entry_view.setSelectionBehavior(QtWidgets.QAbstractItemView.SelectionBehavior.SelectRows)
         self._entry_view.verticalHeader().hide()
-        self._entry_view.setSortingEnabled(True)
+        self._entry_view.setAlternatingRowColors(True)
         self._entry_view.setItemDelegate(DisplayDelegate(self._entry_view))
-        self._entry_view.setItemDelegateForColumn(1, SizeDelegate(self._entry_view))
-
-        e_header = self._entry_view.horizontalHeader()
-        e_header.setStretchLastSection(False)
-        e_header.setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
-        e_header.resizeSection(1, 100)
-        e_header.resizeSection(2, 90)
-        e_header.resizeSection(3, 90)
-        e_header.resizeSection(4, 80)
-
-        # Double-click entry → navigate to MatchReviewPage
-        self._entry_view.doubleClicked.connect(self._on_entry_double_clicked)
-
-        # ── Right panel (entry details) ──────────────────────────────────
-        self._entry_panel = QtWidgets.QWidget()
-        entry_layout = QtWidgets.QVBoxLayout(self._entry_panel)
-        entry_layout.setContentsMargins(0, 0, 0, 0)
-        entry_layout.setSpacing(4)
-
-        self._entry_header = QtWidgets.QLabel("Select a report to view entries")
-        self._entry_header.setObjectName("mutedLabel")
-        entry_layout.addWidget(self._entry_header)
-        entry_layout.addWidget(self._entry_view)
-
-        # ── Splitter ─────────────────────────────────────────────────────
-        self._splitter = QtWidgets.QSplitter(Qt.Orientation.Horizontal, self)
-        self._splitter.addWidget(self._report_view)
-        self._splitter.addWidget(self._entry_panel)
-        self._splitter.setStretchFactor(0, 2)
-        self._splitter.setStretchFactor(1, 3)
-
-        # ── Metric cards ─────────────────────────────────────────────────
-        self._card_reports = MetricCard("Total Reports", "0", MetricKind.NEUTRAL)
-        self._card_entries = MetricCard("Total Entries", "0", MetricKind.NEUTRAL)
-        self._card_matched = MetricCard("Matched", "0", MetricKind.SUCCESS)
-        self._card_unmatched = MetricCard("Unmatched", "0", MetricKind.WARNING)
-
-        metrics_row = QtWidgets.QHBoxLayout()
-        metrics_row.setContentsMargins(0, 0, 0, 0)
-        metrics_row.setSpacing(8)
-        for card in (
-            self._card_reports,
-            self._card_entries,
-            self._card_matched,
-            self._card_unmatched,
-        ):
-            metrics_row.addWidget(card)
-
-        # ── Toolbar ──────────────────────────────────────────────────────
-        self._import_btn = QtWidgets.QPushButton(Icons.upload(), "Import Report")
-        self._import_btn.setObjectName("accentButton")
-        self._import_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._import_btn.clicked.connect(self._on_import)
-
-        self._delete_btn = QtWidgets.QPushButton(Icons.close(), "Delete Report")
-        self._delete_btn.setObjectName("subtleButton")
-        self._delete_btn.setCursor(Qt.CursorShape.PointingHandCursor)
-        self._delete_btn.clicked.connect(self._on_delete)
-        self._delete_btn.setEnabled(False)
-
-        toolbar = QtWidgets.QHBoxLayout()
-        toolbar.setContentsMargins(0, 0, 0, 0)
-        toolbar.setSpacing(8)
-        toolbar.addWidget(self._import_btn)
-        toolbar.addWidget(self._delete_btn)
-        toolbar.addStretch(1)
-
-        # ── Results widget ───────────────────────────────────────────────
-        results_widget = QtWidgets.QWidget()
-        results_layout = QtWidgets.QVBoxLayout(results_widget)
-        results_layout.setContentsMargins(0, 0, 0, 0)
-        results_layout.setSpacing(8)
-
-        results_layout.addWidget(PageHeader("Fix Reports"))
-        results_layout.addLayout(metrics_row)
-        results_layout.addLayout(toolbar)
-        results_layout.addWidget(self._splitter, stretch=1)
-
-        # ── State stack ──────────────────────────────────────────────────
-        self._stack = QtWidgets.QStackedWidget(self)
-
-        self._empty_state = EmptyState(
-            parent=self._stack,
-            icon=Icons.upload(),
-            title="No fix reports yet",
-            description="Import a DAT or CSV file to get started.",
-            action_text="Import Report",
+        self._entry_view.setItemDelegateForColumn(4, SizeDelegate(self._entry_view))
+        self._entry_view.horizontalHeader().setSectionResizeMode(0, QtWidgets.QHeaderView.ResizeMode.Stretch)
+        self._entry_view.horizontalHeader().setSectionResizeMode(1, QtWidgets.QHeaderView.ResizeMode.Stretch)
+        for column, width in {2: 90, 3: 90, 4: 90, 5: 85}.items():
+            self._entry_view.horizontalHeader().resizeSection(column, width)
+        self._entry_view.doubleClicked.connect(lambda _index: self._review_selected())
+
+        self._header = PageHeader(
+            "Reports",
+            "Import FixDAT and CSV reports, review match quality, and prepare downloads.",
         )
-        self._empty_state.action_clicked.connect(self._on_import)
-        self._stack.addWidget(self._empty_state)  # index 0 — EMPTY
-
-        self._label_loading = _state_label("Loading\u2026")
-        self._stack.addWidget(self._label_loading)  # index 1 — LOADING
-
-        self._label_error = _state_label("An error occurred")
-        self._stack.addWidget(self._label_error)  # index 2 — ERROR
-
-        self._stack.addWidget(results_widget)  # index 3 — RESULTS
-
-        # ── Root layout ──────────────────────────────────────────────────
-        layout = QtWidgets.QVBoxLayout(self)
+        self._import_btn = self._header.add_action("Add report", Icons.add(), primary=True)
+        self._rematch_btn = self._header.add_action("Match again", Icons.refresh())
+        self._delete_btn = self._header.add_action("Remove", Icons.trash(), danger=True)
+        self._import_btn.clicked.connect(self._on_import)
+        self._rematch_btn.clicked.connect(self._rematch_selected)
+        self._delete_btn.clicked.connect(self._delete_selected)
+
+        self._card_requested = MetricCard("Total requested", "0", icon=Icons.file())
+        self._card_matched = MetricCard("Matched", "0", MetricKind.SUCCESS, icon=Icons.status_success())
+        self._card_unmatched = MetricCard("Unmatched", "0", MetricKind.WARNING, icon=Icons.status_warning())
+        self._card_coverage = MetricCard("Coverage", "0%", MetricKind.INFO, icon=Icons.chart())
+        metrics = QtWidgets.QHBoxLayout()
+        for card in (self._card_requested, self._card_matched, self._card_unmatched, self._card_coverage):
+            metrics.addWidget(card)
+
+        reports_card = SectionCard("Imported reports", Icons.reports())
+        reports_card.content_layout().addWidget(self._report_view)
+        reports_card.setMinimumWidth(245)
+        reports_card.setMaximumWidth(340)
+
+        search = QtWidgets.QLineEdit()
+        search.setPlaceholderText("Search entries…")
+        search.setClearButtonEnabled(True)
+        search.addAction(Icons.search(), QtWidgets.QLineEdit.ActionPosition.LeadingPosition)
+        search.textChanged.connect(self._set_entry_text_filter)
+        filter_row = QtWidgets.QHBoxLayout()
+        filter_row.addWidget(search, 1)
+        self._filter_buttons: dict[str, QtWidgets.QPushButton] = {}
+        group = QtWidgets.QButtonGroup(self)
+        group.setExclusive(True)
+        for key, label in (("all", "All"), ("exact", "Matched"), ("unmatched", "Unmatched"), ("fuzzy", "Fuzzy")):
+            button = QtWidgets.QPushButton(label)
+            button.setCheckable(True)
+            button.setObjectName("subtleButton")
+            button.clicked.connect(lambda _checked, category=key: self._set_entry_category(category))
+            group.addButton(button)
+            filter_row.addWidget(button)
+            self._filter_buttons[key] = button
+        self._filter_buttons["all"].setChecked(True)
+
+        entries_panel = QtWidgets.QWidget()
+        entries_layout = QtWidgets.QVBoxLayout(entries_panel)
+        entries_layout.setContentsMargins(0, 0, 0, 0)
+        entries_layout.addLayout(filter_row)
+        entries_layout.addWidget(self._entry_view, 1)
+
+        self._detail_name = QtWidgets.QLabel("Select a report")
+        self._detail_name.setObjectName("sectionTitle")
+        self._detail_name.setWordWrap(True)
+        self._detail_meta = QtWidgets.QLabel("")
+        self._detail_meta.setObjectName("mutedLabel")
+        self._detail_meta.setWordWrap(True)
+        self._review_btn = QtWidgets.QPushButton(Icons.search(), "Review matches")
+        self._review_btn.setObjectName("primaryButton")
+        self._review_btn.clicked.connect(self._review_selected)
+        self._export_btn = QtWidgets.QPushButton(Icons.file(), "Export reviewed DAT")
+        self._export_btn.clicked.connect(self._export_reviewed)
+        self._open_source_btn = QtWidgets.QPushButton(Icons.folder_open(), "Open source folder")
+        self._open_source_btn.clicked.connect(self._open_source_folder)
+        detail_card = SectionCard("Report details", Icons.file())
+        detail_card.content_layout().addWidget(self._detail_name)
+        detail_card.content_layout().addWidget(self._detail_meta)
+        detail_card.content_layout().addStretch(1)
+        detail_card.content_layout().addWidget(self._review_btn)
+        detail_card.content_layout().addWidget(self._export_btn)
+        detail_card.content_layout().addWidget(self._open_source_btn)
+        detail_card.setMinimumWidth(240)
+        detail_card.setMaximumWidth(310)
+
+        splitter = QtWidgets.QSplitter(Qt.Orientation.Horizontal)
+        splitter.addWidget(reports_card)
+        splitter.addWidget(entries_panel)
+        splitter.addWidget(detail_card)
+        splitter.setSizes([275, 760, 270])
+        splitter.setStretchFactor(1, 1)
+        self._splitter = splitter
+
+        results = QtWidgets.QWidget()
+        layout = QtWidgets.QVBoxLayout(results)
         layout.setContentsMargins(0, 0, 0, 0)
-        layout.addWidget(self._stack)
-
-        # Initial state
-        self._set_state(ReportsPageState.EMPTY)
-        self.refresh()
-
-    # ── State management ─────────────────────────────────────────────────
+        layout.setSpacing(10)
+        layout.addWidget(self._header)
+        layout.addLayout(metrics)
+        layout.addWidget(splitter, 1)
+
+        self._stack = QtWidgets.QStackedWidget()
+        self._stack.addWidget(_state_label("Drop a FixDAT or CSV here, or choose Add report"))
+        self._stack.addWidget(_state_label("Importing and matching report…"))
+        self._stack.addWidget(results)
+        self._error = _state_label("Report operation failed")
+        self._stack.addWidget(self._error)
+        root = QtWidgets.QVBoxLayout(self)
+        root.setContentsMargins(0, 0, 0, 0)
+        root.addWidget(self._stack)
+        QtCore.QTimer.singleShot(0, self.refresh)
 
     def _set_state(self, state: ReportsPageState) -> None:
-        """Switch to the given visual state."""
-        index_map = {
+        self._stack.setCurrentIndex({
             ReportsPageState.EMPTY: 0,
             ReportsPageState.LOADING: 1,
-            ReportsPageState.ERROR: 2,
-            ReportsPageState.RESULTS: 3,
-        }
-        self._stack.setCurrentIndex(index_map[state])
-
-    # ── Lifecycle ────────────────────────────────────────────────────────
+            ReportsPageState.RESULTS: 2,
+            ReportsPageState.ERROR: 3,
+        }[state])
 
     def activate(self) -> None:
-        """Refresh data when the page becomes visible."""
-        super().activate()
         self.refresh()
 
     def refresh(self) -> None:
-        """Reload the report list from ``MinervaState``."""
         try:
-            from minerva_state import MinervaState
-
-            state = MinervaState()
-            reports = state.list_reports()
+            reports = MinervaState().list_reports()
         except Exception as exc:
-            log.warning("Failed to load reports: %s", exc)
+            self._error.setText(str(exc))
             self._set_state(ReportsPageState.ERROR)
             return
-
         self._report_model.set_records(reports)
-        self._update_metrics(reports)
-        self._entry_model.set_records([])
-        self._entry_header.setText("Select a report to view entries")
-        self._delete_btn.setEnabled(False)
-
+        self._set_state(ReportsPageState.RESULTS if reports else ReportsPageState.EMPTY)
         if reports:
-            self._set_state(ReportsPageState.RESULTS)
+            target = next((index for index, report in enumerate(reports) if report.id == self._selected_report_id), 0)
+            self._report_view.selectRow(target)
         else:
-            self._set_state(ReportsPageState.EMPTY)
-
-    # ── Metric helpers ───────────────────────────────────────────────────
-
-    def _update_metrics(self, reports: list[ReportSummary] | None = None) -> None:
-        """Recompute and update the four metric cards."""
-        if reports is None:
-            reports = self._report_model._records  # noqa: SLF001
-        total_reports = len(reports)
-        total_entries = sum(r.requested_count for r in reports)
-        total_matched = sum(r.matched_count for r in reports)
-        total_unmatched = sum(r.unmatched_count for r in reports)
-
-        self._card_reports.set_value(f"{total_reports:,}")
-        self._card_entries.set_value(f"{total_entries:,}")
-        self._card_matched.set_value(f"{total_matched:,}")
-        self._card_unmatched.set_value(f"{total_unmatched:,}")
-
-    # ── Import flow ──────────────────────────────────────────────────────
+            self._show_report(None)
 
     def _on_import(self) -> None:
-        """Open a file dialog to import a DAT or CSV file."""
-        path_str, _filter = QtWidgets.QFileDialog.getOpenFileName(
-            self,
-            "Import Fix Report",
-            "",
-            "DAT files (*.dat);;CSV files (*.csv);;All files (*)",
-        )
-        if not path_str:
-            return
-        self._import_file(Path(path_str))
-
-    def _import_file(self, path: Path) -> None:
-        """Parse and import a single DAT/CSV file.
-
-        Steps:
-            1. Parse the file via ``parse_dat_file`` or ``parse_rv_fix_csv``.
-            2. Create a ``ReportSummary`` and persist it.
-            3. Run background matching via ``match_dat_detailed``.
-            4. On completion, save entries and update the UI.
-        """
-        self._set_state(ReportsPageState.LOADING)
+        paths, _ = QtWidgets.QFileDialog.getOpenFileNames(self, "Import fix reports", "", "Fix reports (*.dat *.csv);;All files (*)")
+        for path in paths:
+            self._import_file(Path(path))
 
+    def _import_file(self, path: Path, report_id: str | None = None) -> None:
         try:
-            # Parse
-            if path.suffix.lower() == ".csv":
-                from minerva_db import parse_rv_fix_csv
-
-                dat_info = parse_rv_fix_csv(path)
-            else:
-                from minerva_db import parse_dat_file
-
-                dat_info = parse_dat_file(path)
-
-            if not dat_info.entries:
-                self._set_state(ReportsPageState.EMPTY)
-                log.warning("No entries found in %s", path)
+            info = parse_rv_fix_csv(path) if path.suffix.lower() == ".csv" else parse_dat_file(path)
+            if not info.entries:
+                NotificationBanner.show_warning(self, "Empty report", str(path))
                 return
-
-            # Create report
-            from minerva_state import MinervaState
-
-            from minerva.domain.reports import ReportSummary
-
-            report_id = uuid.uuid4().hex
-            from datetime import datetime, timezone
-
-            now_iso = datetime.now(timezone.utc).isoformat()
-
+            state = MinervaState()
+            existing = state.get_report_by_path(path)
+            if existing and report_id is None:
+                reply = QtWidgets.QMessageBox.question(self, "Reimport report", f'"{existing.name}" is already imported. Replace its matching results?')
+                if reply != QtWidgets.QMessageBox.StandardButton.Yes:
+                    return
+                report_id = existing.id
+            report_id = report_id or uuid.uuid4().hex
             report = ReportSummary(
                 id=report_id,
                 path=str(path),
-                name=dat_info.name or path.stem,
-                collection=dat_info.collection,
-                system=dat_info.system,
-                imported_at=now_iso,
-                requested_count=len(dat_info.entries),
-                matched_count=0,
-                fuzzy_count=0,
-                unmatched_count=0,
-                status="draft",
+                name=info.name or path.stem,
+                collection=info.collection,
+                system=info.system,
+                imported_at=datetime.now(timezone.utc).isoformat(),
+                requested_count=len(info.entries),
+                status="matching",
             )
-
-            state = MinervaState()
-            state.save_report(report)
-
-            # Serialise entries for the background task (DatEntry is not
-            # pickleable via QRunnable directly; dict is safe).
-            entry_dicts = [
-                {"filename": e.filename, "size": e.size}
-                for e in dat_info.entries
-            ]
-
-            # Launch background matching
-            self._gen_counter += 1
-            generation = self._gen_counter
-
+            if existing:
+                state.update_report(
+                    report_id,
+                    name=report.name,
+                    collection=report.collection,
+                    system=report.system,
+                    requested_count=report.requested_count,
+                    status="matching",
+                )
+            else:
+                state.save_report(report)
+            self._selected_report_id = report_id
+            self._set_state(ReportsPageState.LOADING)
+            self._generation += 1
+            generation = self._generation
             task = TaskRunner(
                 _match_task,
                 generation,
-                entry_dicts,
-                dat_info.collection or "",
-                dat_info.system or "",
-            )
-            task.signals.result.connect(
-                lambda payload, rid=report_id, entries=dat_info.entries: self._on_match_result(
-                    generation, rid, payload, entries,
-                ),
-            )
-            task.signals.error.connect(
-                lambda details, rid=report_id: self._on_match_error(
-                    generation, rid, details,
-                ),
+                [{"filename": entry.filename, "size": entry.size} for entry in info.entries],
+                info.collection or "",
+                info.system or "",
             )
+            task.signals.result.connect(lambda payload, rid=report_id: self._on_match_result(generation, rid, payload))
+            task.signals.error.connect(lambda details, rid=report_id: self._on_match_error(generation, rid, details))
             self._pool.start(task)
-
         except Exception as exc:
-            log.warning("Import failed: %s", exc, exc_info=True)
-            self._label_error.setText(f"Import failed: {exc}")
+            self._error.setText(str(exc))
             self._set_state(ReportsPageState.ERROR)
 
-    # ── Match result handling ───────────────────────────────────────────
-
-    def _on_match_result(
-        self,
-        generation: int,
-        report_id: str,
-        payload: object,
-        dat_entries: object,
-    ) -> None:
-        """Process the result from the background matching task."""
-        if generation != self._gen_counter:
-            return  # stale
-
-        try:
-            _gen, result = payload  # type: ignore[misc]
-        except (TypeError, ValueError) as exc:
-            log.warning("Invalid match result payload: %s", exc)
-            self.refresh()
+    def _on_match_result(self, generation: int, report_id: str, payload: object) -> None:
+        if generation != self._generation:
             return
-
-        try:
-            from minerva_state import MinervaState
-
-            from minerva.domain.reports import ReviewEntry
-
-            state = MinervaState()
-
-            # Build ReviewEntry objects from match results
-            results_list = result["results"]
-            entries: list[ReviewEntry] = []
-            for i, entry_result in enumerate(results_list):
-                entries.append(
-                    ReviewEntry(
-                        id=uuid.uuid4().hex,
-                        report_id=report_id,
-                        ordinal=i,
-                        filename=entry_result["entry"].filename,
-                        size=entry_result["entry"].size,
-                        automatic_file_id=entry_result["automatic_file_id"],
-                        automatic_method=entry_result["automatic_method"],
-                        automatic_confidence=entry_result["automatic_confidence"],
-                        decision="pending",
-                        selected_file_id=None,
-                    ),
-                )
-
-            state.save_entries(entries)
-
-            # Update report counts
-            summary = result["summary"]
-            state.update_report(
-                report_id,
-                matched_count=summary["exact"],
-                fuzzy_count=summary["fuzzy"],
-                unmatched_count=summary["unmatched"],
-                status="draft",
-            )
-
-            log.debug(
-                "Match complete for %s: %s",
-                report_id,
-                summary,
+        _generation, result = payload  # type: ignore[misc]
+        entries = [
+            ReviewEntry(
+                id=uuid.uuid4().hex,
+                report_id=report_id,
+                ordinal=index,
+                filename=item["entry"].filename,
+                size=item["entry"].size,
+                automatic_file_id=item["automatic_file_id"],
+                automatic_method=item["automatic_method"],
+                automatic_confidence=item["automatic_confidence"],
             )
-
-        except Exception as exc:
-            log.warning("Failed to persist match results: %s", exc, exc_info=True)
-
-        self.refresh()
-
-    def _on_match_error(
-        self,
-        generation: int,
-        report_id: str,
-        details: str,
-    ) -> None:
-        """Handle a matching error — show the report anyway (no entries)."""
-        if generation != self._gen_counter:
-            return  # stale
-        log.warning("Match failed for %s: %s", report_id, details)
+            for index, item in enumerate(result["results"])
+        ]
+        state = MinervaState()
+        state.replace_entries(report_id, entries)
+        summary = result["summary"]
+        state.update_report(
+            report_id,
+            matched_count=summary["exact"],
+            fuzzy_count=summary["fuzzy"],
+            unmatched_count=summary["unmatched"],
+            status="ready",
+        )
+        state.add_event("match", f"Matched report {report_id}: {summary['exact']} exact, {summary['fuzzy']} fuzzy, {summary['unmatched']} unmatched")
         self.refresh()
 
-    # ── Report selection ────────────────────────────────────────────────
+    def _on_match_error(self, generation: int, report_id: str, details: str) -> None:
+        if generation != self._generation:
+            return
+        MinervaState().update_report(report_id, status="failed")
+        self._error.setText(details)
+        self._set_state(ReportsPageState.ERROR)
 
     def _on_report_selected(self) -> None:
-        """Show entry details for the selected report."""
-        indexes = self._report_view.selectionModel().selectedRows()
-        if not indexes:
+        rows = self._report_view.selectionModel().selectedRows()
+        if not rows:
+            self._show_report(None)
+            return
+        report = self._report_model._records[rows[0].row()]
+        self._selected_report_id = report.id
+        self._show_report(report)
+
+    def _show_report(self, report: ReportSummary | None) -> None:
+        enabled = report is not None
+        for button in (self._rematch_btn, self._delete_btn, self._review_btn, self._export_btn, self._open_source_btn):
+            button.setEnabled(enabled)
+        if report is None:
             self._entry_model.set_records([])
-            self._entry_header.setText("Select a report to view entries")
-            self._delete_btn.setEnabled(False)
+            self._detail_name.setText("Select a report")
+            self._detail_meta.clear()
+            for card in (self._card_requested, self._card_matched, self._card_unmatched):
+                card.set_value("0")
+            self._card_coverage.set_value("0%")
             return
+        entries = MinervaState().get_entries(report.id)
+        ids = [entry.automatic_file_id for entry in entries if entry.automatic_file_id]
+        item_map = {item.id: item for item in MinervaDB().get_files_by_ids(ids)} if ids else {}
+        rows = [
+            ReportEntryView(
+                entry=entry,
+                best_match=item_map[entry.automatic_file_id].stem if entry.automatic_file_id in item_map else "",
+                collection=item_map[entry.automatic_file_id].collection if entry.automatic_file_id in item_map else "",
+                system=item_map[entry.automatic_file_id].system if entry.automatic_file_id in item_map else "",
+            )
+            for entry in entries
+        ]
+        self._entry_model.set_records(rows)
+        matched = report.matched_count + report.fuzzy_count
+        coverage = matched / report.requested_count if report.requested_count else 0.0
+        self._card_requested.set_value(f"{report.requested_count:,}")
+        self._card_matched.set_value(f"{matched:,}")
+        self._card_matched.set_subtitle(f"{report.matched_count:,} exact · {report.fuzzy_count:,} fuzzy")
+        self._card_unmatched.set_value(f"{report.unmatched_count:,}")
+        self._card_coverage.set_value(f"{coverage:.1%}")
+        self._card_coverage.set_subtitle(f"{matched:,} / {report.requested_count:,}")
+        kind = "RomVault CSV" if Path(report.path).suffix.lower() == ".csv" else "FixDAT"
+        self._detail_name.setText(report.name)
+        self._detail_meta.setText(
+            f"Source path\n{report.path}\n\nReport type\n{kind}\n\nCollection\n{report.collection or 'Unknown'}\n\n"
+            f"System\n{report.system or 'Unknown'}\n\nRequested entries\n{report.requested_count:,}\n\n"
+            f"Matched entries\n{matched:,}\n\nUnmatched entries\n{report.unmatched_count:,}\n\nStatus\n{report.status.title()}"
+        )
 
-        proxy_index = indexes[0]
-        source_index = self._report_proxy.mapToSource(proxy_index)
-        row = source_index.row()
-        if row < 0 or row >= len(self._report_model._records):  # noqa: SLF001
-            return
+    def _set_entry_category(self, category: str) -> None:
+        self._entry_proxy.category = category
+        self._entry_proxy.invalidateFilter()
 
-        report = self._report_model._records[row]  # noqa: SLF001
-        self._delete_btn.setEnabled(True)
+    def _set_entry_text_filter(self, text: str) -> None:
+        self._entry_proxy.text = text.strip()
+        self._entry_proxy.invalidateFilter()
 
-        try:
-            from minerva_state import MinervaState
+    def _selected_report(self) -> ReportSummary | None:
+        return next((report for report in self._report_model._records if report.id == self._selected_report_id), None)
 
-            state = MinervaState()
-            entries = state.get_entries(report.id)
-            self._entry_model.set_records(entries)
-            self._entry_header.setText(
-                f"Entries for {report.name}  ({len(entries)} total)",
-            )
-        except Exception as exc:
-            log.warning("Failed to load entries: %s", exc)
-            self._entry_model.set_records([])
-            self._entry_header.setText("Failed to load entries")
+    def _rematch_selected(self) -> None:
+        report = self._selected_report()
+        if report is not None:
+            self._import_file(Path(report.path), report.id)
 
-    def _on_report_double_clicked(self, index: QtCore.QModelIndex) -> None:
-        """Navigate to MatchReviewPage for the selected report."""
-        source_index = self._report_proxy.mapToSource(index)
-        row = source_index.row()
-        if row < 0 or row >= len(self._report_model._records):  # noqa: SLF001
-            return
-        report = self._report_model._records[row]  # noqa: SLF001
-        self._navigate_to_match_review(report.id)
-
-    def _on_entry_double_clicked(self, index: QtCore.QModelIndex) -> None:
-        """Navigate to MatchReviewPage scoped to the entry's report."""
-        # Find the currently selected report
-        sel_indexes = self._report_view.selectionModel().selectedRows()
-        if not sel_indexes:
-            return
-        src = self._report_proxy.mapToSource(sel_indexes[0])
-        row = src.row()
-        if row < 0 or row >= len(self._report_model._records):  # noqa: SLF001
-            return
-        report = self._report_model._records[row]  # noqa: SLF001
-        self._navigate_to_match_review(report.id)
-
-    def _navigate_to_match_review(self, report_id: str) -> None:
-        """Navigate to the Match Review page for the given report."""
-        shell = self.window()
-        if shell is not None and hasattr(shell, "show_match_review"):
-            shell.show_match_review(report_id)
-        else:
-            log.warning("Cannot navigate: shell has no show_match_review")
-
-    # ── Delete ──────────────────────────────────────────────────────────
+    def _review_selected(self) -> None:
+        if self._selected_report_id:
+            shell = self.window()
+            if hasattr(shell, "show_match_review"):
+                shell.show_match_review(self._selected_report_id)
 
-    def _on_delete(self) -> None:
-        """Delete the selected report after confirmation."""
-        indexes = self._report_view.selectionModel().selectedRows()
-        if not indexes:
+    def _export_reviewed(self) -> None:
+        report = self._selected_report()
+        if report is None:
             return
-
-        proxy_index = indexes[0]
-        source_index = self._report_proxy.mapToSource(proxy_index)
-        row = source_index.row()
-        if row < 0 or row >= len(self._report_model._records):  # noqa: SLF001
+        entries = MinervaState().get_entries(report.id)
+        ids = [entry.selected_file_id or entry.automatic_file_id for entry in entries if entry.decision in {"accept", "fuzzy"} and (entry.selected_file_id or entry.automatic_file_id)]
+        items = MinervaDB().get_files_by_ids(ids)
+        if not items:
+            NotificationBanner.show_warning(self, "Nothing approved", "Review and approve matches first")
             return
-
-        report = self._report_model._records[row]  # noqa: SLF001
-
-        reply = QtWidgets.QMessageBox.question(
-            self,
-            "Delete Report",
-            f'Delete "{report.name}" and all its entries?\n\n'
-            "This action cannot be undone.",
-            QtWidgets.QMessageBox.StandardButton.Yes
-            | QtWidgets.QMessageBox.StandardButton.No,
-            QtWidgets.QMessageBox.StandardButton.No,
-        )
-        if reply != QtWidgets.QMessageBox.StandardButton.Yes:
+        path, _ = QtWidgets.QFileDialog.getSaveFileName(self, "Export reviewed DAT", f"{report.name}-reviewed.dat", "DAT files (*.dat)")
+        if not path:
             return
-
-        try:
-            from minerva_state import MinervaState
-
-            state = MinervaState()
-            state.delete_report(report.id)
-        except Exception as exc:
-            log.warning("Failed to delete report: %s", exc)
-            self._label_error.setText(f"Failed to delete report: {exc}")
-            self._set_state(ReportsPageState.ERROR)
+        rows = [{"stem": item.stem, "basename": item.basename, "size": item.size} for item in items]
+        Path(path).write_text(MinervaDB().build_synthetic_dat(rows, f"{report.name} reviewed", report.system, report.collection), encoding="utf-8")
+        NotificationBanner.show_success(self, "DAT exported", path)
+
+    def _open_source_folder(self) -> None:
+        report = self._selected_report()
+        if report:
+            QtGui.QDesktopServices.openUrl(QtCore.QUrl.fromLocalFile(str(Path(report.path).parent)))
+
+    def _delete_selected(self) -> None:
+        report = self._selected_report()
+        if report is None:
             return
-
+        if QtWidgets.QMessageBox.question(self, "Remove report", f'Remove "{report.name}" and its review decisions?') != QtWidgets.QMessageBox.StandardButton.Yes:
+            return
+        MinervaState().delete_report(report.id)
+        self._selected_report_id = None
         self.refresh()
 
-    # ── Drag-drop ───────────────────────────────────────────────────────
-
     def handle_drop(self, event: QtGui.QDropEvent) -> None:
-        """Accept dropped DAT/CSV files."""
-        urls = event.mimeData().urls()
-        if not urls:
-            return
-
-        imported = 0
-        for url in urls:
-            if not url.isLocalFile():
-                continue
-            path = Path(url.toLocalFile())
-            if path.suffix.lower() in (".dat", ".csv"):
-                self._import_file(path)
-                imported += 1
-
-        if imported:
+        paths = [Path(url.toLocalFile()) for url in event.mimeData().urls() if url.isLocalFile()]
+        valid = [path for path in paths if path.suffix.lower() in {".dat", ".csv"}]
+        for path in valid:
+            self._import_file(path)
+        if valid:
             event.acceptProposedAction()
diff --git a/minerva/app/pages/settings.py b/minerva/app/pages/settings.py
index 9369374..fa2d74d 100644
--- a/minerva/app/pages/settings.py
+++ b/minerva/app/pages/settings.py
@@ -1,6 +1,4 @@
-"""
-Settings/preferences page with SettingsDraft pattern and QSettings persistence.
-"""
+"""Category-based Settings dashboard matching the application mockup."""
 
 from __future__ import annotations
 
@@ -8,32 +6,35 @@ import logging
 from pathlib import Path
 
 from PyQt6 import QtCore, QtWidgets
+from superqt import QToggleSwitch
 
 from minerva.app.app_state import AppState
 from minerva.app.pages.base import BasePage
+from minerva.app.task_runner import TaskRunner
 from minerva.domain.settings import AccentName, SettingsDraft, ThemeName
 from minerva.ui.density import DENSITY_SETTINGS_KEY, Density
 from minerva.ui.icons import Icons
 from minerva.ui.widgets.notification_banner import NotificationBanner
+from minerva.ui.widgets.page_header import PageHeader
+from minerva.ui.widgets.path_picker import PathPicker
+from minerva.ui.widgets.section_card import SectionCard
+from minerva.ui.widgets.status_badge import BadgeKind, StatusBadge
 
 log = logging.getLogger(__name__)
 
 
-class SettingsPage(BasePage):
-    """Settings/preferences page with SettingsDraft pattern and QSettings persistence.
+def _connection_task(url: str, username: str, password: str) -> str:
+    from minerva_qbit import QBittorrentClient
+
+    client = QBittorrentClient(url, username, password)
+    client.login()
+    return client.test_connection()
 
-    Draft pattern:
-        1. On construction, load QSettings into a ``SettingsDraft``.
-        2. Form fields bind to the draft values.
-        3. "Save" validates the draft, then persists to QSettings.
-        4. "Cancel" discards form edits and reloads from saved state.
 
-    Parameters
-    ----------
-    settings_org, settings_app
-        QSettings organisation and application names.  Exposed for test
-        isolation — production callers should never pass these.
-    """
+class SettingsPage(BasePage):
+    """Persistent settings editor with dirty-state protection and live preview."""
+
+    CATEGORIES = ("General", "Downloads", "qBittorrent", "Library", "Appearance", "Advanced")
 
     def __init__(
         self,
@@ -43,368 +44,449 @@ class SettingsPage(BasePage):
     ) -> None:
         super().__init__(app_state)
         self._settings = QtCore.QSettings(settings_org, settings_app)
+        self._pool = QtCore.QThreadPool.globalInstance()
         self._draft = SettingsDraft()
+        self._dirty = False
+        self._loading = False
         self._load_draft()
 
-        # ── Form fields ──────────────────────────────────────────────────
-        form = QtWidgets.QFormLayout()
-        form.setFieldGrowthPolicy(
-            QtWidgets.QFormLayout.FieldGrowthPolicy.AllNonFixedFieldsGrow,
-        )
-        form.setSpacing(8)
-
-        # ── Output directory ─────────────────────────────────────────────
-        output_row = QtWidgets.QHBoxLayout()
-        output_row.setContentsMargins(0, 0, 0, 0)
-        output_row.setSpacing(4)
-        self._output_edit = QtWidgets.QLineEdit(self)
-        self._output_edit.setPlaceholderText("Path to download output folder")
-        output_row.addWidget(self._output_edit, 1)
-        browse_btn = QtWidgets.QPushButton("Browse\u2026", self)
-        browse_btn.clicked.connect(self._on_browse_output)
-        output_row.addWidget(browse_btn)
-        form.addRow("Output directory", output_row)
-
-        # ── qBittorrent section ──────────────────────────────────────────
-        qbit_label = QtWidgets.QLabel("qBittorrent", self)
-        qbit_label.setObjectName("sectionTitle")
-        form.addRow(qbit_label)
-
-        self._qbit_url_edit = QtWidgets.QLineEdit(self)
-        self._qbit_url_edit.setPlaceholderText("http://localhost:8080")
-        form.addRow("Web UI URL", self._qbit_url_edit)
-
-        self._qbit_user_edit = QtWidgets.QLineEdit(self)
-        self._qbit_user_edit.setPlaceholderText("admin")
-        form.addRow("Username", self._qbit_user_edit)
-
-        self._qbit_pass_edit = QtWidgets.QLineEdit(self)
-        self._qbit_pass_edit.setEchoMode(QtWidgets.QLineEdit.EchoMode.Password)
-        self._qbit_pass_edit.setPlaceholderText("Password")
-        form.addRow("Password", self._qbit_pass_edit)
-
-        # Connection test button
-        test_conn_btn = QtWidgets.QPushButton("Test Connection", self)
-        test_conn_btn.clicked.connect(self._on_test_connection)
-        form.addRow("", test_conn_btn)
-
-        # ── Behaviour section ────────────────────────────────────────────
-        beh_label = QtWidgets.QLabel("Behaviour", self)
-        beh_label.setObjectName("sectionTitle")
-        form.addRow(beh_label)
-
-        self._max_concurrent_spin = QtWidgets.QSpinBox(self)
-        self._max_concurrent_spin.setRange(1, 10)
-        self._max_concurrent_spin.setValue(self._draft.max_concurrent)
-        form.addRow("Max concurrent downloads", self._max_concurrent_spin)
-
-        self._timeout_spin = QtWidgets.QSpinBox(self)
-        self._timeout_spin.setRange(5, 300)
-        self._timeout_spin.setSuffix(" s")
-        self._timeout_spin.setValue(self._draft.timeout_seconds)
-        form.addRow("Timeout", self._timeout_spin)
-
-        self._keep_seeding_cb = QtWidgets.QCheckBox(
-            "Keep seeding after download", self,
-        )
-        self._keep_seeding_cb.setChecked(self._draft.keep_seeding)
-        form.addRow("", self._keep_seeding_cb)
-
-        self._hardlinks_cb = QtWidgets.QCheckBox(
-            "Create hardlinks (instead of copy)", self,
+        self._header = PageHeader(
+            "Settings",
+            "Configure downloads, indexing, qBittorrent, and application behavior.",
         )
-        self._hardlinks_cb.setChecked(self._draft.create_hardlinks)
-        form.addRow("", self._hardlinks_cb)
 
-        self._preserve_partial_cb = QtWidgets.QCheckBox(
-            "Preserve partial downloads on cancel", self,
-        )
-        self._preserve_partial_cb.setChecked(self._draft.preserve_partial)
-        form.addRow("", self._preserve_partial_cb)
-
-        # ── Appearance section ───────────────────────────────────────────
-        app_label = QtWidgets.QLabel("Appearance", self)
-        app_label.setObjectName("sectionTitle")
-        form.addRow(app_label)
-
-        # Density selector
-        self._density_combo = QtWidgets.QComboBox(self)
-        for d in Density:
-            self._density_combo.addItem(d.value.title(), d.value)
-        saved_density = self._settings.value(
-            DENSITY_SETTINGS_KEY, Density.COMFORTABLE.value, str,
-        )
-        idx = self._density_combo.findData(saved_density)
-        self._density_combo.setCurrentIndex(max(idx, 0))
-        form.addRow("Density", self._density_combo)
-
-        # Theme selector
-        self._theme_combo = QtWidgets.QComboBox(self)
-        self._theme_combo.addItem("Dark", ThemeName.DARK)
-        form.addRow("Theme", self._theme_combo)
-
-        # Accent selector
-        self._accent_combo = QtWidgets.QComboBox(self)
-        self._accent_combo.addItem("Blue", AccentName.BLUE)
-        form.addRow("Accent", self._accent_combo)
-
-        # ── Migration section ────────────────────────────────────────────
-        migrate_label = QtWidgets.QLabel("Migration", self)
-        migrate_label.setObjectName("sectionTitle")
-        form.addRow(migrate_label)
-
-        migrate_btn = QtWidgets.QPushButton("Migrate Legacy Settings", self)
-        migrate_btn.clicked.connect(self._on_migrate_legacy)
-        form.addRow("", migrate_btn)
-
-        # ── Buttons ──────────────────────────────────────────────────────
-        save_btn = QtWidgets.QPushButton("Save settings", self)
-        save_btn.setObjectName("primaryButton")
-        save_btn.clicked.connect(self._on_save)
-
-        cancel_btn = QtWidgets.QPushButton("Cancel", self)
-        cancel_btn.setObjectName("subtleButton")
-        cancel_btn.clicked.connect(self._on_cancel)
-
-        button_row = QtWidgets.QHBoxLayout()
-        button_row.setContentsMargins(0, 0, 0, 0)
-        button_row.setSpacing(8)
-        button_row.addStretch(1)
-        button_row.addWidget(cancel_btn)
-        button_row.addWidget(save_btn)
-
-        # ── Layout ───────────────────────────────────────────────────────
-        outer_layout = QtWidgets.QVBoxLayout(self)
-        outer_layout.setContentsMargins(16, 16, 16, 16)
-        outer_layout.setSpacing(12)
-        outer_layout.addLayout(form)
-        outer_layout.addStretch(1)
-        outer_layout.addLayout(button_row)
-
-        # ── Apply draft values to form ───────────────────────────────────
+        self._category_list = QtWidgets.QListWidget()
+        self._category_list.setObjectName("settingsCategories")
+        self._category_list.setFixedWidth(190)
+        icons = {
+            "General": Icons.settings(),
+            "Downloads": Icons.download(),
+            "qBittorrent": Icons.queue(),
+            "Library": Icons.library(),
+            "Appearance": Icons.image(),
+            "Advanced": Icons.database(),
+        }
+        for category in self.CATEGORIES:
+            self._category_list.addItem(QtWidgets.QListWidgetItem(icons[category], category))
+        self._category_list.currentRowChanged.connect(self._stack_category_changed)
+
+        self._pages = QtWidgets.QStackedWidget()
+        self._pages.addWidget(self._build_general_page())
+        self._pages.addWidget(self._build_downloads_page())
+        self._pages.addWidget(self._build_qbit_page())
+        self._pages.addWidget(self._build_library_page())
+        self._pages.addWidget(self._build_appearance_page())
+        self._pages.addWidget(self._build_advanced_page())
+
+        body = QtWidgets.QHBoxLayout()
+        body.setContentsMargins(0, 0, 0, 0)
+        body.setSpacing(14)
+        body.addWidget(self._category_list)
+        body.addWidget(self._pages, 1)
+
+        self._restore_btn = QtWidgets.QPushButton(Icons.refresh(), "Restore defaults")
+        self._restore_btn.clicked.connect(self._restore_defaults)
+        self._cancel_btn = QtWidgets.QPushButton("Cancel")
+        self._cancel_btn.clicked.connect(self._on_cancel)
+        self._save_btn = QtWidgets.QPushButton(Icons.save(), "Save changes")
+        self._save_btn.setObjectName("primaryButton")
+        self._save_btn.clicked.connect(self._on_save)
+        footer = QtWidgets.QHBoxLayout()
+        footer.addWidget(self._restore_btn)
+        footer.addStretch(1)
+        footer.addWidget(self._cancel_btn)
+        footer.addWidget(self._save_btn)
+
+        root = QtWidgets.QVBoxLayout(self)
+        root.setContentsMargins(0, 0, 0, 0)
+        root.setSpacing(12)
+        root.addWidget(self._header)
+        root.addLayout(body, 1)
+        root.addLayout(footer)
+
+        self._connect_dirty_signals()
         self._apply_draft_to_form()
-
-    # ── Draft management ─────────────────────────────────────────────────
+        self._category_list.setCurrentRow(1)
+        self._set_dirty(False)
+
+    # ── Page builders ───────────────────────────────────────────────────
+
+    @staticmethod
+    def _scroll_page(card: QtWidgets.QWidget) -> QtWidgets.QScrollArea:
+        scroll = QtWidgets.QScrollArea()
+        scroll.setWidgetResizable(True)
+        scroll.setFrameShape(QtWidgets.QFrame.Shape.NoFrame)
+        scroll.setWidget(card)
+        return scroll
+
+    def _build_general_page(self) -> QtWidgets.QWidget:
+        container = QtWidgets.QWidget()
+        layout = QtWidgets.QVBoxLayout(container)
+        notifications = SectionCard("Application behavior", Icons.settings())
+        self._notifications_toggle = QToggleSwitch("Show desktop and in-app notifications")
+        self._open_destination_toggle = QToggleSwitch("Open destination when a download finishes")
+        notifications.content_layout().addWidget(self._notifications_toggle)
+        notifications.content_layout().addWidget(self._open_destination_toggle)
+        layout.addWidget(notifications)
+        layout.addStretch(1)
+        return self._scroll_page(container)
+
+    def _build_downloads_page(self) -> QtWidgets.QWidget:
+        container = QtWidgets.QWidget()
+        layout = QtWidgets.QVBoxLayout(container)
+        card = SectionCard("Downloads", Icons.download())
+        form = QtWidgets.QFormLayout()
+        self._download_method = QtWidgets.QComboBox()
+        self._download_method.addItem("qBittorrent", "qbit")
+        form.addRow("Download method", self._download_method)
+        self._output_picker = PathPicker("", parent=self)
+        form.addRow("Output directory", self._output_picker)
+        self._keep_seeding = QToggleSwitch("Keep completed torrents seeding")
+        self._hardlinks = QToggleSwitch("Create hardlinks in library")
+        self._preserve_partial = QToggleSwitch("Preserve partial downloads")
+        form.addRow("", self._keep_seeding)
+        form.addRow("", self._hardlinks)
+        form.addRow("", self._preserve_partial)
+        self._max_concurrent = QtWidgets.QSpinBox()
+        self._max_concurrent.setRange(1, 50)
+        form.addRow("Max concurrent torrents", self._max_concurrent)
+        self._timeout = QtWidgets.QSpinBox()
+        self._timeout.setRange(30, 86400)
+        self._timeout.setSuffix(" sec")
+        form.addRow("Timeout per batch", self._timeout)
+        card.content_layout().addLayout(form)
+        layout.addWidget(card)
+        layout.addStretch(1)
+        return self._scroll_page(container)
+
+    def _build_qbit_page(self) -> QtWidgets.QWidget:
+        container = QtWidgets.QWidget()
+        layout = QtWidgets.QVBoxLayout(container)
+        card = SectionCard("qBittorrent connection", Icons.queue())
+        form = QtWidgets.QFormLayout()
+        self._qbit_url = QtWidgets.QLineEdit()
+        self._qbit_url.setPlaceholderText("http://127.0.0.1:8080")
+        self._qbit_user = QtWidgets.QLineEdit()
+        self._qbit_password = QtWidgets.QLineEdit()
+        self._qbit_password.setEchoMode(QtWidgets.QLineEdit.EchoMode.Password)
+        form.addRow("Connection URL", self._qbit_url)
+        form.addRow("Username", self._qbit_user)
+        form.addRow("Password", self._qbit_password)
+        connection_row = QtWidgets.QHBoxLayout()
+        self._test_btn = QtWidgets.QPushButton(Icons.queue(), "Test connection")
+        self._test_btn.setObjectName("primaryButton")
+        self._test_btn.clicked.connect(self._on_test_connection)
+        self._connection_badge = StatusBadge("Not tested", BadgeKind.NEUTRAL)
+        connection_row.addWidget(self._test_btn)
+        connection_row.addWidget(self._connection_badge)
+        connection_row.addStretch(1)
+        form.addRow("", connection_row)
+        card.content_layout().addLayout(form)
+        layout.addWidget(card)
+        layout.addStretch(1)
+        return self._scroll_page(container)
+
+    def _build_library_page(self) -> QtWidgets.QWidget:
+        container = QtWidgets.QWidget()
+        layout = QtWidgets.QVBoxLayout(container)
+        card = SectionCard("Library and index", Icons.library())
+        form = QtWidgets.QFormLayout()
+        self._torrent_picker = PathPicker("", parent=self)
+        self._index_edit = QtWidgets.QLineEdit()
+        self._index_browse = QtWidgets.QPushButton("Browse…")
+        self._index_browse.clicked.connect(self._browse_index)
+        index_row = QtWidgets.QHBoxLayout()
+        index_row.addWidget(self._index_edit, 1)
+        index_row.addWidget(self._index_browse)
+        self._cover_picker = PathPicker("", parent=self)
+        form.addRow("Torrent source directory", self._torrent_picker)
+        form.addRow("Index database", index_row)
+        form.addRow("Cover directory", self._cover_picker)
+        card.content_layout().addLayout(form)
+        layout.addWidget(card)
+        layout.addStretch(1)
+        return self._scroll_page(container)
+
+    def _build_appearance_page(self) -> QtWidgets.QWidget:
+        container = QtWidgets.QWidget()
+        layout = QtWidgets.QVBoxLayout(container)
+        card = SectionCard("Appearance", Icons.image())
+        form = QtWidgets.QFormLayout()
+        self._theme = QtWidgets.QComboBox()
+        self._theme.addItem("Dark", ThemeName.DARK)
+        self._density = QtWidgets.QComboBox()
+        for density in Density:
+            self._density.addItem(density.value.title(), density.value)
+        self._accent = QtWidgets.QComboBox()
+        for label, value in (("Blue", AccentName.BLUE), ("Purple", AccentName.PURPLE), ("Green", AccentName.GREEN)):
+            self._accent.addItem(label, value)
+        form.addRow("Theme", self._theme)
+        form.addRow("Density", self._density)
+        form.addRow("Accent color", self._accent)
+        card.content_layout().addLayout(form)
+        self._preview = QtWidgets.QFrame()
+        self._preview.setObjectName("appearancePreview")
+        self._preview.setMinimumHeight(150)
+        preview_layout = QtWidgets.QHBoxLayout(self._preview)
+        sidebar = QtWidgets.QFrame()
+        sidebar.setFixedWidth(50)
+        sidebar.setObjectName("previewSidebar")
+        content = QtWidgets.QVBoxLayout()
+        content.addWidget(QtWidgets.QLabel("Preview"))
+        preview_button = QtWidgets.QPushButton("Primary action")
+        preview_button.setObjectName("primaryButton")
+        content.addWidget(preview_button)
+        content.addStretch(1)
+        preview_layout.addWidget(sidebar)
+        preview_layout.addLayout(content, 1)
+        card.content_layout().addWidget(self._preview)
+        layout.addWidget(card)
+        layout.addStretch(1)
+        return self._scroll_page(container)
+
+    def _build_advanced_page(self) -> QtWidgets.QWidget:
+        container = QtWidgets.QWidget()
+        layout = QtWidgets.QVBoxLayout(container)
+        card = SectionCard("Migration and diagnostics", Icons.database())
+        migrate = QtWidgets.QPushButton("Migrate legacy settings")
+        migrate.clicked.connect(self._on_migrate_legacy)
+        open_config = QtWidgets.QPushButton(Icons.folder_open(), "Open settings location")
+        open_config.clicked.connect(self._open_settings_location)
+        card.content_layout().addWidget(migrate)
+        card.content_layout().addWidget(open_config)
+        layout.addWidget(card)
+        layout.addStretch(1)
+        return self._scroll_page(container)
+
+    # ── Draft lifecycle ─────────────────────────────────────────────────
 
     def _load_draft(self) -> None:
-        """Load persisted QSettings into the draft."""
-        self._draft.qbit_url = self._settings.value(
-            "qbit_url", "http://localhost:8080", str,
-        )
-        self._draft.qbit_username = self._settings.value(
-            "qbit_user", "admin", str,
-        )
-        self._draft.qbit_password = self._settings.value(
-            "qbit_pass", "", str,
-        )
-        output_dir = self._settings.value(
-            "output_dir", "downloads", str,
-        )
-        self._draft.output_directory = Path(output_dir)
-        self._draft.keep_seeding = self._settings.value(
-            "keep_seeding", False, bool,
-        )
-        self._draft.create_hardlinks = self._settings.value(
-            "create_hardlinks", True, bool,
-        )
-        self._draft.preserve_partial = self._settings.value(
-            "preserve_partial", False, bool,
-        )
-        self._draft.max_concurrent = int(
-            self._settings.value("max_concurrent", 3, str),
-        )
-        self._draft.timeout_seconds = int(
-            self._settings.value("timeout_seconds", 30, str),
+        s = self._settings
+        self._draft = SettingsDraft(
+            qbit_url=s.value("qbit_url", "http://localhost:8080", str),
+            qbit_username=s.value("qbit_user", "admin", str),
+            qbit_password=s.value("qbit_pass", "", str),
+            output_directory=Path(s.value("output_dir", "downloads", str)),
+            torrent_directory=Path(s.value("torrent_dir", "torrents/Minerva Myrient - 1050 torrents", str)),
+            index_path=Path(s.value("index_path", "torrents/minerva_index.db", str)),
+            cover_directory=Path(s.value("cover_dir", "covers", str)),
+            keep_seeding=s.value("keep_seeding", True, bool),
+            create_hardlinks=s.value("create_hardlinks", True, bool),
+            preserve_partial=s.value("preserve_partial", True, bool),
+            open_destination=s.value("open_destination", False, bool),
+            notifications=s.value("notifications", True, bool),
+            max_concurrent=int(s.value("max_concurrent", 6, int)),
+            timeout_seconds=int(s.value("timeout_seconds", 1800, int)),
+            density=Density(s.value(DENSITY_SETTINGS_KEY, Density.COMPACT.value, str)),
+            theme=s.value("ui/theme", ThemeName.DARK, str),
+            accent=s.value("ui/accent", AccentName.BLUE, str),
         )
 
     def _apply_draft_to_form(self) -> None:
-        """Populate form fields from the current draft."""
-        self._output_edit.setText(str(self._draft.output_directory))
-        self._qbit_url_edit.setText(self._draft.qbit_url)
-        self._qbit_user_edit.setText(self._draft.qbit_username)
-        self._qbit_pass_edit.setText(self._draft.qbit_password)
-        self._max_concurrent_spin.setValue(self._draft.max_concurrent)
-        self._timeout_spin.setValue(self._draft.timeout_seconds)
-        self._keep_seeding_cb.setChecked(self._draft.keep_seeding)
-        self._hardlinks_cb.setChecked(self._draft.create_hardlinks)
-        self._preserve_partial_cb.setChecked(self._draft.preserve_partial)
-
-    def _apply_form_to_draft(self) -> None:
-        """Read form field values into the draft."""
-        self._draft.qbit_url = self._qbit_url_edit.text().strip()
-        self._draft.qbit_username = self._qbit_user_edit.text().strip()
-        self._draft.qbit_password = self._qbit_pass_edit.text()
-        self._draft.output_directory = Path(
-            self._output_edit.text().strip() or "downloads",
-        )
-        self._draft.keep_seeding = self._keep_seeding_cb.isChecked()
-        self._draft.create_hardlinks = self._hardlinks_cb.isChecked()
-        self._draft.preserve_partial = self._preserve_partial_cb.isChecked()
-        self._draft.max_concurrent = self._max_concurrent_spin.value()
-        self._draft.timeout_seconds = self._timeout_spin.value()
-
-    def _persist_draft(self) -> None:
-        """Persist the current draft values to QSettings."""
-        self._settings.setValue("qbit_url", self._draft.qbit_url)
-        self._settings.setValue("qbit_user", self._draft.qbit_username)
-        self._settings.setValue("qbit_pass", self._draft.qbit_password)
-        self._settings.setValue("output_dir", str(self._draft.output_directory))
-        self._settings.setValue("keep_seeding", self._draft.keep_seeding)
-        self._settings.setValue("create_hardlinks", self._draft.create_hardlinks)
-        self._settings.setValue("preserve_partial", self._draft.preserve_partial)
-        self._settings.setValue("max_concurrent", self._draft.max_concurrent)
-        self._settings.setValue("timeout_seconds", self._draft.timeout_seconds)
-        self._settings.setValue(
-            DENSITY_SETTINGS_KEY, self._density_combo.currentData(),
+        self._loading = True
+        draft = self._draft
+        self._output_picker.set_path(str(draft.output_directory))
+        self._torrent_picker.set_path(str(draft.torrent_directory))
+        self._index_edit.setText(str(draft.index_path))
+        self._cover_picker.set_path(str(draft.cover_directory))
+        self._qbit_url.setText(draft.qbit_url)
+        self._qbit_user.setText(draft.qbit_username)
+        self._qbit_password.setText(draft.qbit_password)
+        self._keep_seeding.setChecked(draft.keep_seeding)
+        self._hardlinks.setChecked(draft.create_hardlinks)
+        self._preserve_partial.setChecked(draft.preserve_partial)
+        self._open_destination_toggle.setChecked(draft.open_destination)
+        self._notifications_toggle.setChecked(draft.notifications)
+        self._max_concurrent.setValue(draft.max_concurrent)
+        self._timeout.setValue(draft.timeout_seconds)
+        self._density.setCurrentIndex(max(0, self._density.findData(draft.density.value)))
+        self._theme.setCurrentIndex(max(0, self._theme.findData(draft.theme)))
+        self._accent.setCurrentIndex(max(0, self._accent.findData(draft.accent)))
+        self._loading = False
+
+    def _form_to_draft(self) -> SettingsDraft:
+        return SettingsDraft(
+            qbit_url=self._qbit_url.text().strip(),
+            qbit_username=self._qbit_user.text().strip(),
+            qbit_password=self._qbit_password.text(),
+            output_directory=Path(self._output_picker.path().strip() or "downloads"),
+            torrent_directory=Path(self._torrent_picker.path().strip()),
+            index_path=Path(self._index_edit.text().strip()),
+            cover_directory=Path(self._cover_picker.path().strip() or "covers"),
+            keep_seeding=self._keep_seeding.isChecked(),
+            create_hardlinks=self._hardlinks.isChecked(),
+            preserve_partial=self._preserve_partial.isChecked(),
+            open_destination=self._open_destination_toggle.isChecked(),
+            notifications=self._notifications_toggle.isChecked(),
+            max_concurrent=self._max_concurrent.value(),
+            timeout_seconds=self._timeout.value(),
+            density=Density(self._density.currentData()),
+            theme=self._theme.currentData(),
+            accent=self._accent.currentData(),
         )
-        self._settings.sync()
-        log.debug("Settings persisted")
 
-    # ── Validation ───────────────────────────────────────────────────────
+    def _connect_dirty_signals(self) -> None:
+        for edit in (self._qbit_url, self._qbit_user, self._qbit_password, self._index_edit, self._output_picker.path_edit, self._torrent_picker.path_edit, self._cover_picker.path_edit):
+            edit.textChanged.connect(self._on_form_changed)
+        for combo in (self._theme, self._density, self._accent, self._download_method):
+            combo.currentIndexChanged.connect(self._on_form_changed)
+        for spin in (self._max_concurrent, self._timeout):
+            spin.valueChanged.connect(self._on_form_changed)
+        for toggle in (self._keep_seeding, self._hardlinks, self._preserve_partial, self._open_destination_toggle, self._notifications_toggle):
+            toggle.toggled.connect(self._on_form_changed)
+        self._density.currentIndexChanged.connect(self._preview_appearance)
+        self._accent.currentIndexChanged.connect(self._preview_appearance)
+
+    def _on_form_changed(self, *_args) -> None:
+        if not self._loading:
+            self._set_dirty(True)
+
+    def _set_dirty(self, dirty: bool) -> None:
+        self._dirty = dirty
+        self._save_btn.setEnabled(dirty)
+        self._cancel_btn.setEnabled(dirty)
+        self._header.set_subtitle(
+            "Configure downloads, indexing, qBittorrent, and application behavior."
+            + ("  ·  Unsaved changes" if dirty else "")
+        )
 
-    def _validate(self) -> list[str]:
-        """Validate the current draft. Returns a list of error messages."""
+    def _validate(self, draft: SettingsDraft) -> list[str]:
         errors: list[str] = []
-
-        # Validate qBittorrent URL
-        url = self._draft.qbit_url.strip()
-        if url and not url.startswith(("http://", "https://")):
+        if not draft.qbit_url.startswith(("http://", "https://")):
             errors.append("qBittorrent URL must start with http:// or https://")
-
-        # Validate output directory
-        output = self._draft.output_directory
-        if output and not output.exists():
+        if not str(draft.torrent_directory):
+            errors.append("Torrent source directory is required")
+        if not str(draft.index_path):
+            errors.append("Index database path is required")
+        for path, label in ((draft.output_directory, "Output directory"), (draft.cover_directory, "Cover directory")):
             try:
-                output.mkdir(parents=True, exist_ok=True)
-            except OSError:
-                errors.append(
-                    f"Output directory does not exist and cannot be created: {output}",
-                )
-
+                path.mkdir(parents=True, exist_ok=True)
+            except OSError as exc:
+                errors.append(f"{label} cannot be created: {exc}")
         return errors
 
-    # ── Signal handlers ──────────────────────────────────────────────────
-
-    def _on_browse_output(self) -> None:
-        """Open a directory picker and set the output path."""
-        current = self._output_edit.text().strip() or "."
-        path = QtWidgets.QFileDialog.getExistingDirectory(
-            self,
-            "Select output directory",
-            current,
-        )
-        if path:
-            self._output_edit.setText(path)
-
     def _on_save(self) -> None:
-        """Validate, persist, and confirm."""
-        self._apply_form_to_draft()
-        errors = self._validate()
+        draft = self._form_to_draft()
+        errors = self._validate(draft)
         if errors:
-            msg = "\n".join(errors)
-            NotificationBanner.show_error(
-                self, "Validation failed", msg, duration=6000,
-            )
+            NotificationBanner.show_error(self, "Validation failed", "\n".join(errors), duration=7000)
             return
-
-        self._persist_draft()
+        self._draft = draft
+        values = {
+            "qbit_url": draft.qbit_url,
+            "qbit_user": draft.qbit_username,
+            "qbit_pass": draft.qbit_password,
+            "output_dir": str(draft.output_directory),
+            "torrent_dir": str(draft.torrent_directory),
+            "index_path": str(draft.index_path),
+            "cover_dir": str(draft.cover_directory),
+            "keep_seeding": draft.keep_seeding,
+            "create_hardlinks": draft.create_hardlinks,
+            "preserve_partial": draft.preserve_partial,
+            "open_destination": draft.open_destination,
+            "notifications": draft.notifications,
+            "max_concurrent": draft.max_concurrent,
+            "timeout_seconds": draft.timeout_seconds,
+            DENSITY_SETTINGS_KEY: draft.density.value,
+            "ui/theme": draft.theme,
+            "ui/accent": draft.accent,
+        }
+        for key, value in values.items():
+            self._settings.setValue(key, value)
+        self._settings.sync()
+        shell = self.window()
+        if hasattr(shell, "apply_appearance"):
+            shell.apply_appearance(draft.density, draft.accent)
+        if hasattr(shell, "reload_download_controller"):
+            shell.reload_download_controller()
+        self._set_dirty(False)
         NotificationBanner.show_success(self, "Settings saved")
-        self._app_state.log_message.emit("Settings saved")
 
     def _on_cancel(self) -> None:
-        """Discard form edits and reload from saved state."""
         self._load_draft()
         self._apply_draft_to_form()
-        NotificationBanner.show_info(self, "Changes discarded")
+        self._set_dirty(False)
+        shell = self.window()
+        if hasattr(shell, "apply_appearance"):
+            shell.apply_appearance(self._draft.density, self._draft.accent)
+
+    def can_deactivate(self) -> bool:
+        if not self._dirty:
+            return True
+        reply = QtWidgets.QMessageBox.question(
+            self,
+            "Unsaved settings",
+            "Discard unsaved settings changes?",
+            QtWidgets.QMessageBox.StandardButton.Discard | QtWidgets.QMessageBox.StandardButton.Cancel,
+            QtWidgets.QMessageBox.StandardButton.Cancel,
+        )
+        if reply == QtWidgets.QMessageBox.StandardButton.Discard:
+            self._on_cancel()
+            return True
+        return False
 
-    def _on_test_connection(self) -> None:
-        """Test the qBittorrent connection with the current form values."""
-        url = self._qbit_url_edit.text().strip()
-        username = self._qbit_user_edit.text().strip()
-        password = self._qbit_pass_edit.text()
-
-        if not url:
-            NotificationBanner.show_warning(
-                self, "No URL", "Enter a qBittorrent Web UI URL first",
-            )
-            return
+    # ── Actions ─────────────────────────────────────────────────────────
 
+    def _on_test_connection(self) -> None:
+        url = self._qbit_url.text().strip()
         if not url.startswith(("http://", "https://")):
-            NotificationBanner.show_error(
-                self, "Invalid URL", "URL must start with http:// or https://",
-            )
+            NotificationBanner.show_error(self, "Invalid URL", "Enter a valid HTTP(S) qBittorrent URL")
             return
-
-        # Attempt connection in a background task to keep UI responsive
-        self._test_connection_task(url, username, password)
-
-    def _test_connection_task(
-        self,
-        url: str,
-        username: str,
-        password: str,
-    ) -> None:
-        """Try connecting to qBittorrent and report the result."""
-        try:
-            from minerva_qbit import QBittorrentClient
-
-            client = QBittorrentClient(url, username, password)
-            connected = client.login()
-        except Exception as exc:
-            NotificationBanner.show_error(
-                self, "Connection failed", str(exc),
-            )
+        self._test_btn.setEnabled(False)
+        self._test_btn.setText("Testing…")
+        task = TaskRunner(_connection_task, url, self._qbit_user.text().strip(), self._qbit_password.text())
+        task.signals.result.connect(self._connection_success)
+        task.signals.error.connect(self._connection_failed)
+        self._pool.start(task)
+
+    def _connection_success(self, version: object) -> None:
+        self._test_btn.setEnabled(True)
+        self._test_btn.setText("Test connection")
+        self._connection_badge.setText(f"Connected · {version}")
+        self._connection_badge.set_kind(BadgeKind.SUCCESS)
+
+    def _connection_failed(self, details: str) -> None:
+        self._test_btn.setEnabled(True)
+        self._test_btn.setText("Test connection")
+        self._connection_badge.setText("Connection failed")
+        self._connection_badge.set_kind(BadgeKind.ERROR)
+        NotificationBanner.show_error(self, "Connection failed", details)
+
+    def _preview_appearance(self, *_args) -> None:
+        if self._loading:
             return
+        shell = self.window()
+        if hasattr(shell, "apply_appearance"):
+            shell.apply_appearance(Density(self._density.currentData()), self._accent.currentData())
 
-        if connected:
-            NotificationBanner.show_success(
-                self, "Connection successful",
-                f"Connected to {url}",
-            )
-        else:
-            NotificationBanner.show_error(
-                self, "Authentication failed",
-                "Check your username and password",
-            )
+    def _restore_defaults(self) -> None:
+        self._draft = SettingsDraft()
+        self._apply_draft_to_form()
+        self._set_dirty(True)
+        self._preview_appearance()
 
-    def _on_migrate_legacy(self) -> None:
-        """Migrate legacy settings from old MinervaWindow keys.
+    def _browse_index(self) -> None:
+        path, _ = QtWidgets.QFileDialog.getSaveFileName(self, "Select index database", self._index_edit.text(), "SQLite database (*.db)")
+        if path:
+            self._index_edit.setText(path)
 
-        Reads the old-style QSettings keys and maps them to the new
-        SettingsDraft-compatible keys, then persists.
-        """
-        legacy_org = "MinervaFixDAT"
-        legacy_app = "MinervaGUI"
+    def _stack_category_changed(self, row: int) -> None:
+        if row >= 0:
+            self._pages.setCurrentIndex(row)
 
-        old = QtCore.QSettings(legacy_org, legacy_app)
+    def _on_migrate_legacy(self) -> None:
+        old = QtCore.QSettings("MinervaFixDAT", "MinervaGUI")
         migrated = 0
+        for key in ("qbit_url", "qbit_user", "qbit_pass", "output_dir"):
+            if old.contains(key):
+                self._settings.setValue(key, old.value(key))
+                migrated += 1
+        self._settings.sync()
+        self._load_draft()
+        self._apply_draft_to_form()
+        NotificationBanner.show_success(self, "Migration complete", f"Migrated {migrated} setting(s)")
 
-        # Map old keys → new keys
-        mappings: dict[str, str] = {
-            "qbit_url": "qbit_url",
-            "qbit_user": "qbit_user",
-            "qbit_pass": "qbit_pass",
-            "output_dir": "output_dir",
-        }
+    def _open_settings_location(self) -> None:
+        from PyQt6 import QtGui
 
-        for old_key, new_key in mappings.items():
-            if old.contains(old_key):
-                value = old.value(old_key)
-                if value is not None:
-                    self._settings.setValue(new_key, value)
-                    migrated += 1
-
-        old.sync()
-
-        if migrated:
-            self._load_draft()
-            self._apply_draft_to_form()
-            NotificationBanner.show_success(
-                self, "Migration complete",
-                f"Migrated {migrated} legacy setting(s)",
-            )
-        else:
-            NotificationBanner.show_info(
-                self, "No legacy settings found",
-                "No MinervaWindow settings to migrate.",
-            )
+        QtGui.QDesktopServices.openUrl(QtCore.QUrl.fromLocalFile(str(Path(self._settings.fileName()).parent)))
diff --git a/minerva/app/shell.py b/minerva/app/shell.py
index b7a3256..599c85f 100644
--- a/minerva/app/shell.py
+++ b/minerva/app/shell.py
@@ -28,7 +28,7 @@ from minerva_db import DEFAULT_INDEX_PATH, DEFAULT_TORRENT_DIR, MinervaDB
 from minerva_qbit import QBittorrentClient
 from minerva_state import MinervaState
 from minerva.ui.density import Density
-from minerva.ui.theme import ThemeTokens
+from minerva.ui.theme import ThemeTokens, apply_theme
 
 if typing.TYPE_CHECKING:
     from collections.abc import Iterable
@@ -99,6 +99,7 @@ class AppShell(QtWidgets.QMainWindow):
         self._init_actions()
         self._init_ui()
         self._register_pages()
+        self._create_download_controller()
         self._restore_settings_and_show_initial_page()
 
     # ── Public API ──────────────────────────────────────────────────────
@@ -124,26 +125,29 @@ class AppShell(QtWidgets.QMainWindow):
         return self._download_controller
 
     def show_page(self, page_id: PageId) -> None:
-        """Show (and lazily construct) the page identified by *page_id*.
+        """Show a page, respecting the current page's dirty-state contract."""
+        current = self._stack.currentWidget()
+        if current is not None and current is not self._registry._cache.get(page_id):
+            if hasattr(current, "can_deactivate") and not current.can_deactivate():
+                if self._sidebar is not None:
+                    self._sidebar.set_active(self._current_page_id)
+                return
+            if hasattr(current, "deactivate"):
+                current.deactivate()
 
-        Top-level navigation pages are remembered in ``_nav_history`` so
-        that ``show_match_review`` can offer a Back action.
-        """
         if page_id not in (PageId.MATCH_REVIEW,):
             self._nav_history.append(page_id)
-            # Keep history bounded
             if len(self._nav_history) > 20:
                 self._nav_history.pop(0)
 
         page = self._registry.get_or_create(page_id, self._app_state)
         if page.parentWidget() is not self._stack:
             self._stack.addWidget(page)
+            self._restore_page_layout(page_id, page)
         self._stack.setCurrentWidget(page)
         self._current_page_id = page_id
         if self._sidebar is not None:
             self._sidebar.set_active(page_id)
-
-        # Notify page lifecycle
         page.activate()
 
     def show_match_review(self, report_id: str) -> None:
@@ -151,6 +155,32 @@ class AppShell(QtWidgets.QMainWindow):
         self._app_state.current_report_id = report_id
         self.show_page(PageId.MATCH_REVIEW)
 
+    def apply_appearance(self, density: Density, accent: str = "blue") -> None:
+        """Apply density/accent immediately to all constructed pages."""
+        self._density = density
+        self._tokens = ThemeTokens.for_accent(accent)
+        app = QtWidgets.QApplication.instance()
+        if app is not None:
+            apply_theme(app, self._tokens, self._density)
+        if self._sidebar is not None:
+            self._sidebar.apply_tokens(self._tokens)
+            self._sidebar.apply_density(self._density)
+        for page in self._registry.iter_constructed():
+            for widget in [page, *page.findChildren(QtWidgets.QWidget)]:
+                if hasattr(widget, "apply_tokens"):
+                    widget.apply_tokens(self._tokens)
+                if hasattr(widget, "apply_density"):
+                    widget.apply_density(self._density)
+
+    def reload_download_controller(self) -> None:
+        """Recreate qBittorrent services after connection settings change."""
+        if self._download_controller is not None:
+            self._download_controller.stop_monitoring()
+            self._download_controller.deleteLater()
+        self._download_controller = None
+        self._create_download_controller()
+        self._app_state.queue_changed.emit()
+
     # ── Initialisation helpers ──────────────────────────────────────────
 
     def _init_actions(self) -> None:
@@ -236,6 +266,13 @@ class AppShell(QtWidgets.QMainWindow):
         if state is not None:
             self.restoreState(state)
 
+        density_value = settings.value("ui/density", self._density.value, str)
+        accent = settings.value("ui/accent", "blue", str)
+        try:
+            self.apply_appearance(Density(density_value), accent)
+        except ValueError:
+            self.apply_appearance(Density.COMPACT, accent)
+
         tab_value = settings.value("current_tab", "", str)
         saved_page = PageId.REPORTS
         if tab_value:
@@ -250,33 +287,60 @@ class AppShell(QtWidgets.QMainWindow):
         settings = QtCore.QSettings(self._settings_org, self._settings_app)
         settings.setValue("geometry", self.saveGeometry())
         settings.setValue("window_state", self.saveState())
-        settings.setValue("splitter_sizes", [])
+        self._save_page_layouts(settings)
         settings.setValue("current_tab", self._current_page_id.value)
         settings.setValue(
             "ui/density",
             self._density.value,
         )
 
+    def _save_page_layouts(self, settings: QtCore.QSettings) -> None:
+        for page_id, page in self._registry._cache.items():
+            for index, splitter in enumerate(page.findChildren(QtWidgets.QSplitter)):
+                settings.setValue(
+                    f"page_layouts/{page_id.value}/splitter_{index}",
+                    splitter.sizes(),
+                )
+
+    def _restore_page_layout(self, page_id: PageId, page: QtWidgets.QWidget) -> None:
+        settings = QtCore.QSettings(self._settings_org, self._settings_app)
+        for index, splitter in enumerate(page.findChildren(QtWidgets.QSplitter)):
+            sizes = settings.value(
+                f"page_layouts/{page_id.value}/splitter_{index}",
+                None,
+            )
+            if sizes:
+                splitter.setSizes([int(value) for value in sizes])
+
     # ── Download controller lifecycle ────────────────────────────────────
 
     def _create_download_controller(self) -> None:
-        """Build the download controller from saved qBittorrent settings."""
+        """Build the persistent qBittorrent controller from saved settings."""
         settings = QtCore.QSettings(self._settings_org, self._settings_app)
         qbit_url = settings.value("qbit_url", "http://localhost:8080", str)
         qbit_user = settings.value("qbit_user", "admin", str)
-        qbit_pass = settings.value("qbit_pass", "adminadmin", str)
-        seed_dir = str(DEFAULT_TORRENT_DIR)
+        qbit_pass = settings.value("qbit_pass", "", str)
+        seed_dir = settings.value("qbit_seed_dir", ".qbitseed", str)
+        index_path = settings.value("index_path", str(DEFAULT_INDEX_PATH), str)
+        state_path = settings.value("state_db_path", "data/minerva_state.db", str)
 
-        client = QBittorrentClient(qbit_url, qbit_user, qbit_pass)
-        db = MinervaDB(DEFAULT_INDEX_PATH)
-        state = MinervaState(db)
-        controller = DownloadController(state, self._app_state, client, seed_dir)
-        controller.queue_changed.connect(self._app_state.queue_changed.emit)
-        controller.error.connect(self._show_runtime_error)
-        self._download_controller = controller
-
-        # Wire up the file spec resolver
-        controller.file_spec_resolver = lambda file_id: db.get_download_spec(file_id)
+        try:
+            client = QBittorrentClient(qbit_url, qbit_user, qbit_pass)
+            db = MinervaDB(index_path)
+            state = MinervaState(state_path)
+            controller = DownloadController(state, self._app_state, client, seed_dir)
+            controller.queue_changed.connect(self._app_state.queue_changed.emit)
+            controller.error.connect(self._show_runtime_error)
+            controller.file_spec_resolver = lambda file_id: db.get_download_spec(
+                file_id,
+                Path(settings.value("torrent_dir", str(DEFAULT_TORRENT_DIR), str)),
+            )
+            self._download_controller = controller
+            controller.start_monitoring()
+        except Exception as exc:
+            log.warning("Download controller unavailable: %s", exc, exc_info=True)
+            self._download_controller = None
+            self.statusBar().showMessage(f"Downloads unavailable: {exc}", 8000)
 
     def _show_runtime_error(self, message: str) -> None:
         """Display a non-blocking status bar error message."""
@@ -311,7 +375,7 @@ class AppShell(QtWidgets.QMainWindow):
         5. Destroy / accept.
         """
         # Step 1 — worker-prompt
-        if self._worker_manager.is_active():
+        if self._worker_manager.is_active() or (self._download_controller is not None and self._download_controller.has_active_downloads()):
             reply = QtWidgets.QMessageBox.question(
                 self,
                 "Confirm Exit",
@@ -325,9 +389,6 @@ class AppShell(QtWidgets.QMainWindow):
                 return
             self._worker_manager.prepare_shutdown()
 
-        # Step 2 — auto-save queue (best-effort)
-        self._auto_save_queue()
-
         # Step 3 — per-page prepare_close
         for page in self._registry.iter_constructed():
             page.prepare_close()
@@ -350,14 +411,3 @@ class AppShell(QtWidgets.QMainWindow):
             pass  # Already deleted during teardown
         super().closeEvent(event)
         event.accept()
-
-    def _auto_save_queue(self) -> None:
-        """Best-effort auto-save of the download queue.
-
-        In v1 this is a no-op when ``minerva_gui`` is not loaded.
-        A future slice will move queue persistence here.
-        """
-        try:
-            from minerva_gui import SAVED_QUEUE_PATH  # noqa: F401
-        except ImportError:
-            pass  # Queue persistence not available yet
diff --git a/minerva/domain/collections.py b/minerva/domain/collections.py
index 42a30de..2197e81 100644
--- a/minerva/domain/collections.py
+++ b/minerva/domain/collections.py
@@ -1,11 +1,4 @@
-"""
-Domain types for DAT collection and system indexing overview.
-
-Used by the Collections page to display high-level status of the
-indexed library: which collections and systems are present, how many
-files they contain, and whether the underlying source torrents are
-available.
-"""
+"""Domain types for collection and index diagnostics."""
 
 from __future__ import annotations
 
@@ -14,24 +7,14 @@ from datetime import datetime
 from enum import Enum
 
 
-# ── Status enum ───────────────────────────────────────────────────────────
-
-
 class CollectionStatus(Enum):
-    """Availability status of a DAT collection."""
-
     INDEXED = "indexed"
     PARTIAL = "partial"
     MISSING = "missing"
 
 
-# ── Data types ────────────────────────────────────────────────────────────
-
-
 @dataclass(frozen=True)
 class IndexOverview:
-    """High-level statistics for the entire DAT index."""
-
     collections: int
     systems: int
     files: int
@@ -41,8 +24,6 @@ class IndexOverview:
 
 @dataclass(frozen=True)
 class CollectionSummary:
-    """Overview of a single DAT collection."""
-
     name: str
     systems: int
     files: int
@@ -51,11 +32,28 @@ class CollectionSummary:
 
 @dataclass(frozen=True)
 class SystemSummary:
-    """Overview of a single system within a collection."""
-
     collection: str
     system: str
     files: int
     last_update: datetime | None
     source_present: bool
     expected_files: int | None
+
+    @property
+    def coverage(self) -> float | None:
+        if not self.expected_files:
+            return None
+        return min(1.0, self.files / self.expected_files)
+
+
+@dataclass(frozen=True)
+class ReferenceDat:
+    """A user-imported DAT used as an expected-count reference."""
+
+    id: str
+    path: str
+    name: str
+    collection: str | None
+    system: str | None
+    entry_count: int
+    imported_at: str
diff --git a/minerva/domain/library.py b/minerva/domain/library.py
index df4e034..c6741b1 100644
--- a/minerva/domain/library.py
+++ b/minerva/domain/library.py
@@ -1,24 +1,18 @@
 """
 Domain types for the indexed DAT library.
 
-Used by the Library (search/browse) and Collection pages to represent
-individual indexed game entries and the query parameters that filter them.
+Used by the Library and Collections pages to represent indexed entries,
+search requests, and faceted-search summaries.
 """
 
 from __future__ import annotations
 
-from dataclasses import dataclass, field
-
-
-# ── Data types ────────────────────────────────────────────────────────────
+from dataclasses import dataclass
 
 
 @dataclass(frozen=True)
 class LibraryItem:
-    """A single indexed entry from a DAT file.
-
-    Maps to a row in the ``files`` table managed by ``MinervaDB``.
-    """
+    """A single indexed entry from the torrent-backed library."""
 
     id: int
     stem: str
@@ -33,16 +27,41 @@ class LibraryItem:
     regions: tuple[str, ...] = ()
 
 
+@dataclass(frozen=True)
+class FacetCount:
+    """One value/count pair in a faceted search result."""
+
+    value: str
+    count: int
+
+
+@dataclass(frozen=True)
+class LibraryFacets:
+    """Facet counts calculated for a :class:`LibraryQuery`."""
+
+    total: int
+    regions: tuple[FacetCount, ...] = ()
+    categories: tuple[FacetCount, ...] = ()
+    tags: tuple[FacetCount, ...] = ()
+
+
 @dataclass(frozen=True)
 class LibraryQuery:
-    """Parameters for querying the indexed library."""
+    """Parameters for querying the indexed library.
+
+    ``selected_only`` is interpreted by the UI because the selection is an
+    application-session concept rather than index data.  ``categories`` are
+    normalised to ``game``, ``homebrew``, ``demo`` and ``tool``.
+    """
 
     text: str = ""
     collection: str = ""
     system: str = ""
     tags: frozenset[str] = frozenset()
     regions: frozenset[str] = frozenset()
+    categories: frozenset[str] = frozenset()
     selected_only: bool = False
+    file_ids: tuple[int, ...] = ()
     offset: int = 0
     limit: int = 50
     sort_field: str = "stem"
diff --git a/minerva/domain/settings.py b/minerva/domain/settings.py
index 3ea3c4a..2c67ddb 100644
--- a/minerva/domain/settings.py
+++ b/minerva/domain/settings.py
@@ -1,54 +1,39 @@
-"""
-Domain types for application settings and configuration.
-
-Provides ``SettingsDraft`` as a mutable dataclass for the settings form,
-and ``ThemeName`` / ``AccentName`` as simple name-spaced string constants
-that mirror the QSS class selectors used in ``minerva.ui.theme``.
-"""
+"""Editable application settings used by the Settings dashboard."""
 
 from __future__ import annotations
 
-from dataclasses import dataclass, field
+from dataclasses import dataclass
 from pathlib import Path
 
 from minerva.ui.density import Density
 
 
-# ── String constants (namespaced, not enums) ─────────────────────────────
-
-
 class ThemeName:
-    """Well-known theme identifiers."""
-
     DARK = "dark"
 
 
 class AccentName:
-    """Well-known accent colour identifiers."""
-
     BLUE = "blue"
-
-
-# ── Settings draft ────────────────────────────────────────────────────────
+    PURPLE = "purple"
+    GREEN = "green"
 
 
 @dataclass
 class SettingsDraft:
-    """Editable settings form state.
-
-    Mutable by design — the settings page binds form fields directly to
-    these attributes and only persists on save.
-    """
-
     qbit_url: str = "http://localhost:8080"
     qbit_username: str = "admin"
     qbit_password: str = ""
     output_directory: Path = Path("downloads")
-    keep_seeding: bool = False
+    torrent_directory: Path = Path("torrents/Minerva Myrient - 1050 torrents")
+    index_path: Path = Path("torrents/minerva_index.db")
+    cover_directory: Path = Path("covers")
+    keep_seeding: bool = True
     create_hardlinks: bool = True
-    preserve_partial: bool = False
-    max_concurrent: int = 3
-    timeout_seconds: int = 30
-    density: Density = Density.COMFORTABLE
-    theme: str = "dark"
-    accent: str = "blue"
+    preserve_partial: bool = True
+    open_destination: bool = False
+    notifications: bool = True
+    max_concurrent: int = 6
+    timeout_seconds: int = 1800
+    density: Density = Density.COMPACT
+    theme: str = ThemeName.DARK
+    accent: str = AccentName.BLUE
diff --git a/minerva/ui/icons.py b/minerva/ui/icons.py
index cd3b30e..f05f4e3 100644
--- a/minerva/ui/icons.py
+++ b/minerva/ui/icons.py
@@ -167,3 +167,28 @@ class Icons:
         """Blue info icon."""
         cls._ensure()
         return qta.icon("fa5s.info-circle", color="#2f5f9e")
+
+# Additional dashboard actions are assigned after the class definition to keep
+# compatibility with older imports while expanding the semantic icon surface.
+def _icon_factory(name: str, color: str | None = None):
+    def _factory(cls):
+        cls._ensure()
+        kwargs = {"color": color} if color else {}
+        return qta.icon(name, **kwargs)
+    return classmethod(_factory)
+
+Icons.add = _icon_factory("fa5s.plus")
+Icons.refresh = _icon_factory("fa5s.sync-alt")
+Icons.trash = _icon_factory("fa5s.trash-alt")
+Icons.file = _icon_factory("fa5s.file")
+Icons.database = _icon_factory("fa5s.database")
+Icons.chart = _icon_factory("fa5s.chart-line")
+Icons.warning = _icon_factory("fa5s.exclamation-triangle")
+Icons.copy = _icon_factory("fa5s.copy")
+Icons.external = _icon_factory("fa5s.external-link-alt")
+Icons.play = _icon_factory("fa5s.play")
+Icons.pause = _icon_factory("fa5s.pause")
+Icons.retry = _icon_factory("fa5s.redo")
+Icons.filter = _icon_factory("fa5s.filter")
+Icons.image = _icon_factory("fa5s.image")
+Icons.save = _icon_factory("fa5s.save")
diff --git a/minerva/ui/qss/base.qss b/minerva/ui/qss/base.qss
index ccff04e..15dbd44 100644
--- a/minerva/ui/qss/base.qss
+++ b/minerva/ui/qss/base.qss
@@ -367,3 +367,44 @@ QSpinBox {
     min-height: {{density.control_height}}px;
     font-size: 13px;
 }
+
+/* ── Dashboard refinements ───────────────────────────────────────────── */
+QWidget#pageSurface { background: {{token.background}}; }
+QLabel#pageSubtitle, QLabel#mutedLabel { color: {{token.text_muted}}; }
+QLabel#pageTitle { font-size: 26px; font-weight: 700; }
+QLabel#metricLabel { font-size: 24px; font-weight: 700; color: {{token.text}}; }
+QLabel#successLabel { color: {{token.success}}; } QFrame#successLabel { background: {{token.success}}; }
+QLabel#warningLabel { color: {{token.warning}}; } QFrame#warningLabel { background: {{token.warning}}; }
+QLabel#errorLabel { color: {{token.error}}; } QFrame#errorLabel { background: {{token.error}}; }
+QLabel#infoLabel { color: {{token.accent}}; } QFrame#infoLabel { background: {{token.accent}}; }
+QLabel#purpleLabel { color: {{token.purple}}; } QFrame#purpleLabel { background: {{token.purple}}; }
+QLabel#summaryStrip {
+    background: {{token.surface}};
+    border: 1px solid {{token.border}};
+    border-radius: 6px;
+    padding: 7px 12px;
+    color: {{token.text_muted}};
+}
+QLabel#coverPreview {
+    background: {{token.background}};
+    border: 1px solid {{token.border}};
+    border-radius: 8px;
+}
+QFrame#surfaceCard {
+    background-color: {{token.surface}};
+    border: 1px solid {{token.border}};
+    border-radius: 9px;
+}
+QListWidget#facetList {
+    background: transparent;
+    border: none;
+    color: {{token.text}};
+}
+QListWidget#facetList::item { padding: 3px 2px; }
+QListWidget#facetList::item:hover { background: {{token.surface_raised}}; }
+QPushButton#subtleButton { background: {{token.surface_raised}}; border: 1px solid {{token.border}}; }
+QPushButton#primaryButton { background: {{token.accent}}; border-color: {{token.accent}}; color: white; }
+QPushButton#dangerButton { background: transparent; border-color: {{token.error}}; color: {{token.error}}; }
+QTableView { border-color: {{token.border}}; }
+QHeaderView::section { background: {{token.surface_raised}}; border-color: {{token.border}}; }
+QSplitter::handle { background: {{token.border}}; }
diff --git a/minerva/ui/theme.py b/minerva/ui/theme.py
index 718482f..9b09803 100644
--- a/minerva/ui/theme.py
+++ b/minerva/ui/theme.py
@@ -1,14 +1,8 @@
-"""
-Theme tokens and stylesheet builder for the Minerva design system.
-
-``ThemeTokens`` is a frozen dataclass carrying the 7-token dark palette.
-``build_stylesheet()`` reads ``ui/theme/qss/base.qss`` and substitutes
-``{{token.*}}`` and ``{{density.*}}`` placeholders.
-"""
+"""Semantic theme tokens and stylesheet builder for Minerva."""
 
 from __future__ import annotations
 
-from dataclasses import dataclass
+from dataclasses import dataclass, replace
 from pathlib import Path
 
 from minerva.ui.density import Density
@@ -18,75 +12,61 @@ _HERE = Path(__file__).resolve().parent
 
 @dataclass(frozen=True)
 class ThemeTokens:
-    """Semantic colour tokens for the dark theme.
-
-    Values sourced from ``DARK_STYLESHEET`` in minerva_gui.py (L75-419).
-    """
-
-    background: str = "#171922"
-    surface: str = "#20232f"
-    text: str = "#e8eaf0"
-    accent: str = "#2f5f9e"
-    success: str = "#66c38a"
-    warning: str = "#f0b75c"
-    error: str = "#ef7777"
+    background: str = "#101722"
+    surface: str = "#172230"
+    surface_raised: str = "#1d2a38"
+    border: str = "#2b3a4a"
+    text: str = "#eef3f8"
+    text_muted: str = "#94a3b8"
+    accent: str = "#3b82f6"
+    success: str = "#57c785"
+    warning: str = "#f0ad4e"
+    error: str = "#ef6b73"
+    purple: str = "#a855f7"
+
+    @classmethod
+    def for_accent(cls, name: str) -> "ThemeTokens":
+        accents = {
+            "blue": "#3b82f6",
+            "purple": "#8b5cf6",
+            "green": "#22c55e",
+        }
+        return cls(accent=accents.get(name, accents["blue"]))
 
     def to_dict(self) -> dict[str, str]:
         return {
             "background": self.background,
             "surface": self.surface,
+            "surface_raised": self.surface_raised,
+            "border": self.border,
             "text": self.text,
+            "text_muted": self.text_muted,
             "accent": self.accent,
             "success": self.success,
             "warning": self.warning,
             "error": self.error,
+            "purple": self.purple,
         }
 
 
 def _load_qss_template() -> str:
-    """Read the QSS template from disk, caching the result."""
-    qss_path = _HERE / "qss" / "base.qss"
-    return qss_path.read_text(encoding="utf-8")
+    return (_HERE / "qss" / "base.qss").read_text(encoding="utf-8")
 
 
 def build_stylesheet(tokens: ThemeTokens, density: Density) -> str:
-    """Build a fully-rendered stylesheet string from tokens and density.
-
-    Reads ``minerva/ui/theme/qss/base.qss`` and replaces ``{{token.*}}`` and
-    ``{{density.*}}`` placeholders.
-    """
     qss = _load_qss_template()
-
-    # Substitute token values
     for name, value in tokens.to_dict().items():
         qss = qss.replace(f"{{{{token.{name}}}}}", value)
-
-    # Substitute density values
-    density_map = {
-        "row_height": str(density.row_height),
-        "control_height": str(density.control_height),
-        "nav_height": str(density.nav_height),
-        "card_padding": str(density.card_padding),
-        "page_spacing": str(density.page_spacing),
-    }
-    for name, value in density_map.items():
-        qss = qss.replace(f"{{{{density.{name}}}}}", value)
-
+    for name, value in {
+        "row_height": density.row_height,
+        "control_height": density.control_height,
+        "nav_height": density.nav_height,
+        "card_padding": density.card_padding,
+        "page_spacing": density.page_spacing,
+    }.items():
+        qss = qss.replace(f"{{{{density.{name}}}}}", str(value))
     return qss
 
 
-def apply_theme(
-    qapp: "QtWidgets.QApplication",  # noqa: F821 — lazy
-    tokens: ThemeTokens | None = None,
-    density: Density | None = None,
-) -> None:
-    """Shortcut to build and apply a stylesheet to a QApplication.
-
-    Called once at startup.
-    """
-    from PyQt6 import QtWidgets
-
-    t = tokens or ThemeTokens()
-    d = density or Density.COMPACT
-    qss = build_stylesheet(t, d)
-    qapp.setStyleSheet(qss)
+def apply_theme(qapp, tokens: ThemeTokens | None = None, density: Density | None = None) -> None:
+    qapp.setStyleSheet(build_stylesheet(tokens or ThemeTokens(), density or Density.COMPACT))
diff --git a/minerva/ui/widgets/__init__.py b/minerva/ui/widgets/__init__.py
index 204eb97..c4fbc62 100644
--- a/minerva/ui/widgets/__init__.py
+++ b/minerva/ui/widgets/__init__.py
@@ -16,6 +16,8 @@ from minerva.ui.widgets.search_toolbar import SearchToolbar
 from minerva.ui.widgets.tag_flow import TagFlow
 from minerva.ui.widgets.section_card import SectionCard
 from minerva.ui.widgets.activity_list import ActivityList
+from minerva.ui.widgets.cover_art import CoverLabel, CoverProvider
+from minerva.ui.widgets.facet_list import FacetList
 
 __all__ = [
     "PageHeader",
@@ -35,4 +37,7 @@ __all__ = [
     "TagFlow",
     "SectionCard",
     "ActivityList",
+    "CoverLabel",
+    "CoverProvider",
+    "FacetList",
 ]
diff --git a/minerva/ui/widgets/cover_art.py b/minerva/ui/widgets/cover_art.py
new file mode 100644
index 0000000..4875839
--- /dev/null
+++ b/minerva/ui/widgets/cover_art.py
@@ -0,0 +1,82 @@
+"""Local cover-art resolution and a reusable cover label.
+
+Minerva deliberately keeps cover loading offline and deterministic.  The
+provider searches a user-configurable root for common naming layouts and
+returns a generic gamepad placeholder when no image is available.
+"""
+
+from __future__ import annotations
+
+import re
+from pathlib import Path
+
+from PyQt6 import QtCore, QtGui, QtWidgets
+
+from minerva.domain.library import LibraryItem
+from minerva.ui.icons import Icons
+
+_IMAGE_EXTENSIONS = (".png", ".jpg", ".jpeg", ".webp")
+
+
+def _safe_name(value: str) -> str:
+    value = re.sub(r"[<>:\\|?*\"]", "_", value).strip(" .")
+    return re.sub(r"\s+", " ", value)
+
+
+class CoverProvider:
+    """Resolve local cover images without network access."""
+
+    def __init__(self, root: str | Path = "covers") -> None:
+        self.root = Path(root)
+
+    def candidates(self, item: LibraryItem) -> list[Path]:
+        system = _safe_name(item.system or "Unknown")
+        collection = _safe_name(item.collection or "Unknown")
+        names = {
+            str(item.id),
+            _safe_name(item.stem),
+            _safe_name(Path(item.basename).stem),
+        }
+        directories = (
+            self.root / collection / system,
+            self.root / system,
+            self.root / collection,
+            self.root,
+        )
+        return [directory / f"{name}{ext}" for directory in directories for name in names for ext in _IMAGE_EXTENSIONS]
+
+    def resolve(self, item: LibraryItem) -> Path | None:
+        return next((path for path in self.candidates(item) if path.is_file()), None)
+
+
+class CoverLabel(QtWidgets.QLabel):
+    """Aspect-ratio-preserving cover preview with a graceful fallback."""
+
+    def __init__(self, width: int = 180, height: int = 240, parent=None) -> None:
+        super().__init__(parent)
+        self.setObjectName("coverPreview")
+        self.setAlignment(QtCore.Qt.AlignmentFlag.AlignCenter)
+        self.setFixedSize(width, height)
+        self.setScaledContents(False)
+        self.set_placeholder()
+
+    def set_placeholder(self) -> None:
+        self.setPixmap(Icons.app().pixmap(64, 64))
+        self.setToolTip("No local cover found")
+
+    def set_cover(self, path: str | Path | None) -> None:
+        if not path:
+            self.set_placeholder()
+            return
+        pixmap = QtGui.QPixmap(str(path))
+        if pixmap.isNull():
+            self.set_placeholder()
+            return
+        self.setPixmap(
+            pixmap.scaled(
+                self.size(),
+                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
+                QtCore.Qt.TransformationMode.SmoothTransformation,
+            )
+        )
+        self.setToolTip(str(path))
diff --git a/minerva/ui/widgets/facet_list.py b/minerva/ui/widgets/facet_list.py
new file mode 100644
index 0000000..e2f3525
--- /dev/null
+++ b/minerva/ui/widgets/facet_list.py
@@ -0,0 +1,56 @@
+"""Checkable facet list used by the Library browser."""
+
+from __future__ import annotations
+
+from PyQt6 import QtCore, QtWidgets
+
+from minerva.domain.library import FacetCount
+
+
+class FacetList(QtWidgets.QWidget):
+    selection_changed = QtCore.pyqtSignal()
+
+    def __init__(self, title: str, parent=None) -> None:
+        super().__init__(parent)
+        root = QtWidgets.QVBoxLayout(self)
+        root.setContentsMargins(0, 0, 0, 0)
+        root.setSpacing(4)
+        heading = QtWidgets.QLabel(title)
+        heading.setObjectName("sectionTitle")
+        root.addWidget(heading)
+        self._list = QtWidgets.QListWidget()
+        self._list.setObjectName("facetList")
+        self._list.setFrameShape(QtWidgets.QFrame.Shape.NoFrame)
+        self._list.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff)
+        self._list.itemChanged.connect(lambda _item: self.selection_changed.emit())
+        root.addWidget(self._list)
+
+    def set_facets(self, facets: list[FacetCount] | tuple[FacetCount, ...], selected: set[str]) -> None:
+        self._list.blockSignals(True)
+        self._list.clear()
+        for facet in facets:
+            item = QtWidgets.QListWidgetItem(f"{facet.value.title()}   {facet.count:,}")
+            item.setData(QtCore.Qt.ItemDataRole.UserRole, facet.value)
+            item.setFlags(item.flags() | QtCore.Qt.ItemFlag.ItemIsUserCheckable)
+            item.setCheckState(
+                QtCore.Qt.CheckState.Checked if facet.value in selected else QtCore.Qt.CheckState.Unchecked
+            )
+            self._list.addItem(item)
+        self._list.blockSignals(False)
+        visible_rows = min(7, max(1, self._list.count()))
+        self._list.setFixedHeight(visible_rows * 27 + 4)
+
+    def selected_values(self) -> set[str]:
+        values: set[str] = set()
+        for row in range(self._list.count()):
+            item = self._list.item(row)
+            if item.checkState() == QtCore.Qt.CheckState.Checked:
+                values.add(str(item.data(QtCore.Qt.ItemDataRole.UserRole)))
+        return values
+
+    def clear_selection(self) -> None:
+        self._list.blockSignals(True)
+        for row in range(self._list.count()):
+            self._list.item(row).setCheckState(QtCore.Qt.CheckState.Unchecked)
+        self._list.blockSignals(False)
+        self.selection_changed.emit()
diff --git a/minerva/ui/widgets/metric_card.py b/minerva/ui/widgets/metric_card.py
index 3ee1f09..1f28622 100644
--- a/minerva/ui/widgets/metric_card.py
+++ b/minerva/ui/widgets/metric_card.py
@@ -1,15 +1,13 @@
-"""
-Metric card — labelled value with semantic colouring.
-"""
+"""Dashboard metric card with icon, value, subtitle, and accent bar."""
 
 from __future__ import annotations
 
 from enum import Enum, auto
 
-from PyQt6 import QtCore, QtWidgets
+from PyQt6 import QtGui, QtWidgets
 
-from minerva.ui.theme import ThemeTokens
 from minerva.ui.density import Density
+from minerva.ui.theme import ThemeTokens
 
 
 class MetricKind(Enum):
@@ -17,23 +15,20 @@ class MetricKind(Enum):
     SUCCESS = auto()
     WARNING = auto()
     ERROR = auto()
+    INFO = auto()
+    PURPLE = auto()
 
 
 class MetricCard(QtWidgets.QFrame):
-    """Compact card showing a labelled metric value.
-
-    Kind controls the label colour:
-    - ``NEUTRAL``: theme text colour (default)
-    - ``SUCCESS``: ``tokens.success``
-    - ``WARNING``: ``tokens.warning``
-    - ``ERROR``: ``tokens.error``
-    """
+    """Compact KPI card used throughout the dashboard mockups."""
 
     _KIND_LABEL_NAMES = {
         MetricKind.NEUTRAL: "metricLabel",
         MetricKind.SUCCESS: "successLabel",
         MetricKind.WARNING: "warningLabel",
         MetricKind.ERROR: "errorLabel",
+        MetricKind.INFO: "infoLabel",
+        MetricKind.PURPLE: "purpleLabel",
     }
 
     def __init__(
@@ -44,37 +39,78 @@ class MetricCard(QtWidgets.QFrame):
         tokens: ThemeTokens = ThemeTokens(),
         density: Density = Density.COMPACT,
         parent: QtWidgets.QWidget | None = None,
+        *,
+        icon: QtGui.QIcon | None = None,
+        subtitle: str = "",
     ) -> None:
         super().__init__(parent)
         self._tokens = tokens
         self._density = density
-
+        self._kind = kind
         self.setObjectName("surfaceCard")
-
-        layout = QtWidgets.QVBoxLayout(self)
-        layout.setContentsMargins(density.card_padding, density.card_padding,
-                                   density.card_padding, density.card_padding)
-        layout.setSpacing(4)
+        self.setMinimumHeight(92)
+
+        root = QtWidgets.QVBoxLayout(self)
+        root.setContentsMargins(
+            density.card_padding,
+            density.card_padding,
+            density.card_padding,
+            max(8, density.card_padding - 2),
+        )
+        root.setSpacing(5)
+
+        top = QtWidgets.QHBoxLayout()
+        top.setContentsMargins(0, 0, 0, 0)
+        top.setSpacing(8)
+
+        self.icon_widget = QtWidgets.QLabel()
+        self.icon_widget.setObjectName(self._KIND_LABEL_NAMES[kind])
+        self.icon_widget.setFixedSize(24, 24)
+        self.icon_widget.setScaledContents(True)
+        self.icon_widget.setVisible(icon is not None)
+        if icon is not None:
+            self.icon_widget.setPixmap(icon.pixmap(20, 20))
+        top.addWidget(self.icon_widget)
 
         self.label_widget = QtWidgets.QLabel(label)
         self.label_widget.setObjectName("mutedLabel")
-        layout.addWidget(self.label_widget)
+        top.addWidget(self.label_widget, 1)
+        root.addLayout(top)
 
         self.value_widget = QtWidgets.QLabel(value)
         self.value_widget.setObjectName(self._KIND_LABEL_NAMES[kind])
         self.value_widget.setWordWrap(True)
-        layout.addWidget(self.value_widget)
+        root.addWidget(self.value_widget)
 
-        layout.addStretch(1)
+        self.subtitle_widget = QtWidgets.QLabel(subtitle)
+        self.subtitle_widget.setObjectName("mutedLabel")
+        self.subtitle_widget.setVisible(bool(subtitle))
+        root.addWidget(self.subtitle_widget)
+
+        self.accent_bar = QtWidgets.QFrame()
+        self.accent_bar.setObjectName(self._KIND_LABEL_NAMES[kind])
+        self.accent_bar.setFixedHeight(3)
+        root.addWidget(self.accent_bar)
 
     def set_value(self, text: str) -> None:
         self.value_widget.setText(text)
 
+    def set_subtitle(self, text: str) -> None:
+        self.subtitle_widget.setText(text)
+        self.subtitle_widget.setVisible(bool(text))
+
+    def set_icon(self, icon: QtGui.QIcon | None) -> None:
+        self.icon_widget.setVisible(icon is not None)
+        if icon is not None:
+            self.icon_widget.setPixmap(icon.pixmap(20, 20))
+
     def set_kind(self, kind: MetricKind) -> None:
-        self.value_widget.setObjectName(self._KIND_LABEL_NAMES[kind])
-        # Force style recomputation
-        self.value_widget.style().unpolish(self.value_widget)
-        self.value_widget.style().polish(self.value_widget)
+        self._kind = kind
+        object_name = self._KIND_LABEL_NAMES[kind]
+        for widget in (self.value_widget, self.icon_widget, self.accent_bar):
+            widget.setObjectName(object_name)
+            widget.style().unpolish(widget)
+            widget.style().polish(widget)
 
     def apply_tokens(self, tokens: ThemeTokens) -> None:
         self._tokens = tokens
diff --git a/minerva/ui/widgets/page_header.py b/minerva/ui/widgets/page_header.py
index 20fe170..fec98c9 100644
--- a/minerva/ui/widgets/page_header.py
+++ b/minerva/ui/widgets/page_header.py
@@ -1,19 +1,19 @@
-"""
-Page header widget — title + optional subtitle.
-"""
+"""Reusable page header with title, subtitle, and action buttons."""
 
 from __future__ import annotations
 
-from PyQt6 import QtCore, QtWidgets
+from PyQt6 import QtGui, QtWidgets
 
-from minerva.ui.theme import ThemeTokens
 from minerva.ui.density import Density
+from minerva.ui.theme import ThemeTokens
 
 
 class PageHeader(QtWidgets.QWidget):
-    """Section header with title and optional subtitle.
+    """Desktop page heading with a right-aligned action strip.
 
-    Mirrors the heading pattern in minerva_gui.py L1019-1030.
+    Existing callers that only pass a title/subtitle remain valid.  Actions can
+    be appended with :meth:`add_action` and are returned as ordinary
+    ``QPushButton`` instances so pages can enable/disable them normally.
     """
 
     def __init__(
@@ -28,28 +28,55 @@ class PageHeader(QtWidgets.QWidget):
         self._tokens = tokens
         self._density = density
 
-        layout = QtWidgets.QVBoxLayout(self)
-        layout.setContentsMargins(0, 0, 0, 0)
-        layout.setSpacing(2)
+        root = QtWidgets.QHBoxLayout(self)
+        root.setContentsMargins(0, 0, 0, 0)
+        root.setSpacing(16)
+
+        text_layout = QtWidgets.QVBoxLayout()
+        text_layout.setContentsMargins(0, 0, 0, 0)
+        text_layout.setSpacing(3)
 
         self.title_label = QtWidgets.QLabel(title)
         self.title_label.setObjectName("pageTitle")
-        layout.addWidget(self.title_label)
+        text_layout.addWidget(self.title_label)
+
+        self.subtitle_label = QtWidgets.QLabel(subtitle)
+        self.subtitle_label.setObjectName("pageSubtitle")
+        self.subtitle_label.setWordWrap(True)
+        self.subtitle_label.setVisible(bool(subtitle))
+        text_layout.addWidget(self.subtitle_label)
 
-        if subtitle:
-            self.subtitle_label = QtWidgets.QLabel(subtitle)
-            self.subtitle_label.setObjectName("pageSubtitle")
-            self.subtitle_label.setWordWrap(True)
-            layout.addWidget(self.subtitle_label)
-        else:
-            self.subtitle_label = None
+        root.addLayout(text_layout, 1)
+
+        self._actions = QtWidgets.QHBoxLayout()
+        self._actions.setContentsMargins(0, 0, 0, 0)
+        self._actions.setSpacing(8)
+        self._actions.addStretch(1)
+        root.addLayout(self._actions)
 
     def set_title(self, text: str) -> None:
         self.title_label.setText(text)
 
     def set_subtitle(self, text: str) -> None:
-        if self.subtitle_label is not None:
-            self.subtitle_label.setText(text)
+        self.subtitle_label.setText(text)
+        self.subtitle_label.setVisible(bool(text))
+
+    def add_action(
+        self,
+        text: str,
+        icon: QtGui.QIcon | None = None,
+        *,
+        primary: bool = False,
+        danger: bool = False,
+    ) -> QtWidgets.QPushButton:
+        button = QtWidgets.QPushButton(text)
+        if icon is not None:
+            button.setIcon(icon)
+        button.setObjectName(
+            "dangerButton" if danger else "primaryButton" if primary else "subtleButton"
+        )
+        self._actions.addWidget(button)
+        return button
 
     def apply_tokens(self, tokens: ThemeTokens) -> None:
         self._tokens = tokens
diff --git a/minerva/ui/widgets/search_toolbar.py b/minerva/ui/widgets/search_toolbar.py
index ea5627c..83d6ac4 100644
--- a/minerva/ui/widgets/search_toolbar.py
+++ b/minerva/ui/widgets/search_toolbar.py
@@ -5,6 +5,7 @@ Search toolbar — search input with optional collection/system filter combos.
 from __future__ import annotations
 
 from PyQt6 import QtCore, QtWidgets
+from superqt import QSearchableComboBox
 
 from minerva.ui.icons import Icons
 from minerva.ui.theme import ThemeTokens
@@ -47,7 +48,7 @@ class SearchToolbar(QtWidgets.QWidget):
 
         # ── Search input ───────────────────────────────────────────────────────
         self._search_input = QtWidgets.QLineEdit()
-        self._search_input.setPlaceholderText("Search…")
+        self._search_input.setPlaceholderText(placeholder)
         self._search_input.setClearButtonEnabled(True)
         self._search_input.addAction(
             Icons.search(), QtWidgets.QLineEdit.ActionPosition.LeadingPosition
@@ -58,7 +59,7 @@ class SearchToolbar(QtWidgets.QWidget):
         # ── Collection filter ──────────────────────────────────────────────────
         self._collection_combo: QtWidgets.QComboBox | None = None
         if show_collection_filter:
-            self._collection_combo = QtWidgets.QComboBox()
+            self._collection_combo = QSearchableComboBox()
             self._collection_combo.setMinimumWidth(140)
             self._collection_combo.addItem("All Collections", "")
             self._collection_combo.currentIndexChanged.connect(
@@ -69,7 +70,7 @@ class SearchToolbar(QtWidgets.QWidget):
         # ── System filter ──────────────────────────────────────────────────────
         self._system_combo: QtWidgets.QComboBox | None = None
         if show_system_filter:
-            self._system_combo = QtWidgets.QComboBox()
+            self._system_combo = QSearchableComboBox()
             self._system_combo.setMinimumWidth(140)
             self._system_combo.addItem("All Systems", "")
             self._system_combo.currentIndexChanged.connect(
diff --git a/minerva_cli.py b/minerva_cli.py
index 101751c..024f79f 100644
--- a/minerva_cli.py
+++ b/minerva_cli.py
@@ -15,7 +15,7 @@ import sys
 from pathlib import Path
 
 # Import only from the production modules
-from minerva_db import MinervaDB, build_index
+from minerva_db import DEFAULT_INDEX_PATH, DEFAULT_TORRENT_DIR, MinervaDB, build_index
 from minerva_qbit import QBittorrentClient
 
 
@@ -35,7 +35,7 @@ def command_index(args: argparse.Namespace) -> None:
     """Build or rebuild the torrent index."""
     import time
     t0 = time.time()
-    build_index(rebuild=getattr(args, "rebuild", False))
+    build_index(DEFAULT_TORRENT_DIR, DEFAULT_INDEX_PATH)
     print(f"Done in {time.time() - t0:.1f}s")
 
 
@@ -55,8 +55,26 @@ def command_find(args: argparse.Namespace) -> None:
 
 
 def command_download(args: argparse.Namespace) -> None:
-    """Start a download."""
-    print(f"Download file_id={args.file_id} to {args.dest or 'downloads/'}")
+    """Submit one selectively-enabled file to qBittorrent."""
+    import os
+
+    db = MinervaDB()
+    spec = db.get_download_spec(args.file_id, DEFAULT_TORRENT_DIR)
+    if spec is None:
+        raise SystemExit(f"Unknown file_id: {args.file_id}")
+    client = QBittorrentClient(
+        os.environ.get("MINERVA_QBIT_URL", "http://localhost:8080"),
+        os.environ.get("MINERVA_QBIT_USER", "admin"),
+        os.environ.get("MINERVA_QBIT_PASS", ""),
+    )
+    client.login()
+    torrent_hash = client.add_torrent_paused(str(spec.torrent_path), str(Path(args.dest).resolve()))
+    files = client.get_files(torrent_hash)
+    all_indices = [int(item["index"]) for item in files]
+    client.set_file_priority(torrent_hash, all_indices, 0)
+    client.set_file_priority(torrent_hash, [spec.select_index - 1], 1)
+    client.resume(torrent_hash)
+    print(f"Queued {spec.basename} ({torrent_hash}) → {Path(args.dest).resolve()}")
 
 
 def command_stats(args: argparse.Namespace) -> None:
diff --git a/minerva_db.py b/minerva_db.py
index e462eac..dc7817f 100644
--- a/minerva_db.py
+++ b/minerva_db.py
@@ -29,13 +29,15 @@ from typing import Any, Collection, Sequence
 # ── Domain type imports (graceful fallback for early bootstrap) ────────────────
 _HAS_DOMAIN = False
 try:
-    from minerva.domain.library import LibraryItem, LibraryQuery
+    from minerva.domain.library import FacetCount, LibraryFacets, LibraryItem, LibraryQuery
     from minerva.domain.downloads import DownloadFileSpec
     from minerva.domain.collections import (
         IndexOverview, CollectionSummary, SystemSummary, CollectionStatus,
     )
     _HAS_DOMAIN = True
 except ImportError:
+    FacetCount = None  # type: ignore[assignment, misc]
+    LibraryFacets = None  # type: ignore[assignment, misc]
     LibraryItem = None  # type: ignore[assignment, misc]
     DownloadFileSpec = None  # type: ignore[assignment, misc]
     LibraryQuery = None  # type: ignore[assignment, misc]
@@ -1582,60 +1584,111 @@ class MinervaDB:
             result.setdefault(row["file_id"], []).append(row["region"])
         return {fid: tuple(regions) for fid, regions in result.items()}
 
-    def search_library(
+    @staticmethod
+    def _category_condition(category: str) -> tuple[str, list[Any]]:
+        """Return a SQL predicate for a normalised library category."""
+        category = category.strip().lower()
+        if category == "homebrew":
+            return (
+                "id IN (SELECT file_id FROM file_tags WHERE tag = 'homebrew')",
+                [],
+            )
+        if category == "demo":
+            return (
+                "id IN (SELECT file_id FROM file_tags WHERE tag = 'demo')",
+                [],
+            )
+        if category == "tool":
+            return (
+                "id IN (SELECT file_id FROM file_tags "
+                "WHERE tag IN ('debug', 'trainer', 'inventory'))",
+                [],
+            )
+        if category == "game":
+            return (
+                "id NOT IN (SELECT file_id FROM file_tags "
+                "WHERE tag IN ('homebrew', 'demo', 'debug', 'trainer', 'inventory'))",
+                [],
+            )
+        return "1=0", []
+
+    def _build_library_where(
         self,
         query: "LibraryQuery",
-    ) -> tuple[list["LibraryItem"], int]:
-        """Search library with typed query, returning LibraryItem instances.
-
-        Uses validated sort columns only. Supports tags, regions, text search,
-        collection, system filters.
-        """
-        if not _HAS_DOMAIN:
-            raise RuntimeError("Domain types not available — cannot return LibraryItem instances")
-
-        with self.conn() as c:
-            where: list[str] = []
-            params: list[Any] = []
-
-            # Text search
-            q = query.text.strip().lower()
-            if q:
-                if len(q) >= 3:
-                    escaped = fts_escape(q)
-                    where.append(
-                        "id IN (SELECT rowid FROM files_trigram "
-                        "WHERE files_trigram MATCH ?)"
-                    )
-                    params.append(escaped)
-                else:
-                    where.append("stem LIKE ?")
-                    params.append(f"%{q}%")
-
-            # Collection / system
-            if query.collection:
-                where.append("collection = ?")
-                params.append(query.collection)
-            if query.system:
-                where.append("system = ?")
-                params.append(query.system)
+        *,
+        include_tags: bool = True,
+        include_regions: bool = True,
+        include_categories: bool = True,
+    ) -> tuple[str, list[Any]]:
+        """Build a parameterised WHERE clause shared by search and facets."""
+        where: list[str] = []
+        params: list[Any] = []
+
+        text = query.text.strip().lower()
+        if text:
+            if len(text) >= 3:
+                where.append(
+                    "id IN (SELECT rowid FROM files_trigram "
+                    "WHERE files_trigram MATCH ?)"
+                )
+                params.append(fts_escape(text))
+            else:
+                where.append("stem LIKE ?")
+                params.append(f"%{text}%")
 
-            # Tags (intersection — file must have ALL specified tags)
-            for tag in query.tags:
-                where.append("id IN (SELECT file_id FROM file_tags WHERE tag = ?)")
+        if query.collection:
+            where.append("collection = ?")
+            params.append(query.collection)
+        if query.system:
+            where.append("system = ?")
+            params.append(query.system)
+
+        if query.file_ids:
+            placeholders = ",".join("?" * len(query.file_ids))
+            where.append(f"id IN ({placeholders})")
+            params.extend(query.file_ids)
+        elif query.selected_only:
+            where.append("1=0")
+
+        if include_tags:
+            for tag in sorted(query.tags):
+                where.append(
+                    "id IN (SELECT file_id FROM file_tags WHERE tag = ?)"
+                )
                 params.append(tag)
 
-            # Regions (intersection)
-            for region in query.regions:
-                where.append("id IN (SELECT file_id FROM file_regions WHERE region = ?)")
+        if include_regions:
+            for region in sorted(query.regions):
+                where.append(
+                    "id IN (SELECT file_id FROM file_regions WHERE region = ?)"
+                )
                 params.append(region)
 
-            # Validate sort column
-            sort_col = self._SORT_COLUMNS.get(query.sort_field, "stem")
-            order = "DESC" if query.descending else "ASC"
+        if include_categories and query.categories:
+            category_parts: list[str] = []
+            for category in sorted(query.categories):
+                predicate, category_params = self._category_condition(category)
+                category_parts.append(f"({predicate})")
+                params.extend(category_params)
+            where.append("(" + " OR ".join(category_parts) + ")")
+
+        return (" AND ".join(where) if where else "1=1"), params
+
+    def search_library(
+        self,
+        query: "LibraryQuery",
+    ) -> tuple[list["LibraryItem"], int]:
+        """Search the library with validated sorting and SQL pagination."""
+        if not _HAS_DOMAIN:
+            raise RuntimeError(
+                "Domain types not available — cannot return LibraryItem instances"
+            )
 
-            where_clause = " AND ".join(where) if where else "1=1"
+        where_clause, params = self._build_library_where(query)
+        sort_col = self._SORT_COLUMNS.get(query.sort_field, "stem")
+        order = "DESC" if query.descending else "ASC"
 
+        with self.conn() as c:
             total = c.execute(
                 f"SELECT COUNT(*) FROM files WHERE {where_clause}", params
             ).fetchone()[0]
@@ -1645,28 +1698,100 @@ class MinervaDB:
 
             rows = c.execute(
                 f"SELECT * FROM files WHERE {where_clause} "
-                f"ORDER BY {sort_col} {order} "
+                f"ORDER BY {sort_col} {order}, id ASC "
                 f"LIMIT ? OFFSET ?",
                 [*params, query.limit, query.offset],
             ).fetchall()
-
             if not rows:
                 return [], total
 
-            # Batch-load tags and regions
             ids = [r["id"] for r in rows]
             tag_map = self._load_tags_for_files(c, ids)
             region_map = self._load_regions_for_files(c, ids)
-
-            items = [
+            return [
                 self._row_to_library_item(
-                    r,
-                    tags=tag_map.get(r["id"], ()),
-                    regions=region_map.get(r["id"], ()),
+                    row,
+                    tags=tag_map.get(row["id"], ()),
+                    regions=region_map.get(row["id"], ()),
                 )
-                for r in rows
-            ]
-            return items, total
+                for row in rows
+            ], total
+
+    def get_library_facets(self, query: "LibraryQuery") -> "LibraryFacets":
+        """Return region, category and tag counts for the current search.
+
+        Each facet dimension is calculated without its own selected values so
+        the UI can keep showing alternative choices while another value in the
+        same dimension is selected.
+        """
+        if not _HAS_DOMAIN:
+            raise RuntimeError("Domain types not available")
+
+        with self.conn() as c:
+            total_where, total_params = self._build_library_where(query)
+            total = c.execute(
+                f"SELECT COUNT(*) FROM files WHERE {total_where}", total_params
+            ).fetchone()[0]
+
+            region_where, region_params = self._build_library_where(
+                query, include_regions=False,
+            )
+            region_rows = c.execute(
+                "SELECT fr.region AS value, COUNT(DISTINCT fr.file_id) AS count "
+                "FROM file_regions fr JOIN files f ON f.id = fr.file_id "
+                f"WHERE {region_where.replace('id ', 'f.id ')} "
+                "GROUP BY fr.region ORDER BY count DESC, value ASC",
+                region_params,
+            ).fetchall()
+
+            tag_where, tag_params = self._build_library_where(
+                query, include_tags=False,
+            )
+            tag_rows = c.execute(
+                "SELECT ft.tag AS value, COUNT(DISTINCT ft.file_id) AS count "
+                "FROM file_tags ft JOIN files f ON f.id = ft.file_id "
+                f"WHERE {tag_where.replace('id ', 'f.id ')} "
+                "GROUP BY ft.tag ORDER BY count DESC, value ASC",
+                tag_params,
+            ).fetchall()
+
+            category_query = type(query)(
+                text=query.text,
+                collection=query.collection,
+                system=query.system,
+                tags=query.tags,
+                regions=query.regions,
+                categories=frozenset(),
+                selected_only=query.selected_only,
+                file_ids=query.file_ids,
+                offset=0,
+                limit=0,
+                sort_field=query.sort_field,
+                descending=query.descending,
+            )
+            category_where, category_params = self._build_library_where(
+                category_query, include_categories=False,
+            )
+            category_counts: list[FacetCount] = []
+            for category in ("game", "homebrew", "demo", "tool"):
+                predicate, predicate_params = self._category_condition(category)
+                count = c.execute(
+                    "SELECT COUNT(*) FROM files "
+                    f"WHERE ({category_where}) AND ({predicate})",
+                    [*category_params, *predicate_params],
+                ).fetchone()[0]
+                category_counts.append(FacetCount(category, count))
+
+        return LibraryFacets(
+            total=total,
+            regions=tuple(
+                FacetCount(row["value"], row["count"]) for row in region_rows
+            ),
+            categories=tuple(category_counts),
+            tags=tuple(
+                FacetCount(row["value"], row["count"]) for row in tag_rows
+            ),
+        )
 
     def get_files_by_ids(self, file_ids: Collection[int]) -> list["LibraryItem"]:
         """Fetch multiple files by their primary key IDs."""
@@ -1812,6 +1937,29 @@ class MinervaDB:
                 ))
             return results
 
+    def get_index_runs(self, limit: int = 10) -> list[dict[str, Any]]:
+        """Return recent index build records, newest first."""
+        with self.conn() as c:
+            rows = c.execute(
+                "SELECT id, started_at, completed_at, status, torrent_count, "
+                "file_count, error_count, elapsed_ms "
+                "FROM index_runs ORDER BY id DESC LIMIT ?",
+                (limit,),
+            ).fetchall()
+        return [dict(row) for row in rows]
+
+    def remove_collection(self, collection: str) -> int:
+        """Remove all indexed rows and torrent metadata for a collection."""
+        with self.conn() as c:
+            count = c.execute(
+                "SELECT COUNT(*) FROM files WHERE collection = ?",
+                (collection,),
+            ).fetchone()[0]
+            c.execute("DELETE FROM files WHERE collection = ?", (collection,))
+            c.execute("DELETE FROM torrents WHERE collection = ?", (collection,))
+        self.clear_cache()
+        return count
+
     def run_integrity_check(self) -> dict:
         """Run PRAGMA quick_check and return results."""
         with self.conn() as c:
diff --git a/minerva_state.py b/minerva_state.py
index b591464..42944c9 100644
--- a/minerva_state.py
+++ b/minerva_state.py
@@ -35,6 +35,7 @@ from datetime import datetime, timezone
 from pathlib import Path
 from typing import Any
 
+from minerva.domain.collections import ReferenceDat
 from minerva.domain.downloads import QueueRecord
 from minerva.domain.reports import ReportSummary, ReviewEntry
 
@@ -73,6 +74,20 @@ CREATE TABLE IF NOT EXISTS report_entries (
 CREATE INDEX IF NOT EXISTS idx_report_entries_report
     ON report_entries(report_id);
 
+
+CREATE TABLE IF NOT EXISTS reference_dats (
+    id TEXT PRIMARY KEY,
+    path TEXT NOT NULL UNIQUE,
+    name TEXT NOT NULL,
+    collection TEXT,
+    system TEXT,
+    entry_count INTEGER NOT NULL,
+    imported_at TEXT NOT NULL
+);
+
+CREATE INDEX IF NOT EXISTS idx_reference_dats_scope
+    ON reference_dats(collection, system);
+
 CREATE TABLE IF NOT EXISTS download_queue (
     id TEXT PRIMARY KEY,
     file_id INTEGER NOT NULL,
@@ -145,6 +160,19 @@ def _row_to_entry(row: sqlite3.Row) -> ReviewEntry:
     )
 
 
+
+def _row_to_reference_dat(row: sqlite3.Row) -> ReferenceDat:
+    """Convert a ``reference_dats`` row to ``ReferenceDat``."""
+    return ReferenceDat(
+        id=row["id"],
+        path=row["path"],
+        name=row["name"],
+        collection=row["collection"],
+        system=row["system"],
+        entry_count=row["entry_count"],
+        imported_at=row["imported_at"],
+    )
+
 def _row_to_queue_record(row: sqlite3.Row) -> QueueRecord:
     """Convert a ``download_queue`` row to a ``QueueRecord`` dataclass."""
     return QueueRecord(
@@ -416,6 +444,116 @@ class MinervaState:
             else:
                 log.debug("Updated entry %s → decision=%s", entry_id, decision)
 
+    def get_report_by_path(self, path: str | Path) -> ReportSummary | None:
+        """Return the report imported from *path*, if any."""
+        with self.conn() as c:
+            row = c.execute(
+                "SELECT * FROM reports WHERE path = ?",
+                (str(Path(path)),),
+            ).fetchone()
+        return _row_to_report(row) if row is not None else None
+
+    def replace_entries(self, report_id: str, entries: list[ReviewEntry]) -> None:
+        """Atomically replace every review entry belonging to *report_id*."""
+        with self.conn() as c:
+            c.execute(
+                "DELETE FROM report_entries WHERE report_id = ?",
+                (report_id,),
+            )
+            c.executemany(
+                """
+                INSERT INTO report_entries
+                    (id, report_id, ordinal, filename, size,
+                     automatic_file_id, automatic_method,
+                     automatic_confidence, decision, selected_file_id)
+                VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
+                """,
+                [
+                    (
+                        entry.id,
+                        entry.report_id,
+                        entry.ordinal,
+                        entry.filename,
+                        entry.size,
+                        entry.automatic_file_id,
+                        entry.automatic_method,
+                        entry.automatic_confidence,
+                        entry.decision,
+                        entry.selected_file_id,
+                    )
+                    for entry in entries
+                ],
+            )
+
+    def update_entry_decisions(
+        self,
+        updates: list[tuple[str, str, int | None]],
+    ) -> None:
+        """Apply decision updates in one transaction."""
+        with self.conn() as c:
+            c.executemany(
+                "UPDATE report_entries "
+                "SET decision = ?, selected_file_id = ? WHERE id = ?",
+                [(decision, file_id, entry_id) for entry_id, decision, file_id in updates],
+            )
+
+    # ── Reference DATs ───────────────────────────────────────────────────
+
+    def save_reference_dat(self, reference: ReferenceDat) -> None:
+        """Insert or replace an expected-count reference DAT."""
+        with self.conn() as c:
+            c.execute(
+                """
+                INSERT INTO reference_dats
+                    (id, path, name, collection, system, entry_count, imported_at)
+                VALUES (?, ?, ?, ?, ?, ?, ?)
+                ON CONFLICT(path) DO UPDATE SET
+                    name = excluded.name,
+                    collection = excluded.collection,
+                    system = excluded.system,
+                    entry_count = excluded.entry_count,
+                    imported_at = excluded.imported_at
+                """,
+                (
+                    reference.id,
+                    reference.path,
+                    reference.name,
+                    reference.collection,
+                    reference.system,
+                    reference.entry_count,
+                    reference.imported_at,
+                ),
+            )
+
+    def list_reference_dats(self) -> list[ReferenceDat]:
+        """Return imported reference DATs, newest first."""
+        with self.conn() as c:
+            rows = c.execute(
+                "SELECT * FROM reference_dats ORDER BY imported_at DESC"
+            ).fetchall()
+        return [_row_to_reference_dat(row) for row in rows]
+
+    def delete_reference_dat(self, reference_id: str) -> None:
+        with self.conn() as c:
+            c.execute("DELETE FROM reference_dats WHERE id = ?", (reference_id,))
+
+    def expected_counts(self) -> dict[tuple[str, str], int]:
+        """Return expected file counts keyed by collection/system."""
+        with self.conn() as c:
+            rows = c.execute(
+                """
+                SELECT COALESCE(collection, '') AS collection,
+                       COALESCE(system, '') AS system,
+                       MAX(entry_count) AS entry_count
+                FROM reference_dats
+                GROUP BY collection, system
+                """
+            ).fetchall()
+        return {
+            (row["collection"], row["system"]): row["entry_count"]
+            for row in rows
+        }
+
     # ── Download queue ────────────────────────────────────────────────────
 
     def save_queue_record(self, record: QueueRecord) -> None:
@@ -500,6 +638,24 @@ class MinervaState:
             ).fetchall()
         return [_row_to_queue_record(r) for r in rows]
 
+    def get_queue_record(self, record_id: str) -> QueueRecord | None:
+        with self.conn() as c:
+            row = c.execute(
+                "SELECT * FROM download_queue WHERE id = ?",
+                (record_id,),
+            ).fetchone()
+        return _row_to_queue_record(row) if row is not None else None
+
+    def find_queue_for_file(self, file_id: int) -> list[QueueRecord]:
+        """Return queue records for a library file, newest first."""
+        with self.conn() as c:
+            rows = c.execute(
+                "SELECT * FROM download_queue WHERE file_id = ? "
+                "ORDER BY created_at DESC",
+                (file_id,),
+            ).fetchall()
+        return [_row_to_queue_record(row) for row in rows]
+
     def delete_queue_record(self, record_id: str) -> None:
         """Remove a single record from the download queue.
 
