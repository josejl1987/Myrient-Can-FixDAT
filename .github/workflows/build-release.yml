name: Build and Release

on:
  push:
    tags:
      - 'v*'  # Triggers on tags like v1.0.0, v2.1.3, etc.

permissions:
  contents: write  # Required to create releases

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install ".[dev]" lxml pyinstaller
      
      - name: Build executable
        run: |
          pyinstaller --onefile --windowed --name MyrientCanFixDAT --add-data "minerva/ui/fonts;minerva/ui/fonts" --add-data "minerva/ui/qss;minerva/ui/qss" minerva_gui.py
      
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        with:
          files: dist/MyrientCanFixDAT.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
